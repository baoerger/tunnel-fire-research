# 基于物理约束公式发现与可微物理解码的隧道火灾火源参数反演研究方案

> 调研修订版，2026-08-06。本文以本项目为主体，结合已核实的公式发现、符号回归、方程发现和湍流闭合研究。文中的“拟建立”“拟验证”表示研究任务，不表示已经取得正式科学结论。

## 一、研究题目与核心定位

建议题目：

**基于物理约束公式发现与可微物理解码的纵向通风隧道火灾火源功率—位置反演研究**

本研究面向纵向通风隧道火灾，研究如何仅利用有限数量的顶棚温度传感器，反演火源总热释放速率 `Q` 和火源位置 `x_f`。研究不是直接训练一个从温度到火源参数的黑箱神经网络，而是先建立能够描述顶棚中心线温度分布的解析物理模型，再利用无量纲分析、受限回归和符号回归建立解析模型中未知参数的闭合公式，最后将正向温度公式用于火源直接反演、传感器布局优化和可微物理解码网络。

研究形成两个相互闭合的过程：

```text
正向过程：Q, x_f, U, D_f → 无量纲变量 → 闭合参数 → 顶棚温度 T(x)
反向过程：稀疏顶棚温度 T(x_i) → Q_hat, x_f_hat → 重建温度 T_hat(x_i)
```

最低完整闭环为：

```text
FDS数据可信度审查
→ 顶棚温度解析结构判伪
→ 无量纲参数提取
→ 物理约束闭合公式发现
→ 火源功率和位置直接反演
→ 独立测试与外部评价
```

增强闭环为：

```text
稀疏/缺测温度集合
→ 位置感知集合编码器
→ Q_hat、x_f_hat
→ 解析物理解码器
→ 温度重建、可信度和异常提示
```

## 二、研究背景与拟解决的科学问题

火源热释放速率决定火灾规模，火源位置决定高温区、烟气传播和通风控制策略。实际隧道中火源可能被烟气遮挡，视觉识别容易失效，而顶棚温度传感器布置相对简单、稳定，能够反映热烟气沿隧道的纵向输运。因此，利用稀疏顶棚温度反演火源参数具有实际意义。

现有方法存在两类不足：

1. 传统经验公式通常只描述最大温升、临界风速或某一侧衰减，难以同时表达火源功率、位置、风速、上下游非对称和峰值偏移。
2. 纯神经网络可以拟合复杂关系，但容易依赖训练分布，难以解释预测结果，也不能保证预测出的火源参数能够重新生成观测温度。

本研究重点回答：

1. 在明确的 `100 m` 条件域内，火源近场以外的顶棚中心线温度是否能由低维有效输运模型稳定描述？
2. 有效热源、输运、混合、热损失和峰值偏移能否由无量纲功率、风速和火源尺寸建立简洁、稳定的闭合关系？
3. 在风速和火源尺寸已知时，稀疏顶棚温度是否足以区分火源功率与火源位置？
4. 哪些传感器数量和位置能够降低 `Q` 与 `x_f` 的耦合和反演不确定性？
5. 将解析温度公式作为神经网络的可微解码器，是否能提高缺测、噪声和不规则布局下的稳定性？
6. 温度重建残差能否用于提示模型越界、未达准稳态或传感器异常？

## 三、研究范围、变量和适用边界

主体范围限定为固定全尺寸矩形隧道、单一地面火源、纵向通风和准稳态火灾。当前条件域为：

- 隧道尺寸：`L×W×H = 100×10×5 m`；
- 工作网格：`dx = 0.25 m`；
- 主体观测区：`x = 15–85 m`；
- FDS正式证据版本：`FDS 6.9.1`；
- 已知输入：纵向风速 `U`、火源等效直径 `D_f`、几何与环境条件；
- 反演目标：总热释放速率 `Q` 和火源位置 `x_f`；
- 主体观测：顶棚中心线下方固定高度处的准稳态气体温升。

定义：

\[
\theta(x,t)=T_c(x,t)-T_0,
\qquad
\bar\theta(x)=\overline{T_c}(x)-T_0
\]

其中 `T_c` 为统一测点高度的气体温度，`T_0` 为环境温度。主体分析不得把气体温度、热电偶响应温度、壁面温度和横截面平均烟层温度混为同一变量。

主体暂不研究多火源、车辆阻塞、坡度、机械排烟、风速与功率同时反演、任意瞬态热释放率和任意隧道尺度的普适公式。外部几何或网格结果可以作为域外探索，但必须单列，不能与核心域精度合并。

## 四、相关文献对本方案的具体支撑

### 4.1 传热关联式的符号回归：Pacheco-Vega、Cai、Sen和Yang

经重新检索，现有综述中“Cai等（2006）”更准确地对应 Pacheco-Vega、Weihua Cai、Mihir Sen 和 K. T. Yang 关于空气—水翅片管紧凑式换热器的遗传编程符号回归研究。其基本任务可写为：

\[
Nu\;\text{或}\;j=F(Re,Pr,\text{几何参数})
\]

传统方法先指定幂律结构，例如 `Nu=C Re^m Pr^n`；该研究使用遗传编程把候选表达式表示为解析树，同时搜索函数结构与系数。研究同时指出，多个不同形式的公式可能具有相近误差，不存在唯一“最佳公式”。

对本研究的直接启示是：可以对隧道火灾闭合参数搜索显式公式，但不能只报告误差最低的一个表达式；应同时考虑复杂度、物理趋势、重复搜索稳定性和独立工况表现。

### 4.2 稀缺数据与物理先验：El Hasadi和Padding（2019）

El Hasadi和Padding的准确题名为 *Solving fluid flow problems using semi-supervised symbolic regression on sparse data*，DOI：`10.1063/1.5116183`。论文利用半监督/引导符号回归研究稀疏数据条件下椭球体和球柱体颗粒阻力，目标关系可概括为：

\[
C_D=F(Re,\text{长宽比},\text{来流方向或姿态})
\]

其关键不是单纯增加样本，而是把已知理论、低雷诺数规律或可获得的辅助信息加入公式搜索。对本项目的启示是：FDS和公开试验数据有限时，应把零功率极限、无风对称、正性、远场衰减和删失条件直接写入模型结构，而不是事后筛除明显错误的公式。

### 4.3 自动寻找无量纲组合：Xie等（2022）

Xie等在 *Data-driven discovery of dimensionless numbers and governing laws from scarce measurements*（Nature Communications，DOI：`10.1038/s41467-022-35084-w`）中，把量纲不变性嵌入学习过程。设变量 `x_j` 的量纲矩阵为 `D`，无量纲组合写为：

\[
\Pi_k=\prod_{j=1}^{n}x_j^{a_{jk}},
\qquad
D\mathbf a_k=0
\]

随后再寻找：

\[
\Pi_0=F(\Pi_1,\Pi_2,\ldots)
\]

该研究曾从自然对流数据中恢复主导无量纲关系。它直接支撑本项目“先无量纲化、再做闭合公式发现”的路线，避免符号回归产生量纲不一致表达式。

### 4.4 从候选库中选择稀疏公式：Schmelzer、Dwight和Cinnella

Schmelzer等的 *Discovery of Algebraic Reynolds-Stress Models Using Sparse Symbolic Regression*（Flow, Turbulence and Combustion，DOI：`10.1007/s10494-019-00089-x`）采用 SpaRTA 思路，把雷诺应力各向异性表示为张量基展开：

\[
b_{ij}=\sum_n g_n(I_1,I_2,\ldots)T_{ij}^{(n)}
\]

再从候选基函数库中通过稀疏回归选择少数项。典型稀疏目标为：

\[
\min_{\mathbf c}
\|\mathbf A\mathbf c-\mathbf y\|_2^2
+\lambda_1\|\mathbf c\|_1
+\lambda_2\|\mathbf c\|_2^2
\]

对本项目的启示是：优先建立物理允许的候选基函数，再由数据选择少量项。该路线比完全自由的表达式搜索更稳定，也便于形成可编程闭合公式。

### 4.5 进化算法和深度符号回归生成湍流闭合项

Weatheritt和Sandberg在 *A novel evolutionary algorithm applied to algebraic modifications of the RANS stress–strain relationship*（2016，DOI：`10.1016/j.jcp.2016.08.015`）中，用进化算法生成代数应力修正，并将候选模型重新放回RANS求解器验证。Tang等在 *Discovering explicit Reynolds-averaged turbulence closures ... through deep learning-based symbolic regression*（2023，DOI：`10.1063/5.0135638`）中，将雷诺应力分为线性与非线性部分，再用LSTM和强化学习生成显式非线性项，同时利用不变量和张量基保持伽利略/旋转不变性。

两项研究共同说明：

1. 符号公式必须嵌回完整正向模型验证，不能只比较参数拟合误差；
2. 搜索空间应由物理不变量、允许算符和复杂度共同约束；
3. 简单公式有时已能达到与复杂公式接近的流场效果。

本项目据此采用“两阶段拟合”：先拟合逐工况闭合参数，再把公式代回完整温度曲线进行全局校正。

### 4.6 从稀缺时空数据发现PDE：Chen等（2021）

Chen、Liu和Sun在 *Physics-informed learning of governing equations from scarce data*（DOI：`10.1038/s41467-021-26434-1`）中提出PINN-SR类路线。先用神经网络重建连续场并通过自动微分求导，再从候选库中识别：

\[
u_t=\Theta(u,\nabla u,\nabla^2u,\ldots)\boldsymbol\lambda
\]

其中 `λ` 通过稀疏约束选出少数非零项。该论文说明少量、含噪时空数据也可能用于方程发现。

本项目与其不同：主体不准备从FDS三维场重新发现完整Navier–Stokes方程，而是由输运机理先提出一维有效方程，再用数据识别闭合关系。Chen等方法可作为后续瞬态温度传播方程研究的扩展，而不是当前硕士研究的必要条件。

### 4.7 高湍流数据的弱形式方程发现：Gurevich等（2024）

Gurevich等在 *Learning fluid physics from highly turbulent data using sparse physics-informed discovery of empirical relations (SPIDER)*（Journal of Fluid Mechanics，DOI：`10.1017/jfm.2024.813`）中寻找：

\[
\sum_n c_n f_n(\mathbf u,p,\nabla\mathbf u,\ldots)=0
\]

并通过时空积分或弱形式降低直接求高阶导数对噪声的敏感性：

\[
\int_{\Omega\times\Delta t}w
\left(\sum_n c_n f_n\right)\,d\Omega dt=0
\]

该研究从高湍流通道流数据中恢复动量、连续性和边界关系，也用残差判断数据分辨率和数值误差。对本项目的启示主要是：FDS数据在进入公式拟合前必须进行质量、守恒趋势、网格和边界诊断；完整场数据可用于解释有效参数，但当前不把三维方程发现作为主体。

## 五、FDS数据库与数据可信度

### 5.1 工况设计

主体数据库计划包含68个有效准稳态工况：

- 50个开发工况：用于结构选择、闭合拟合、分组交叉验证和超参数确定；
- 12个独立测试工况：在公式、损失、阈值和网络设置全部冻结后解封；
- 6个偏移火源工况：专门检查平移性、峰值偏移和火源定位。

变量覆盖包括火源功率 `Q`、纵向风速 `U`、火源等效直径 `D_f` 和火源位置 `x_f`。风速可按 `U/U_c` 分层，覆盖无风、弱风、临界风和强风。临界风速只用于工况分层，不能替代FDS回流判定。

### 5.2 临界风速预分层公式

当前采用Li、Lei和Ingason（2010）的候选分段式作预分层：

\[
Q_c^*=\frac{Q_c}{\rho_0c_pT_0\sqrt g H^{5/2}}
\]

\[
U_c^*=\begin{cases}
0.81(Q_c^*)^{1/3},&Q_c^*\le 0.15\\
0.43,&Q_c^*>0.15
\end{cases},
\qquad
U_c=U_c^*\sqrt{gH}
\]

该式当前仅为 `PRELIMINARY_STRATIFICATION_ONLY`。若取得可靠FDS回流证据，应以同一功率下的FDS临界风速覆盖经验估计，并保留证据路径。

### 5.3 输出与准稳态处理

每个工况至少保存：

- 顶棚中心线温度、纵向速度；
- 总HRR及 `_hrr.csv` 中的 `HRR`、`Q_RADI`；
- 上游回流长度、峰值位置和温度平台；
- 代表截面的温度、密度和速度积分；
- 必要的三维场、切片和壁面输出。

火源对流功率定义为：

\[
Q_c=Q+Q_{RADI}=(1-\chi_r)Q
\]

其中FDS的 `Q_RADI` 通常为负值。不得把计算域能量收支列 `Q_CONV` 直接当作火源对流HRR。

准稳态不按固定时段强行指定，而根据HRR、最大温度、代表测点、回流长度和纵向超温焓流的趋势联合判定。进入准稳态后取10–30 s时间窗，计算时间平均温度和移动分块Bootstrap不确定性。

### 5.4 数据隔离

同一物理工况的时间窗、噪声版本、缺测版本和布局版本必须保持在同一数据组，不允许跨训练与测试子集。所有模型选择只使用开发集；独立测试只在最终评价时一次性解封。外部试验不进入闭合公式训练。

## 六、顶棚温度解析模型

### 6.1 一维有效输运方程

对准稳态顶棚中心线温升建立：

\[
D_e\frac{d^2\theta}{dx^2}
-U_e\frac{d\theta}{dx}
-K_e\theta
=-S_f(x)
\]

其中：

- `U_e`：顶棚高温区的有效纵向输运参数；
- `D_e`：有效纵向混合/扩散参数；
- `K_e`：壁面换热、卷吸和其他损失的综合有效参数；
- `S_f(x)`：火源和羽流向顶棚高温区输入的等效热源。

这些量是拟合顶棚中心线温度的有效参数，不等于局部真实流速、分子扩散系数或唯一换热系数。

### 6.2 点源远场解析解

第一候选取：

\[
S_f(x)=S_0\delta(x-x_p)
\]

其中 `x_p` 是连续温度峰值位置。解析解为：

\[
\theta(x)=\theta_p\exp[k_u(x-x_p)],\qquad x<x_p
\]

\[
\theta(x)=\theta_p\exp[-k_d(x-x_p)],\qquad x\ge x_p
\]

\[
k_u=\frac{\sqrt{U_e^2+4D_eK_e}+U_e}{2D_e},
\qquad
k_d=\frac{\sqrt{U_e^2+4D_eK_e}-U_e}{2D_e}
\]

\[
\theta_p=\frac{S_0}{\sqrt{U_e^2+4D_eK_e}},
\qquad
k_u-k_d=\frac{U_e}{D_e},
\qquad
k_uk_d=\frac{K_e}{D_e}
\]

该结构把经验性的上下游衰减重新解释为有效输运与损失的共同结果。

### 6.3 平滑有限源备选

若点源在峰值附近产生不可接受的导数跳跃或系统残差，使用高斯有限源：

\[
S_f(x)=\frac{S_0}{\sqrt{2\pi}\sigma_s}
\exp\left[-\frac{(x-x_p)^2}{2\sigma_s^2}\right],
\qquad
\sigma_s=c_sD_f
\]

温度由格林函数卷积得到：

\[
\theta(x)=\int_{-\infty}^{\infty}G(x-s)S_f(s)\,ds
\]

`c_s` 只作为开发工况共同标定的全局参数，不允许逐工况自由调整。若有限源不能显著改善交叉验证误差和峰值附近残差，则保留更简单的点源结构。

### 6.4 无量纲温度核

定义：

\[
\xi=\frac{x-x_p}{H},\qquad
\phi=\frac{\theta}{T_0}
\]

\[
Pe_e=\frac{U_eH}{D_e},\qquad
Da_e=\frac{K_eH^2}{D_e},\qquad
\Pi_S=\frac{S_0H}{D_eT_0}
\]

则：

\[
\kappa_u=\frac{\sqrt{Pe_e^2+4Da_e}+Pe_e}{2},
\qquad
\kappa_d=\frac{\sqrt{Pe_e^2+4Da_e}-Pe_e}{2}
\]

\[
\phi(\xi)=
\frac{\Pi_S}{\sqrt{Pe_e^2+4Da_e}}
\begin{cases}
\exp(\kappa_u\xi),&\xi<0\\
\exp(-\kappa_d\xi),&\xi\ge0
\end{cases}
\]

并满足：

\[
Pe_e=\kappa_u-\kappa_d,
\qquad
Da_e=\kappa_u\kappa_d
\]

当 `Fr→0` 时，应有 `Pe_e→0`，从而 `κ_u≈κ_d`；当 `|ξ|→∞` 时，`φ→0`，温度回到环境值。

### 6.5 火源位置与温度峰值

真实火源位置和温度峰值位置分开定义：

\[
x_p=x_f+\delta_p
\]

并拟建立：

\[
\frac{\delta_p}{H}=F_\delta(Q_c^*,Fr,D_f/H)
\]

峰值不能直接取“最高温测点的位置”，而应采用局部二次拟合、样条或局部核回归得到连续峰值，并通过时间块、测点温度和测点位置扰动Bootstrap给出置信区间。若大多数工况的峰值位置区间过宽，则只保留低复杂度修正，不能强行建立复杂峰值公式。

### 6.6 近场排除和删失

点源远场拟合初始排除：

\[
|x-x_p|<0.3H\sim0.5H
\]

具体范围由开发集按完整工况交叉验证确定。温升检测阈值为：

\[
\Delta T_{th}=\max(\Delta T_{noise},\Delta T_{sensor})
\]

现阶段采用FDS种子噪声包络 `0.125 °C` 和经批准的工程传感器阈值 `5 °C`，因此暂取 `ΔT_th=5 °C`。若最终测量链变化，应重新评估系统误差。

低于阈值的强风上游数据不能拟合成虚假的指数衰减，而应使用左删失高斯/Tobit似然：

\[
\mathcal L_{cens,i}
=-\log\Phi\left(\frac{\Delta T_{th}-\mu_i}{\sigma_i}\right)
\]

## 七、解析参数提取与条件域协议

对每个质量和准稳态通过的温度剖面执行：

1. 提取连续峰值 `ΔT_p`、`x_p` 和高温平台宽度；
2. 在近场排除区外分别拟合上、下游 `log(ΔT)` 与距离的关系；
3. 至少使用预先固定的多个拟合窗口检查 `κ_u`、`κ_d` 的稳定性；
4. 主要衰减参数对窗口的相对变化原则上不超过20%；
5. 通过Bootstrap评估时间窗、测点、峰值和随机种子不确定性；
6. 只有双侧衰减均可辨识且稳定时，才计算完整 `Pe_e`、`Da_e`、`Π_S`。

条件域V2分类为：

- `bilateral_identifiable`：两侧都可进行稳定性审查；
- `upstream_censored`：上游低于检测阈值，只保留下游条件目标；
- `downstream_domain_censored`：100 m域没有提供可辨识的下游远场；
- `no_obvious_backflow`：不从上游数值波动反推衰减参数。

缺失的一侧参数必须留空并附参数掩码，不能写成零。缺一侧时，不能计算依赖双侧信息的 `Pe_e`、`Da_e` 和 `Π_S`，但完整观测温度曲线仍可进入第二阶段全曲线损失。

## 八、闭合公式的建立与寻找

### 8.1 输入与闭合对象

闭合输入为：

\[
Q_c^*=\frac{(1-\chi_r)Q}{\rho_0c_pT_0\sqrt gH^{5/2}},
\qquad
Fr=\frac{U}{\sqrt{gH}},
\qquad
d=\frac{D_f}{H}
\]

闭合对象为：

\[
\Pi_S=F_S(Q_c^*,Fr,d)
\]

\[
Pe_e=F_{Pe}(Q_c^*,Fr,d)
\]

\[
Da_e=F_{Da}(Q_c^*,Fr,d)
\]

\[
\delta_p/H=F_\delta(Q_c^*,Fr,d)
\]

符号回归只寻找这四个低维闭合关系，不直接搜索任意形式的完整 `T(Q,U,D_f,x_f,x)`。这样既保留解析核的物理解释，又显著缩小公式搜索空间。

### 8.2 硬编码的物理约束

采用潜变量 `G_y` 和 `softplus(z)=ln(1+e^z)`：

\[
Pe_e=Fr\,\operatorname{softplus}(G_{Pe})
\]

\[
Da_e=\operatorname{softplus}(G_{Da})+\varepsilon
\]

\[
\Pi_S=Q_c^*\,\operatorname{softplus}(G_S)
\]

\[
\delta_p/H=Fr\,\operatorname{softplus}(G_\delta)
\]

由此保证：无风时 `Pe_e=0`；`Da_e>0`；`Π_S>0`；零功率时有效源趋于零；无风时峰值偏移趋于零。

### 8.3 第一层：结构化低复杂度候选

先比较四类可解释候选。令 `q=Q_c^*`：

1. 幂律/对数线性基：

   \[
   \mathbf b=[1,\ln q,\ln(1+Fr),\ln d]
   \]

2. 受限非线性基：

   \[
   \mathbf b=[1,\ln q,\ln(1+Fr),\ln d,\sqrt q,Fr/(1+Fr),d]
   \]

3. 二阶响应面：

   \[
   \mathbf b=[1,q,Fr,d,q^2,Fr^2,d^2,qFr,qd,Frd]
   \]

4. 加性GAM型基：

   \[
   \mathbf b=[1,\ln q,\sqrt q,q,Fr,Fr^2,\ln d,d,d^2]
   \]

对潜变量拟合：

\[
G_y=\boldsymbol\beta_y^T\mathbf b
\]

使用带轻微岭正则的最小二乘，并按完整FDS工况进行五折分组交叉验证。若这些简单候选已经满足精度、稳定性和物理趋势，优先使用简单模型。

### 8.4 第二层：受限符号回归

仅在简单候选存在稳定系统残差时使用受限符号回归。建议以PySR、遗传编程或等价搜索器实现，并设置：

- 变量：`Q_star`、`Fr`、`Df_over_H`；
- 允许运算：`+、−、×、÷、常数幂、sqrt、log1p、softplus`；
- 幂指数：绝对值不超过3的常数；
- 最大表达式复杂度：30个语法节点；
- 禁止：高频三角函数、深层嵌套、研究域内奇点、负的耗散或源强；
- 每个候选必须在完整研究域网格上做有限值、正性和极限检查。

多目标评分建议为：

\[
J_{SR}=E_{CV}
+\lambda_C C_{expr}
+\lambda_P P_{physics}
+\lambda_S P_{instability}
\]

其中 `E_CV` 为分组交叉验证误差，`C_expr` 为公式复杂度，`P_physics` 为物理违约惩罚，`P_instability` 为不同随机种子或Bootstrap下的结构不稳定惩罚。保留误差—复杂度Pareto前沿上的多个候选，不因单次最低误差直接定稿。

### 8.5 两阶段拟合

第一阶段使用逐工况提取的闭合参数拟合 `F_S、F_Pe、F_Da、F_δ`。第二阶段把闭合公式代回完整温度核，直接最小化所有开发工况温度：

\[
\min_{\boldsymbol\beta}
\sum_{j,i}w_{ji}
\left[
\frac{T_{FDS,j}(x_i)-F_{physics}(Q_j,x_{f,j},U_j,D_{f,j},x_i;\boldsymbol\beta)}
{s_j}
\right]^2
+\lambda_c\mathcal L_{cens}
\]

其中 `s_j` 用于平衡不同温升幅值的工况。逐工况参数用于解释和初始化，完整曲线损失用于最终系数校正。这对应Weatheritt、Schmelzer和Tang等研究强调的“公式必须放回完整正向模型验证”。

### 8.6 公式稳定性

按完整工况Bootstrap重复拟合20–30次，统计：

- 变量和公式项出现频率；
- 公式结构重复率；
- 系数和指数变异；
- 去掉少量工况后的结构变化；
- 临界风速附近样本对公式的影响；
- 预测区间与域外行为。

如果符号回归与简单候选精度接近，采用更短、更稳定、更容易解释的公式。

## 九、正向模型对照与验证

至少比较五类模型：

1. 传统隧道顶棚最大温升经验公式；
2. 单工况独立上下游指数拟合；
3. 普通非线性温度回归；
4. 直接完整温度符号回归；
5. 本研究的有效输运解析核加闭合公式。

### 9.1 Kurioka等（2003）最大温升基线

为避免与本研究采用的 `Fr=U/\sqrt{gH}` 混淆，这里把Kurioka原式的无量纲量单独记为：

\[
Q_K^*=\frac{Q}{\rho_0c_pT_0\sqrt gH_{eff}^{5/2}},
\qquad
Fr_K=\frac{U^2}{gH_{eff}}
\]

令：

\[
P=\frac{(Q_K^*)^{2/3}}{Fr_K^{1/3}}
\]

则：

\[
\frac{\Delta T_{max}}{T_0}=
\begin{cases}
1.77P^{6/5},&P<1.35\\
2.54,&P\ge1.35
\end{cases}
\]

该式只用于最大顶棚气体温升，`U=0` 时奇异，不能扩展成完整纵向温度剖面。

### 9.2 Li、Lei和Ingason（2011）最大温升基线

定义：

\[
w^*=\left(\frac{Q_cg}{r\rho_0c_pT_0}\right)^{1/3},
\qquad
u'=\frac{U}{w^*}
\]

则：

\[
\Delta T_{max}=
\begin{cases}
17.5Q^{2/3}/H_{eff}^{5/3},&u'\le0.19\\
Q/(Ur^{1/3}H_{eff}^{5/3}),&u'>0.19
\end{cases}
\]

原式使用特定工程单位，程序接口必须显式换算；`r` 是火源半径而不是直径。该式同样只预测峰值，不预测峰值位置和纵向剖面。

### 9.3 评价指标

正向评价包括：

- 全温度曲线NRMSE；
- 最大温升相对误差；
- 峰值位置误差；
- 上、下游分区误差；
- 临界风速附近误差；
- 强风删失似然；
- 留功率、留风速、留尺寸、留位置区间误差；
- 无风对称极限和远场环境温度极限。

预设目标为：独立测试全曲线NRMSE中位数不超过15%，最大温升相对误差中位数不超过15%，峰值位置误差中位数不超过 `0.5H`。这些阈值必须在独立测试解封前冻结，不能根据最终结果事后修改。

## 十、火源功率与位置的直接反演

### 10.1 正向算子

闭合公式确定后，形成统一正向算子：

\[
\widehat{\Delta T}_i=
F_{physics}(Q,x_f,U,D_f,H,x_i)
\]

计算顺序为：

```text
Q → Q_c*；U → Fr；D_f → D_f/H
→ Π_S、Pe_e、Da_e、δ_p/H
→ x_p=x_f+δ_p
→ 顶棚各测点温升
```

### 10.2 参数化和目标函数

采用：

\[
\eta_1=\ln Q,
\qquad
\eta_2=x_f/H
\]

保证功率为正并统一参数尺度。若采用独立高斯噪声，目标函数为：

\[
\mathcal L_{inv}=
\frac{1}{N}\sum_iw_i
\left(\frac{\Delta T_i^{obs}-F_{physics,i}}{\sigma_i}\right)^2
+\lambda_c\mathcal L_{cens}
\]

若考虑空间相关噪声，使用：

\[
\mathcal L_{inv}=
(\mathbf y-\mathbf F)^T\Sigma^{-1}(\mathbf y-\mathbf F)
+\log|\Sigma|
+\lambda_c\mathcal L_{cens}
\]

### 10.3 求解算法

建议采用“粗搜索+多初值局部优化”：

1. 在 `Q∈[5,100] MW`、`x_f∈[15,85] m` 内计算二维粗网格；
2. 选择损失最小的若干不同初值；
3. 在 `(ln Q,x_f/H)` 空间使用有界L-BFGS-B、`least_squares`或等价局部方法；
4. 比较多个局部解和二维目标面，检查是否存在功率—位置耦合或多峰；
5. 输出 `Q_hat`、`x_f_hat`、重建温度、残差、适用域状态和置信区间。

当前仓库软件框架已经实现 `25×36` 粗网格、5个初值和局部坐标模式搜索。正式评价前可把局部阶段升级为成熟有界优化器，但必须用同一开发集固定设置，并保留当前方法作为可复现基线。

### 10.4 反演不确定性

同时使用：

- 局部Hessian近似；
- `Q` 和 `x_f` 的剖面似然/目标函数区间；
- 相关观测噪声扰动；
- 参数Bootstrap；
- 闭合公式Bootstrap传播。

若Fisher矩阵或Hessian接近奇异，应明确报告不可辨识，不能用任意数值正则化掩盖。

## 十一、可辨识性与传感器布局优化

对参数 `η=[ln Q,x_f/H]` 计算中心差分灵敏度：

\[
J_{i1}=\frac{\partial T_i}{\partial\ln Q},
\qquad
J_{i2}=\frac{\partial T_i}{\partial(x_f/H)}
\]

考虑相邻传感器空间相关噪声：

\[
\Sigma_{ij}=\sigma^2
\left[
(1-\gamma)\exp\left(-\frac{|x_i-x_j|}{\ell}\right)
+\gamma\delta_{ij}
\right]
\]

Fisher信息矩阵为：

\[
I=J^T\Sigma^{-1}J
\]

评价：

- `det(I)` 或 `log det(I)`：总体信息量；
- `cond(I)`：功率与位置耦合程度；
- `tr(I^{-1})`：局部总方差；
- 最小特征值：最弱可辨识方向。

比较4、8、12、16个传感器，以及均匀布置、火源两侧布置、单侧不利布置、强风下游加密和Fisher优化布置。布局不能只针对一个标称工况优化，应在多个 `Q、U、D_f、x_f` 场景上同时考虑平均性能和最差场景。

## 十二、可微物理解码网络

### 12.1 网络定位

神经网络不是论文成立的必要条件，而是显式公式和直接反演完成后的增强模块。其价值应通过缺测、噪声、不规则布局、批量推理或优化初值中的至少一项优势证明。

### 12.2 位置感知集合编码器

每个传感器输入：

\[
\{\Delta T_i/100,\;x_i/H,\;m_i,\;U/5,\;D_f/H\}
\]

其中 `m_i∈{0,1}` 是缺测掩码。所有传感器共享同一个小型MLP编码器：

\[
h_i=\varphi(\Delta T_i,x_i/H,m_i,U,D_f/H)
\]

对有效传感器采用掩码均值、最大值和有效数量聚合：

\[
h=[h_{mean},h_{max},N_{valid}/N_{max}]
\]

再与 `U、D_f/H、H、W、T_0` 等已知量共同输入输出头。当前软件原型采用 `5→10→6` 的共享编码器和 `18→10→2` 的输出头，规模较小，可根据开发集验证调整，但必须在独立测试前冻结。

输出约束为：

\[
\hat Q=Q_{ref}\exp(z_Q)>0
\]

\[
\hat x_f=x_{min}+(x_{max}-x_{min})\sigma(z_x)
\]

### 12.3 解析物理解码器

网络输出后执行：

\[
(\hat Q,\hat x_f)
\rightarrow
(\hat\Pi_S,\hat Pe_e,\hat Da_e,\hat\delta_p)
\rightarrow
\hat T_i^{physics}
\]

即：

\[
T^{obs}\rightarrow\hat Q,\hat x_f\rightarrow\hat T^{physics}
\]

这使网络预测不仅要接近真实火源参数，还必须能够通过物理公式重建温度。

### 12.4 联合损失

\[
\mathcal L=
\lambda_s\mathcal L_{source}
+\lambda_T\mathcal L_{recon}
+\lambda_c\mathcal L_{cens}
\]

\[
\mathcal L_{source}=
w_Q(\ln\hat Q-\ln Q)^2
+w_x\left(\frac{\hat x_f-x_f}{H}\right)^2
\]

\[
\mathcal L_{recon}=
\frac{\sum_im_i(\hat T_i^{physics}-T_i^{obs})^2/\sigma_i^2}
{\sum_im_i+\varepsilon}
\]

强风低温升点使用删失似然，不把低于检测限的随机波动当作精确温度目标。

### 12.5 训练与对照

1. 用冻结后的解析公式生成覆盖参数域的合成观测进行预训练；
2. 加入相关温度噪声、位置误差、随机缺测、偏置和模型残差；
3. 用50个开发FDS工况进行分组交叉验证和微调；
4. 设置冻结后解封12个独立测试工况；
5. 使用外部试验进行端到端域外评价。

比较四种反演方法：

1. 显式公式直接优化；
2. 普通固定向量全连接网络；
3. 纯数据位置感知集合网络；
4. 参数监督加物理解码集合网络。

评价功率相对误差、位置绝对误差、位置误差/H、温度重建误差、推理时间、缺测、噪声、布局变化和区间泛化。如果直接优化已满足需求而网络没有显著优势，应把网络定位为辅助模块。

## 十三、鲁棒性、异常提示与外部验证

鲁棒性试验包括：

- 温度白噪声与空间相关噪声；
- 随机缺测和连续区段失效；
- 传感器恒定偏置与空间漂移；
- 测点位置误差；
- 留功率、留风速、留尺寸和留位置区间；
- 临界风速附近；
- 条件域外几何与外部试验。

定义重建残差：

\[
r_i=T_i^{obs}-F_{physics}(\hat Q,\hat x_f,U,D_f,x_i)
\]

当残差幅值、空间结构或模型间分歧超过开发集阈值时，只提示可能存在模型越界、未达准稳态、传感器故障、多火源、阻塞或排烟等情况；在没有专门异常数据时，不宣称能够识别具体异常类型。

外部试验中的热电偶响应温度不能直接等同于主体气体温度。应建立热电偶响应模型，或把观测类型差异作为单独误差来源。外部几何、网格和边界超出100 m条件域时，结果标为 `OOD_EXPLORATORY`，不能替代核心域独立测试。

## 十四、当前证据状态与必须保留的科学限制

截至2026-08-06，必须在方案和论文中如实保留：

1. 网格中—细结果未收敛，用户已决定不再增加更细网格，因此不能写“网格无关”；
2. 100 m与150/200 m洞口边界对照被取消，因此不能写“洞口影响可忽略”或“长度无关”；
3. 三组官方FDS 6.9.1外部工况运行质量通过，但与试验温度吻合仍较弱，外部精度门未通过；
4. 原统一分段指数协议V1只有 `4/12` 先导工况通过，结论为 `FAIL`；
5. 当前采用100 m条件域协议V2，允许单侧可辨识、参数掩码和域删失，但现有12组参与过协议制定，不能作为V2独立验证；
6. 50个开发工况目前只有12个真实结果，另38个等待回传；12个独立测试和6个偏移工况也不能提前用于模型选择；
7. 阶段四至阶段七已经具备软件框架和合成测试，但正式闭合公式、真实反演精度、推荐布局和网络优势尚未形成。

这不等于研究路线失效，而是意味着正式论文应把研究对象表述为“100 m条件域内、允许单侧删失的有效温度模型和火源反演”，不能把原V1失败改写成成功，也不能宣称获得长隧道通用双侧衰减公式。

## 十五、建议的具体执行顺序

### 步骤1：冻结条件域和观测合同

固定FDS 6.9.1、几何、网格、温度定义、测点高度、准稳态判据、检测阈值、删失类别和数据字段。任何修改必须形成新协议版本。

### 步骤2：完成真实开发数据库

回传并审查剩余38个开发工况。逐工况检查FDS错误/警告、火源非零、风向、HRR闭合、准稳态和温度输出。失败工况不静默进入拟合。

### 步骤3：在开发集上重新判定解析结构

比较点源分段指数、平滑有限源和必要的简单条件域替代结构。分别评价双侧可辨识、上游删失和下游域删失工况。V2结构和近场范围只由开发集确定。

### 步骤4：提取逐工况参数与不确定性

输出峰值、平台、`κ_u`、`κ_d`、参数掩码、`Pe_e`、`Da_e`、`Π_S`、`δ_p/H`及Bootstrap区间。缺失侧保持空值。

### 步骤5：建立闭合公式

依次比较幂律、受限非线性、响应面、GAM和受限符号回归；采用分组交叉验证、完整曲线二阶段校正和工况Bootstrap。冻结最终公式、系数、算符、复杂度和适用域。

### 步骤6：完成正向独立评价

解封12个独立测试工况，只做一次预先规定的正式评价。与Kurioka、Li等峰值公式、独立指数、普通回归和直接温度符号回归比较。

### 步骤7：完成直接反演和布局优化

在冻结的正向模型上建立功率—位置二维目标面，执行多初值反演、置信区间和Fisher布局比较。先证明显式公式具备反演能力，再训练网络。

### 步骤8：训练物理解码网络

完成合成预训练、开发FDS微调和四模型对照。独立测试不得用于调网络结构、损失权重或停止轮数。

### 步骤9：鲁棒性和外部端到端评价

系统评估噪声、缺测、偏置、位置误差和域外试验。核心域与域外结果分开报告。

### 步骤10：形成论文证据链

每个结论绑定输入工况、模型版本、数据子集、脚本、指标和图表。合成软件结果只证明程序可运行，不作为真实科学结论。

## 十六、预期创新点

1. **条件域顶棚温度有效输运核**：用统一解析结构组织上下游非对称、无风对称极限和远场衰减，同时诚实处理单侧不可辨识。
2. **物理约束闭合公式发现**：只对有效参数进行低维符号搜索，通过无量纲、正性、零输入极限、复杂度和稳定性约束提高可解释性。
3. **峰值偏移和删失联合处理**：区分火源位置与温度峰值，避免把低于检测限的强风波动拟合成衰减规律。
4. **同一公式贯通正向与反向问题**：解析公式同时用于温度预测、直接反演、可辨识性、布局优化、置信区间和异常提示。
5. **可微物理解码反演**：要求网络预测的火源参数重新生成温度，实现参数监督与物理重建双重约束。

## 十七、建议的最终论文表述

本研究不是“利用深度学习直接预测隧道火灾”，也不是“让符号回归从零猜测完整温度方程”，而是：

> 从隧道顶棚热烟气的纵向输运、混合和热损失机理出发，建立低维有效温度解析核；以无量纲变量和物理极限约束回归或符号回归，只发现解析核中的未知闭合关系；在此基础上，利用稀疏顶棚温度直接反演火源功率和位置，并将同一解析公式作为神经网络的可微物理解码器，评价其在噪声、缺测和不规则布局下的增益。

## 十八、核心参考文献

1. Pacheco-Vega, A., Cai, W., Sen, M., & Yang, K. T. *Heat Transfer Correlations in an Air-Water Fin-Tube Compact Heat Exchanger by Symbolic Regression*. Proceedings of the ASME Heat Transfer Division, 2003, 23–28.
2. Weatheritt, J., & Sandberg, R. D. A novel evolutionary algorithm applied to algebraic modifications of the RANS stress–strain relationship. *Journal of Computational Physics*, 2016. https://doi.org/10.1016/j.jcp.2016.08.015
3. El Hasadi, Y. M. F., & Padding, J. T. Solving fluid flow problems using semi-supervised symbolic regression on sparse data. *AIP Advances*, 2019. https://doi.org/10.1063/1.5116183
4. Schmelzer, M., Dwight, R. P., & Cinnella, P. Discovery of Algebraic Reynolds-Stress Models Using Sparse Symbolic Regression. *Flow, Turbulence and Combustion*. https://doi.org/10.1007/s10494-019-00089-x
5. Chen, Z., Liu, Y., & Sun, H. Physics-informed learning of governing equations from scarce data. *Nature Communications*, 2021. https://doi.org/10.1038/s41467-021-26434-1
6. Xie, X., Samaei, A., Guo, J., Liu, W. K., & Gan, Z. Data-driven discovery of dimensionless numbers and governing laws from scarce measurements. *Nature Communications*, 2022. https://doi.org/10.1038/s41467-022-35084-w
7. Tang, H., Wang, Y., Wang, T., & Tian, L. Discovering explicit Reynolds-averaged turbulence closures for turbulent separated flows through deep learning-based symbolic regression with non-linear corrections. *Physics of Fluids*, 2023. https://doi.org/10.1063/5.0135638
8. Gurevich, D. R., Golden, M. R., Reinbold, P. A. K., & Grigoriev, R. O. Learning fluid physics from highly turbulent data using sparse physics-informed discovery of empirical relations (SPIDER). *Journal of Fluid Mechanics*, 2024. https://doi.org/10.1017/jfm.2024.813
9. Kurioka, H. et al. Fire properties in near field of square fire source with longitudinal ventilation in tunnels. *Fire Safety Journal*, 2003. https://doi.org/10.1016/S0379-7112(02)00089-9
10. Li, Y. Z., Lei, B., & Ingason, H. Study of critical velocity and backlayering length in longitudinally ventilated tunnel fires. *Fire Safety Journal*, 2010. https://doi.org/10.1016/j.firesaf.2010.07.003
11. Li, Y. Z., Lei, B., & Ingason, H. The maximum temperature of buoyancy-driven smoke flow beneath the ceiling in tunnel fires. *Fire Safety Journal*, 2011. https://doi.org/10.1016/j.firesaf.2011.02.002
