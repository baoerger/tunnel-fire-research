# 可提前完成.md — FDS 外部计算期间的并行工作清单

> 适用场景：FDS 工况在其他平台计算，计算结果回传本项目后继续分析。
>
> 本清单覆盖《实施.md》全部七个阶段，只列出等待 FDS 结果期间可以安全推进的工作，并标明不得提前定稿的内容。
>
> 使用原则：完成一项并经过检查后，将 `- [ ]` 改为 `- [x]`。凡标注“框架可先做”的任务，只表示代码、接口、测试或模板可以完成，不表示相关研究结论已经成立。

---

## 一、依赖等级

| 等级 | 含义 | 执行要求 |
|---|---|---|
| A：完全不依赖 FDS 结果 | 文献、数据结构、代码框架、合成测试、运行与回传工具 | 可立即完成并验收 |
| B：可先开发框架 | 方法实现不依赖真实结果，但参数、阈值和模型选择依赖 FDS | 可完成代码，不得提前固定最终结论 |
| C：必须等待结果 | 网格、边界、准稳态、拟合、反演和验证的数值结论 | 等对应 FDS 数据回传并通过质量检查后执行 |

本文件中的待办以 A、B 类为主；C 类仅作为依赖边界和后续交接条件记录。

---

## 二、全局优先任务

### 2.1 整个工况文件夹回传（A）

- [x] 外部平台每个 CHID 使用独立目录 `/project/fds_tunnel/runs/<chid>/`。
- [x] 计算完成后将整个目录复制到 `C:\Users\xiao.cheng\Desktop\科研\阶段一\outputs\runs\<chid>\`。
- [x] 文件夹名与 `.fds` 的 CHID 保持一致，不再建立同名嵌套目录。
- [x] 至少保留实际执行的 `.fds`、`.out`、`_devc.csv`、`_hrr.csv`，建议保留 `.end`。
- [x] 网格、长度、外部验证和代表工况额外保留 `.smv`、切片及 `.bf`。

不再使用工况登记表、SHA256、批次冻结、自动导入或 `run_manifest`。工况物理参数仍以各阶段原有 CSV 和实际执行的 `.fds` 为准。

### 2.2 轻量结果检查（A）

- [x] 实现 `src/check_fds_results.py`，直接扫描 `outputs/runs/<chid>/`。
- [x] 检查关键文件、CHID、FDS 日志中的 ERROR/WARNING/rejected 和 BURNER 问题。
- [x] 检查 DEVC/HRR 是否达到 T_END、时间顺序、缺失值、关键列和单位。
- [x] 检查 HRR 非零及约 `3×TAU_Q` 后的功率闭合。
- [x] 将版本不一致标记为 `REVIEW`，不自动判废；正式混用版本前需做代表工况对比。
- [x] 输出 `outputs/analysis/quality/result_check.csv`，状态为 `PASS/REVIEW/FAIL`。

---

# 阶段一：模型建立与外部验证

## 1.1 文献调研（A）

- [x] 核对 Kurioka、Li/Ingason、Wu & Bakar、Memorial、Runehamar 等文献的原始公式、系数、单位和适用范围。
- [x] 补全完整参考文献信息、试验编号和数据来源。
- [x] 增补 2018—2025 年隧道顶棚温度反演和数据驱动研究。
- [x] 确定阶段四正向对照公式，形成可直接编程的公式、参数和适用条件表。
- [x] 记录外部试验中气体温度、热电偶温度、壁面温度的差异，避免与主体温度定义混用。

## 1.2 FDS 平台与运行链路（A/B）

- [x] 将 README、阶段一完成报告、资源预算和运行脚本中的“单 MESH”旧说明统一更新为当前 22 MESH 状态。
- [x] 更新 Slurm 模板，使 MPI 任务数、OpenMP 线程数和 MESH 数关系明确，并阻止 MPI 进程数超过 MESH 数。
- [x] 为 22 MESH 输入增加静态合同测试：完整覆盖、无重叠、无缝隙、总单元数守恒、边界在网格线上、燃烧器不被内部边界切穿。
- [x] 为外部平台准备 22 MESH 短试算输入，不用 300 s 正式输入代替启动测试。
- [x] 建立 `22 MPI×2 OpenMP`、`22 MPI×1 OpenMP`、`11 MPI×3 OpenMP` 等候选配置的性能记录模板。
- [x] 明确短试算只验证解析、时间推进、非零 HRR、入口方向、设备列和能量文件，不用于准稳态结论。

必须等待：22 MESH FDS 实跑合法性和并行性能结论（C）。

## 1.3 网格敏感性准备（A/B）

- [x] 复核 9 个网格工况的 Q、U、Df、dx、T_END、MESH 数和 CHID。
- [x] 检查 9 个 `.fds` 的 CHID、T_END、MESH 数和工况参数是否与 `grid_sensitivity_cases.csv` 一致。
- [x] 修正并测试网格收敛分析代码，覆盖 ΔT_p、x_p、κ_u、κ_d、回流长度和壁面热流。
- [x] 修正“相对变化为 0 时被当作缺失值”的判定逻辑。
- [x] 为缺失工况、未达准稳态、衰减拟合点不足和 NaN 建立显式失败状态，禁止默认判定收敛。
- [x] 准备网格收敛结果表、图片和决策门自动报告模板。
- [x] 规划横截面超温焓和纵向超温焓流所需的切片读取或外部导出方案。

必须等待：9 个正式工况回传后才能确定生产网格（C）。

## 1.4 隧道长度与洞口边界准备（A/B）

- [x] 修正边界分析脚本中延长隧道与基线的坐标比较，峰值位置应相对火源或统一坐标原点比较。
- [x] 修正边界分析 CSV 字段名与内部字典键不一致的问题。
- [x] 实现文档中承诺的 `boundary_effect_summary.csv` 和温度曲线对比图。
- [x] 增加主体测量区逐测点温度差异，而不只比较四个汇总特征。
- [x] 修正并测试上游回流长度算法，覆盖“远处正向流、近火源负向回流”的常见符号分布。
- [x] 复核 150 m 必做工况和 200 m 可选工况，生成正式输入前明确采用的 MESH 策略。
- [x] 准备“100 m 可用/需延长”的自动决策报告模板。

必须等待：100 m 基线与 150/200 m 结果回传后才能形成洞口结论（C）。

## 1.5 外部试验复现准备（A/B）

- [x] 最终选定 3—4 个公开试验及具体试验编号。
- [x] 填完 `external_cases_template.csv` 中所有 `TBD`。
- [x] 获取或数字化试验顶棚温度数据，保存原始数据、数字化数据和来源说明。
- [x] 统一试验坐标、环境温度、温升定义、测点高度和单位。
- [x] 判断试验截面能否用现有矩形生成器表示；拱形或复杂截面必须明确几何近似及误差来源。
- [x] 为热电偶观测准备响应差异说明或响应修正方案。
- [x] 为每个试验准备 FDS 与实测测点映射表。
- [x] 完善 `validation_metrics.py` 的合成测试和异常输入处理。
- [x] 建立外部验证报告模板，预先固定 Mean Bias、NRMSE、相对离散度和峰值位置误差口径。

必须等待：外部 FDS 结果和试验数据齐全后才能评价复现精度（C）。

## 1.6 计算资源预算准备（A/B）

- [x] 按 22 MESH 重写资源预算口径，删除“当前单 MESH、单进程”的旧假设。
- [x] 建立每种网格、每种并行配置的壁钟时间、CPU 小时、峰值内存和输出量记录表。
- [x] 区分全部工况必需输出与代表工况全场输出，制定下载和归档策略。
- [x] 制定断点续算、超时重提、失败重跑和结果去重规则。
- [x] 建立 81—84 个核心工况的批次预算公式，等待实测性能后自动更新。

必须等待：外部平台短试算与正式代表工况才能给出最终资源预算（C）。

## 1.7 测点和输出规范（A/B）

- [x] 复核现有 23 个顶棚测点是否全部处于测量区，ID、坐标和高度是否与生成器一致。
- [x] 复核 4/8/12/16 个稀疏子集是否均为原始测点子集且不存在重复。
- [x] 建立偏移火源工况使用固定传感器布局的规则，避免每个工况自动改变传感器位置。
- [x] 建立所有输出设备 ID、量名、单位、用途和下游消费者的机器可读清单。
- [x] 明确哪些工况必须保留全场切片和边界文件。

必须等待：网格和洞口验证后才能最终冻结测点高度、测量区和生产输出组合（C）。

## 1.8 HRR 与对流比例准备（A/B）

- [x] 为 `check_convection_ratio.py` 增加目标 Q、U、Df 和准稳态窗口字段。
- [x] 增加 HRR 功率闭合误差、χ_r 跨工况极差和趋势统计。
- [x] 增加不足两个有效工况、窗口为空、HRR 接近零和符号异常的失败处理。
- [x] 准备固定 χ_r 与修正关系两种后续接口，暂不选择最终方案。

必须等待：多个正式工况准稳态 `_hrr.csv` 才能判断 χ_r 是否稳定（C）。

## 1.9 准稳态和时间平均准备（A/B）

- [x] 将准稳态脚本从当前 HRR 波动和 Tmax 斜率两项扩展到实施方案要求的可用判据。
- [x] 增加代表测点温度变化率。
- [x] 增加上游回流长度变化率。
- [x] 条件具备时增加纵向超温焓流变化率；暂不具备时必须在结果中标记“未检查”。
- [x] 将各阈值和窗口长度改为显式配置并写入输出文件。
- [x] 增加短于最小稳定时长、时间间隔不规则和数据缺失的测试。
- [x] 测试分块 Bootstrap、置信区间和确定性随机种子。
- [x] 建立 `steady_windows.csv` 到时间平均脚本的批量衔接工具，避免人工逐个填写 t0/t1。

必须等待：正式时序和随机种子重复结果才能确定准稳态窗口与 ΔT_noise（C）。

---

# 阶段二：先导结构判伪

## 2.1 先导工况设计（A/B）

- [x] 建立 12 组先导候选表，覆盖低/中/高 Q、无风/弱风/临界/强风、不同 Df 和不同回流状态。
- [x] 明确 12 组先导属于后续 50 组开发集，避免重复计数和数据集污染。
- [x] 为每个候选工况记录选择理由、预期 U/U_c 区间和替补工况。
- [x] 在阶段一生产网格和长度未冻结前，将先导输入标记为“候选”，不得作为正式输入提交。

## 2.2 临界风速与分层代码（A/B）

- [x] 整理临界风速候选经验式及适用范围。
- [x] 实现 U_c 初估和 U/U_c 分层工具。
- [x] 实现临界区 `0.85≤U/U_c≤1.15` 自动标记与加密检查。
- [x] 允许阶段一/先导 FDS 回流结果更新 U_c，但保留版本记录。

## 2.3 解析结构与参数提取工具（A/B）

- [x] 实现点源分段指数解析解和极限测试。
- [x] 实现高斯有限源数值卷积或解析计算接口。
- [x] 实现近场排除范围网格搜索和分组交叉验证接口。
- [x] 实现拟合区间扰动及参数变化率统计。
- [x] 用合成温度曲线验证 κ_u、κ_d、x_p 和 ΔT_p 的恢复能力。

## 2.4 强风删失与峰值提取（A/B）

- [x] 实现双侧可识别、上游删失、无明显回流三类自动分类的候选规则。
- [x] 实现删失似然或 Tobit 型损失，并用合成数据测试。
- [x] 实现峰值附近平滑、二次拟合、样条或局部核回归候选方法。
- [x] 实现时间分块和测点扰动 Bootstrap，输出 x_p 置信区间和平台宽度。
- [x] 建立五项判伪检查和 75% 通过率的自动汇总报告。

不得提前定稿：近场排除范围、点源/有限源选择、删失阈值、峰值偏移复杂度和是否扩大数据库（C）。

---

# 阶段三：准稳态数据库

## 3.1 工况设计与登记（A/B）

- [x] 建立 68 组候选工况表：50 开发、12 独立测试、6 偏移。
- [x] 实现受约束 Sobol 或拉丁超立方抽样工具。
- [x] 检查 Q、Df 和 HRRPUA 是否位于物理合理范围。
- [x] 自动检查至少 6—8 组“同功率不同尺寸/同尺寸不同功率”的匹配工况。
- [x] 自动检查无风、弱/中风、临界和强风数量是否达到设计目标。
- [x] 自动检查偏移火源是否仍处于均匀区和洞口缓冲之外。
- [x] 为随机种子重复工况预留登记结构。
- [x] 记录 9 个网格工况中可计入 68 组数据库的 3 个中网格工况，避免重复运行。

注意：阶段二判伪前只能形成候选版本，不能锁定并批量提交全部 68 组。

## 3.2 数据集隔离和数据库结构（A）

- [x] 以完整 FDS 物理工况为最小分组单位设计数据表。
- [x] 将时间窗口、噪声增强、布局变化和缺测样本绑定到原始工况 ID，禁止跨子集。
- [x] 为 12 个独立测试工况建立只读/封存机制。
- [x] 设计原始时序表、准稳态窗口表、时间平均温度表、不确定性表和工况元数据表。
- [x] 明确单位、缺失值、删失标记、流态标签和数据版本字段。
- [x] 明确同一 CHID 只使用一套正式结果，若重算则分开保存并记录采用版本。

## 3.3 批量后处理框架（A/B）

- [x] 实现所有回传工况的批量质量检查、准稳态识别、时间平均和 Bootstrap。
- [x] 输出数据库完成率、失败原因、需重算工况和封存状态。
- [x] 建立 ΔT_noise 汇总接口，结合时间 Bootstrap 和随机种子重复结果。

必须等待：阶段二通过后才能锁定工况并正式运行；正式 FDS 结果回传后才能形成数据库（C）。

---

# 阶段四：解析模型与闭合公式

## 4.1 无量纲解析核（A）

- [x] 实现 ξ、φ、Pe_e、Da_e、Π_S 的统一计算接口。
- [x] 实现点源无量纲温度核和上下游分支。
- [x] 增加 Fr→0 对称、远场趋零、Da_e>0、Π_S>0 等单元测试。
- [x] 预留有限源核接口，但在阶段二决策前不同时维护多套复杂最终模型。

## 4.2 参数提取与不确定性框架（B）

- [x] 实现 ΔT_p、x_p、κ_u、κ_d、Pe_e、Da_e、Π_S 的批量提取。
- [x] 实现时间窗口、测点、峰值位置、噪声、拟合区间和随机种子扰动接口。
- [x] 对强风删失工况允许只参与全曲线损失，不强制输出不可靠完整参数。

## 4.3 闭合模型与拟合框架（B）

- [x] 实现带 softplus 约束的 Π_S、Pe_e、Da_e、δ_p 参数化接口。
- [x] 实现幂律、受限非线性、低阶响应面和广义加性模型候选。
- [x] 为受限符号回归设计允许算符、复杂度上限和奇点检查。
- [x] 实现按完整物理工况分组的交叉验证。
- [x] 实现逐工况初拟合和全温度曲线全局优化两阶段流程。
- [x] 实现按工况 Bootstrap 的公式稳定性评价。

## 4.4 正向验证和全场解释工具（A/B）

- [x] 实现传统经验式、独立指数、普通回归、符号回归和解析核模型的统一评价接口。
- [x] 固定 NRMSE、最大温升、峰值位置、上下游误差和区间泛化指标。
- [x] 开发横截面超温焓 C_T、纵向超温焓流 J_T 和 `U_e^field` 读取/计算工具。
- [x] 用合成截面数据验证积分和单位处理。

不得提前定稿：闭合变量选择、公式结构、系数、预测区间和独立测试结论（C）。

---

# 阶段五：可辨识性与直接反演

## 5.1 直接反演求解器（A/B）

- [x] 建立 `F_physics` 统一接口，允许先接入合成或占位参数进行软件测试。
- [x] 实现 Q、x_f 粗网格搜索。
- [x] 实现多初值局部优化接口（L-BFGS、LM 或等价方法）。
- [x] 实现参数边界、删失损失和温度重建残差。
- [x] 用已知合成参数验证能否恢复 Q 和 x_f。

## 5.2 Fisher 信息和布局优化（A/B）

- [x] 实现对 log Q、x_f/H 的数值或自动微分灵敏度。
- [x] 实现含空间相关性的观测噪声协方差。
- [x] 实现 det(I)、cond(I) 和 tr(I^-1) 指标。
- [x] 生成并检查 4/8/12/16 个传感器的均匀、双侧、下游偏置和候选优化布局。
- [x] 实现多场景平均信息量和最差场景信息量优化。
- [x] 实现 Q—位置二维目标函数和剖面似然图。

## 5.3 置信区间框架（A/B）

- [x] 实现局部 Hessian、剖面目标函数、观测扰动和参数 Bootstrap 接口。
- [x] 用合成噪声验证传感器减少或噪声增加时区间能够合理变宽。

不得提前定稿：真实反演误差、推荐布局、可辨识性结论和置信区间覆盖率（C）。

---

# 阶段六：可微物理解码网络

## 6.1 网络与数据接口（A/B）

- [x] 定义单传感器输入 `{ΔT_i, x_i/H, m_i}` 和已知条件 U、Df/H、H、W、T_0。
- [x] 实现共享传感器编码器、掩码均值、最大值和有效数量聚合。
- [x] 实现保证 Q>0、x_f 位于允许区间的约束输出层。
- [x] 增加传感器顺序置换不变性、数量变化和随机缺测测试。

## 6.2 合成数据和物理解码框架（B）

- [x] 建立合成参数采样、温度噪声、位置误差、缺测和模型残差扰动工具。
- [x] 建立可替换的 `F_physics` 解码器接口，先用合成公式验证梯度和训练流程。
- [x] 实现参数监督、温度重建和删失损失的联合损失。
- [x] 明确合成数据只用于训练和软件验证，不作为独立物理证据。

## 6.3 训练和四模型对照框架（A/B）

- [x] 建立预训练、开发集微调、分组交叉验证和独立测试流程。
- [x] 固定随机种子、模型版本、数据版本和训练日志格式。
- [x] 建立四模型统一评价接口：直接优化、全连接网络、纯数据集合网络、物理解码网络。
- [x] 预先固定噪声、缺测、不规则布局、推理时间和区间泛化评价方法。

不得提前进行：用未定稿闭合公式生成最终预训练集、用独立测试集调参、宣称网络优于直接反演（C）。

---

# 阶段七：鲁棒性与论文总结

## 7.1 鲁棒性测试工具（A/B）

- [x] 实现温度噪声、随机缺测、温度偏置、漂移和测点位置误差注入。
- [x] 实现留功率、留风速、留尺寸和留位置区间评价分组。
- [x] 建立直接反演与物理解码网络的统一鲁棒性矩阵模板。

## 7.2 外部端到端反演准备（A/B）

- [x] 建立外部试验观测到模型传感器输入的转换流程。
- [x] 记录试验已知 Q、x_f、U、Df、几何、测点高度和温度类型。
- [x] 建立外部端到端评价表和误差口径。

## 7.3 不确定性和异常提示框架（A/B）

- [x] 建立正向公式、直接反演和网络三层不确定性汇总格式。
- [x] 实现温度重建残差计算和空间残差图。
- [x] 设计基于开发工况残差分布的异常阈值接口。
- [x] 明确异常残差只提示模型失配，不能在缺少专项数据时诊断具体异常类型。

## 7.4 可选瞬态与论文材料（A/B）

- [x] 设计 6 个 t² 增长火灾候选工况和命名规则，但暂不提交 FDS。
- [x] 建立验收复核表、创新点—证据对应表和预期成果清单。
- [x] 建立论文方法章节、数据流程图、结果表和图件模板。
- [x] 为每项结论预留“数据来源、工况子集、版本、脚本、评价指标”追溯字段。

必须等待：阶段四至六完成后才能形成鲁棒性、外部反演、不确定性、网络优势和瞬态适用性结论（C）。

---

## 三、外部 FDS 计算的推荐批次与决策门

### 批次 0：本地准备

- [x] 完成整个工况文件夹回传约定和轻量结果检查工具。
- [x] 完成 22 MESH 运行配置和后处理脚本修复。
- [x] 生成专用 22 MESH 短试算输入与校验清单。

### 批次 1：22 MESH 短试算和并行基准

输入来源：

```text
C:\Users\xiao.cheng\Desktop\科研\阶段一\outputs\inputs\<smoke_chid>.fds
```

外部输出目录：

```text
/project/fds_tunnel/runs/<smoke_chid>/
```

回传目录：

```text
C:\Users\xiao.cheng\Desktop\科研\阶段一\outputs\runs\<smoke_chid>\
```

- [ ] 检查输入解析、时间推进、MESH 通信、负载、非零 HRR、入口方向、设备 CSV 和 `_hrr.csv`。
- [ ] 比较候选 MPI/OpenMP 配置的壁钟时间和资源占用。
- [ ] 通过后才提交正式批次。

### 批次 2：网格敏感性

输入位置：

```text
C:\Users\xiao.cheng\Desktop\科研\阶段一\fds_cases\gsA_c.fds ... gsC_f.fds
```

CHID 与参数关系以：

```text
C:\Users\xiao.cheng\Desktop\科研\阶段一\03_网格敏感性\grid_sensitivity_cases.csv
```

为准。

- [ ] 运行 9 个正式工况并回传最低文件和全场文件。
- [ ] 通过准稳态、时间平均和网格收敛决策门。
- [ ] 确定生产网格。

### 批次 3：长度边界与外部验证

- [ ] `lenA_150`、`lenC_150` 已由用户取消，不进入当前运行队列；本项保留未完成以表明洞口边界未验证。
- [ ] 200 m 可选工况同样取消，除非用户将来重新开启长度验证。
- [ ] 外部试验参数、几何和测点映射完成后运行 3—4 个外部复现工况。
- [ ] 外部复现通过后按 100 m、0.25 m 条件性范围继续；网格和洞口原决策门不得改写成通过。

### 批次 4：12 组先导

- [x] 按用户确认的 100 m、0.25 m 条件性范围生成 12 个先导输入；允许与 3 个外部验证工况并行提交。
- [ ] 完成结构判伪、近场范围、源项形式、删失规则和峰值稳定性决策。
- [ ] 只有阶段二决策门通过才扩大数据库。

### 批次 5：68 组数据库与随机种子

- [ ] 锁定 50 开发、12 测试、6 偏移工况及版本。
- [ ] 扣除已完成的先导和可计入数据库的中网格工况，避免重复计算。
- [ ] 可同时计算独立测试工况，但结果必须封存。
- [ ] 运行代表性随机种子重复工况。
- [ ] 普通数据库工况回传最低文件；代表工况同时回传全场文件。

### 批次 6：可选瞬态

- [ ] 仅当准稳态模型、直接反演和网络对照均完成后再决定是否运行。
- [ ] 不因外部平台当前空闲而提前运行尚未通过决策门的瞬态工况。

---

## 四、建议执行优先级

### P0：外部计算提交前必须完成

1. 整个工况文件夹回传目录约定；
2. 轻量结果检查工具；
3. 22 MESH 运行脚本、Slurm 和文档一致性；
4. 准稳态、回流长度和边界分析脚本修复；
5. 22 MESH 专用短试算输入与并行基准方案。

### P1：FDS 运行期间优先完成

1. 外部试验参数和观测数据整理；
2. 阶段二 12 组先导候选设计及全部分析代码；
3. 阶段三候选数据库、工况表和数据隔离机制；
4. 阶段四解析核、参数提取和闭合拟合框架；
5. 阶段五直接反演和 Fisher 工具。

### P2：在前述工作稳定后推进

1. 阶段六网络、合成数据和四模型对照框架；
2. 阶段七鲁棒性、不确定性、异常残差和论文模板；
3. 可选瞬态工况设计，但不提前提交计算。

---

## 五、禁止提前形成的结论

- [ ] 未完成网格敏感性前，不宣称 0.25 m 是生产网格。
- [ ] 未完成长度对照前，不宣称 100 m 隧道不受洞口影响。
- [ ] 未完成外部复现前，不宣称 FDS 平台已经外部验证。
- [ ] 未完成多工况准稳态检查前，不固定最终 χ_r。
- [ ] 未完成 12 组先导判伪前，不批量运行全部 68 组数据库。
- [ ] 未完成数据库前，不确定最终闭合公式和系数。
- [ ] 未完成独立测试前，不报告最终正向或反演精度。
- [ ] 未完成直接反演基线前，不宣称神经网络具有必要性或优势。
- [ ] 未完成正式鲁棒性试验前，不形成噪声、缺测或区间泛化结论。

最终依赖关系保持为：

```text
22 MESH 短试算
→ 网格/边界/外部验证
→ 阶段一验收
→ 12 组先导结构判伪
→ 68 组数据库
→ 解析闭合公式
→ 直接反演与传感器优化
→ 物理解码网络
→ 鲁棒性、外部端到端与论文结论
```

---

## 六、循环执行记录

### 2026-07-29 · 第 1 轮

- 本轮任务：完成阶段一 P0 外部提交与后处理链路。
- 选择原因：它是所有正式 FDS 回传与后续科学决策的共同前置，且当前不依赖正式结果。
- 修改文件：`阶段一/src` 中运行、网格、边界、准稳态、时间平均、对流比例和 CSV 读取脚本；阶段一测试；22 MESH/长度 CSV；阶段一 README、模型、预算、测点与报告文档；本清单。
- 实际命令：两次生成 9 个网格输入；生成 `outputs/inputs/smoke_22mesh.fds`；Git Bash `bash -n` 检查 3 个运行脚本；标准库环境与 `uv --with numpy --with matplotlib` 两套单元测试；`compileall`；无结果预检网格/边界脚本；读取两套历史 5 s CSV。
- 验证结果：38/38 测试通过；带 Matplotlib 的合成网格分析实际生成 CSV 与 PNG；预检在缺少真实结果/准稳态窗口时明确输出 FAIL；9 个跟踪 `.fds` 与 CSV/生成器逐字一致；短输入为 22 MESH、60 s。
- 任务状态：工程准备完成；22 MESH 实跑合法性、性能、网格/边界/外部科学结论仍等待外部结果。
- 未解决问题：`outputs/runs` 尚无正式回传；历史 `smoke_gsB_v6101` 与旧 `smoke_gsB` 均只有 5 s，前者最大 HRR 15.804 MW，后者异常达到 353.952 MW，均不得用于准稳态结论。
- 下一轮候选：阶段一其余不依赖结果的文献/外部试验数据、验证指标和资源预算完善；阶段二先导工具。

### 2026-07-29 · 第 2 轮

- 本轮任务：完成阶段一外部验证资料、官方输入提交包和资源预算准备。
- 选择原因：它是阶段一外部验证和阶段七端到端反演的共同数据前置，优先级为 P1，且除正式复现精度与性能数值外不依赖待回传 FDS 结果。
- 修改文件：`05_外部试验复现` 的工况表、测点映射、来源说明、真实观测剖面和验证报告模板；`src/generate_fds_case.py`、`prepare_external_validation.py`、`validation_metrics.py`；`06_计算资源预算/计算资源预算表.md`、`00_外部计算与回传/parallel_benchmark.csv`；阶段一 README、文献笔记、完成报告和测试；本清单。
- 实际命令：重新读取固定规则/阶段 README/全部测试并扫描工作树；`git ls-remote` 核验 firemodels/fds 与 exp HEAD，GitHub tree API 核验原始路径；`uv run --python 3.12 python src/generate_fds_case.py --csv 05_外部试验复现/external_cases_template.csv --outdir outputs/external_inputs`；`uv run --python 3.12 python src/prepare_external_validation.py`；PowerShell 比较提交输入与源快照 SHA256；标准库环境及 `uv run --python 3.12 --with numpy --with matplotlib python -m unittest discover -s tests -v`；`uv run --python 3.12 python -m compileall -q src tests`。完整最终测试日志：`阶段一/outputs/logs/round2_unittest.log`。
- 测试或检查结果：远端 commit 分别为 `724cf63...` 与 `d32179c...`；3 个外部提交目录成功生成且 `.fds` 逐字一致，IFAB 的 2 个 CATF 文件齐全；真实观测生成 Arup 9 点、CSTB 6 点、IFAB 11 点；44/44 测试通过，含外部回传专用测点/全场合同并实际生成合成收敛图；compileall 通过。首次直接调用系统 `python` 返回 9009，随后统一使用 uv；首次整理脚本暴露 `STAGE_DIR` 导入错误，改为现有 `STAGE_ROOT` 后通过。
- 任务状态：A/B 准备完成。外部 FDS 复现精度、阶段一外部验证决策门和最终资源可承载结论仍明确等待真实回传/目标平台实测。
- 未解决问题：`outputs/runs` 仍不存在正式回传；`parallel_benchmark.csv` 的 walltime/CPU/内存/输出量保持空白；未在目标 FDS 6.10.1 平台验证 restart 链路，因此超时工况当前须完整重跑。
- 下一轮候选：阶段二 12 组先导候选设计、临界风速分层和解析结构/删失/峰值工具；或继续阶段一尚未完成的原始公式与 2018—2025 文献核对。

### 2026-07-29 · 第 3 轮

- 本轮任务：完成阶段二 12 组先导候选设计与临界风速分层工具。
- 选择原因：这是阶段一验收后的直接下一决策门，也是后续 68 组数据库的前置；候选设计、公式初估和软件合同不依赖正式 FDS 回传。
- 修改文件：新增 `阶段二/README.md`、`01_先导工况设计/pilot_design_targets.csv`、生成的 `pilot_cases_candidate.csv` 与 `critical_velocity_estimates.csv`、临界风速说明、先导设计报告、`src/design_pilot_cases.py` 和 `tests/test_pilot_design.py`；更新本清单。
- 实际命令：`uv run --python 3.12 python 阶段二/src/design_pilot_cases.py`；`uv run --python 3.12 python -m unittest discover -s 阶段二/tests -v`；查看生成 CSV 并核验覆盖计数。
- 测试或检查结果：生成 12 组候选和 7 个功率档 U_c 表；覆盖 `no_wind=1, weak=2, near_critical=6, strong=3`；3 个中网格工况被复用；至少 8 个候选形成同 Q/同 U/不同 Df 配对；离散 HRRPUA 为 1111.111–2500.000 kW/m²；5/5 测试通过，含输出逐字再生成和带证据 FDS U_c 覆盖测试。
- 任务状态：候选设计与代码完成；全部行标为 `WAITING_STAGE1_GATE`，没有生成阶段二正式 `.fds`，没有形成真实 U_c 或回流流态结论。
- 未解决问题：当前 U_c 使用 Li–Lei–Ingason 分段关联和暂定 `chi_r=0.30`，须由阶段一/先导真实回流结果更新；生产网格与长度尚未冻结。
- 下一轮候选：阶段二点源/有限源解析核、近场与拟合区间扰动工具；随后实现强风删失和峰值 Bootstrap。

### 2026-07-29 · 第 4 轮

- 本轮任务：完成阶段二点源/有限源解析结构、近场排除和拟合区间扰动的软件工具。
- 选择原因：它是 12 组先导结构判伪的直接分析前置，且解析核、数值卷积、拟合和合成恢复可在无正式 FDS 结果时严格测试。
- 修改文件：新增 `阶段二/src/transport_model.py`、`02_解析结构与参数提取/解析结构软件验证报告.md`、生成的 `synthetic_software_check.csv`、`tests/test_transport_model.py`；更新阶段二 README 与本清单。
- 实际命令：两次 `uv run --python 3.12 python -m unittest discover -s 阶段二/tests -v`（首次定位有限源零宽度数值极限断言，改用更接近零的宽度后重跑）；`uv run --python 3.12 python 阶段二/src/transport_model.py`；读取合成检查 CSV。
- 测试或检查结果：13/13 阶段二测试通过；点源无风对称、远场趋零、Pe/Da 关系、有限源零宽度极限、无噪声参数恢复、分组近场选择、拟合区间稳定性、有限源候选接口和异常输入均通过；3 个明确标记的合成软件工况最差远场 NRMSE=0.000793。
- 任务状态：A/B 软件框架完成；合成输出仅证明实现正确，真实近场范围、点源/有限源选择、参数稳定性和 75% 通过率全部保持等待先导 FDS。
- 未解决问题：强风删失分类/似然和峰值位置 Bootstrap 尚未实现；真实热电偶/FDS 波动决定的检测阈值未知。
- 下一轮候选：阶段二强风删失、Tobit 型损失、连续峰值提取与时间/测点 Bootstrap。

### 2026-07-29 · 第 5 轮

- 本轮任务：完成阶段二强风删失、Tobit 型损失、连续峰值和 Bootstrap 软件工具。
- 选择原因：这是阶段二先导判伪剩余的直接分析前置，且分类、似然、峰值算法和汇总合同可在不依赖正式 FDS 结果时用明确标记的合成数据验证。
- 修改文件：新增 `阶段二/src/censoring_peak.py`、`03_删失与峰值/强风删失与峰值软件验证报告.md`、生成的 `synthetic_censor_peak_check.csv` 和 `tests/test_censoring_peak.py`；更新阶段二 README 与本清单。审阅后补充 CSV 布尔值严格解析和候选参数边界校验。
- 实际命令：三次 `uv run --python 3.12 python -m unittest discover -s 阶段二/tests -v`（首次验证新增实现，补回归测试后再次运行，文档更新后最终回归）；`uv run --python 3.12 python 阶段二/src/censoring_peak.py`；读取生成的合成检查 CSV；`uv run --python 3.12 --with numpy --with matplotlib python -m unittest discover -s 阶段一/tests -v`；`uv run --python 3.12 python -m compileall -q 阶段一/src 阶段一/tests 阶段二/src 阶段二/tests`。最终日志：`阶段一/outputs/logs/round5_stage1_unittest.log`、`round5_stage2_unittest.log`、`round5_compileall.log`。首次 PowerShell `Tee-Object` 因 unittest 正常写 stderr 被包装为 `NativeCommandError`，随后以原生退出码和直接日志重定向重跑，三项退出码均为 0。
- 测试或检查结果：阶段一 44/44、阶段二 21/21 测试通过，`compileall` 通过；合成删失/峰值检查 6/6 PASS；三类分类、左删失高斯损失、二次/核平滑峰值、平台宽度、时间分块和测点扰动 Bootstrap、缺失字段不默认通过均有测试。发现并修复原实现会把 CSV 非空字符串 `"false"` 当真的问题。
- 任务状态：A/B 软件框架完成；所有合成行标记 `SYNTHETIC_SOFTWARE_TEST_NOT_SCIENTIFIC_EVIDENCE`。检测阈值、时间块长、峰值置信区间和 75% 决策门仍等待真实先导/随机种子结果，未更新《实施.md》的科学结论勾选。
- 未解决问题：`阶段一/outputs/runs` 仍不存在；阶段一进入门未通过，不能正式生成或提交先导工况，也不能把回流长度为未知的低温上游曲线判作“无明显回流”。
- 下一轮候选：阶段三 68 组候选工况、数据集隔离和数据库结构；或阶段四无量纲解析核。优先阶段三，因为它同时约束后续数据污染风险且完全可先做候选/结构。

### 2026-07-29 · 第 6 轮

- 本轮任务：完成阶段三 68 组候选工况、完整物理工况级数据隔离和数据库结构。
- 选择原因：该任务是阶段二通过后扩大数据库的直接工程前置，并从源头约束后续闭合、反演和网络的数据泄漏；候选设计和合同测试不依赖正式 FDS 回传。
- 修改文件：新增 `阶段三/src/design_database_cases.py`、`case_registry.py`、`tests/test_database_design.py`、`01_工况设计/database_cases_candidate.csv`、候选设计报告、数据库结构规范和阶段三 README；将阶段一候选 `Q/A` 与离散 HRRPUA 范围集中到 `tunnel_config.py`，阶段二先导生成器和测试改读同一常量；更新本清单。
- 实际命令：`uv run --python 3.12 python 阶段三/src/design_database_cases.py`（首次被 MESH 切穿合同拒绝，修正尺寸后重跑）；`uv run --python 3.12 python -m unittest discover -s 阶段三/tests -v`；PowerShell `Import-Csv | Group-Object` 核验子集和流态计数；最终运行阶段一、二、三全部单元测试、三阶段 `compileall` 和 `git diff --check`。日志：`阶段一/outputs/logs/round6_stage1_unittest.log`、`round6_stage2_unittest.log`、`round6_stage3_unittest.log`、`round6_compileall.log`。
- 测试或检查结果：生成 68 个唯一候选 CHID，子集为 50/12/6；开发流态为无风 6、弱/中风 15、临界 17、强风 12；复用 3 个中网格工况；功率/尺寸匹配 41 行；离散 HRRPUA 为 769.2–2522.5 kW/m²；阶段一 44/44、阶段二 21/21、阶段三 8/8 测试通过，`compileall` 与 `git diff --check` 通过。首次 `Df=9.5 m` 大尺寸候选被 `x=46.0 m` 内部 MESH 边界切穿检查拒绝，收缩到 `Df=9.0 m` 后通过，未放宽合同。
- 任务状态：A/B 候选与数据隔离框架完成；全部行仍为 `WAITING_STAGE2_GATE`，没有生成阶段三 `.fds` 或 FDS 数据，独立测试访问策略为 `SEALED_UNTIL_FINAL_EVALUATION`。
- 未解决问题：Uc 和 `chi_r=0.30` 仍为初估；100 m、0.25 m 和 22 MESH 尚未通过阶段一真实结果门；阶段二判伪未通过，不能锁定并批量提交 68 组；候选 500–3000 kW/m² 范围仍需阶段一文献和正式燃烧闭合复核。
- 下一轮候选：阶段三批量质量检查—稳态—时间平均—Bootstrap 编排；或阶段四无量纲解析核。优先批量后处理，因为正式结果回传后所有阶段共同依赖该入口。

### 2026-07-29 · 第 7 轮

- 本轮任务：完成阶段三回传结果的批量质量—准稳态—时间平均—Bootstrap 编排及噪声分量汇总。
- 选择原因：这是正式结果回传后数据库入库的共同入口，能在外部计算前用合成临时目录验证错误传播和数据隔离，同时不会提前形成科学阈值。
- 修改文件：新增 `阶段三/src/batch_postprocess.py`、批量后处理软件验证报告；扩展 `tests/test_database_design.py` 和阶段三 README；生成当前真实状态的 `阶段三/outputs/analysis/batch/` 预检表；更新本清单。
- 实际命令：`uv run --python 3.12 python -m unittest discover -s 阶段三/tests -v`；`uv run --python 3.12 python 阶段三/src/batch_postprocess.py --n-boot 200`；PowerShell 分组检查 `database_completion.csv` 并列出批量产物；最终运行阶段一至三全部测试、三阶段 `compileall` 和 `git diff --check`。日志：`阶段一/outputs/logs/round7_stage1_unittest.log`、`round7_stage2_unittest.log`、`round7_stage3_unittest.log`、`round7_compileall.log`。
- 测试或检查结果：阶段一 44/44、阶段二 21/21、阶段三 10/10 测试通过，`compileall` 与 `git diff --check` 通过；合成临时回传中 1 个工况贯通质量检查、准稳态、时间平均、Bootstrap 和噪声分量，其余 49 个保持等待；当前真实项目预检为 `WAITING_RETURN=50`、完成率 0/50，所有无真实数据的分析表只有表头。两重复种子合成分量能进入 `READY_FOR_THRESHOLD_REVIEW`，单种子保持等待。
- 任务状态：A/B 批量后处理框架完成；没有把测试数据写入正式科研结果，没有确定 `Delta T_noise`，独立测试默认不读取。
- 未解决问题：`阶段一/outputs/runs` 仍不存在；真实质量 PASS、准稳态窗口、时间平均、重复种子离散和数据库完成率均等待外部结果。
- 下一轮候选：阶段四无量纲解析核、批量参数提取与不确定性框架；这是下一个无需真实 FDS 即可用合成数据严格验证的核心方法任务。

### 2026-07-29 · 第 8 轮

- 本轮任务：完成阶段四无量纲点源核、批量参数提取和多扰动不确定性框架。
- 选择原因：这是闭合、直接反演和可微解码器的共同数学基础；极限、参数关系、删失降级和合成恢复可以在没有正式 FDS 时严格验证。
- 修改文件：新增 `阶段四/src/dimensionless_model.py`、`tests/test_dimensionless_model.py`、合成参数检查 CSV、软件验证报告和阶段四 README；更新本清单。
- 实际命令：两次 `uv run --python 3.12 python -m unittest discover -s 阶段四/tests -v`（首次通过后补严格分类和空拟合窗口回归）；`uv run --python 3.12 python 阶段四/src/dimensionless_model.py`；读取生成的合成检查 CSV；最终运行阶段一至四全部测试、四阶段 `compileall` 和 `git diff --check`。日志：`阶段一/outputs/logs/round8_stage1_unittest.log` 至 `round8_stage4_unittest.log` 及 `round8_compileall.log`。
- 测试或检查结果：阶段一 44/44、阶段二 21/21、阶段三 10/10、阶段四 7/7 测试通过，`compileall` 与 `git diff --check` 通过；3 个合成工况全部 PASS，最差远场 NRMSE=0.000477，参数 Bootstrap 成功率 100%；验证了 `Pe/Da` 关系、无风对称、远场趋零、正值约束、精确参数恢复、删失不输出完整参数、确定性多扰动区间和坏工况显式失败。
- 任务状态：A/B 数学与参数提取框架完成；合成 CSV 全部标记为非科研证据，尚未生成真实逐工况参数或闭合公式。
- 未解决问题：真实近场排除、点源/有限源选择、检测阈值、时间/种子不确定性和逐工况参数全部受阶段一至三结果阻塞。
- 下一轮候选：阶段四带 softplus 约束的闭合模型、分组交叉验证、两阶段全曲线拟合和工况 Bootstrap 框架。

### 2026-07-29 · 第 9 轮

- 本轮任务：完成阶段四物理约束闭合候选、分组交叉验证、两阶段温度拟合和工况 Bootstrap 框架。
- 选择原因：闭合框架是阶段五直接反演和阶段六物理解码器的必要接口，且约束、数据隔离、优化单调性和合成恢复可在无正式 FDS 时验证。
- 修改文件：新增 `阶段四/src/closure_models.py`、`tests/test_closure_models.py` 和闭合拟合框架软件验证报告；更新阶段四 README 与本清单。
- 实际命令：`uv run --python 3.12 python -m unittest discover -s 阶段四/tests -v`；最终运行阶段一至四全部测试、四阶段 `compileall` 和 `git diff --check`。日志：`阶段一/outputs/logs/round9_stage1_unittest.log` 至 `round9_stage4_unittest.log` 及 `round9_compileall.log`。
- 测试或检查结果：阶段一 44/44、阶段二 21/21、阶段三 10/10、阶段四 14/14 测试通过，`compileall` 与 `git diff --check` 通过；物理约束在无风处严格成立；幂律合成闭合可恢复；四候选均完成无工况泄漏的 4 折验证；二阶段温度损失不增；20 次工况 Bootstrap 固定种子可复现；禁用名称和研究域除零公式被拒绝。
- 任务状态：B 类闭合拟合框架完成；没有选择最终候选、报告真实交叉验证误差或写入闭合系数。
- 未解决问题：逐工况真实参数和全曲线均不存在，闭合比较、公式稳定性、全场物理解释和独立测试全部等待正式数据库。
- 下一轮候选：阶段五统一 `F_physics`、直接反演粗搜/多初值优化和合成参数恢复；或阶段四正向统一评价工具。

### 2026-07-29 · 第 10 轮

- 本轮任务：完成阶段五统一 `F_physics` 和 Q/x_f 直接反演软件基线。
- 选择原因：直接反演是最低完整研究闭环的核心方法，也是 Fisher 布局和网络对照的共同基线；可先用合成闭合严格检查求解流程。
- 修改文件：新增 `阶段五/src/direct_inversion.py`、`tests/test_direct_inversion.py`、直接反演软件验证报告和阶段五 README；更新本清单。
- 实际命令：`uv run --python 3.12 python -m unittest discover -s 阶段五/tests -v`；最终运行阶段一至五全部测试、五阶段 `compileall` 和 `git diff --check`。日志：`阶段一/outputs/logs/round10_stage1_unittest.log` 至 `round10_stage5_unittest.log` 及 `round10_compileall.log`。
- 测试或检查结果：阶段一 44/44、阶段二 21/21、阶段三 10/10、阶段四 14/14、阶段五 4/4 测试通过，`compileall` 与 `git diff --check` 通过；合成 Q/位置恢复、输入边界、多初值解排序和左删失损失方向均通过。
- 任务状态：A/B 直接反演框架完成；没有报告真实 Q/x_f 误差、独立测试成绩或局部最优结论。
- 未解决问题：正式 `F_physics` 依赖真实闭合公式；正式反演、置信区间和验收均等待数据库与独立测试。
- 下一轮候选：阶段五 Fisher 信息、空间相关噪声、4/8/12/16 布局优化和剖面目标函数工具。

### 2026-07-29 · 第 11 轮

- 本轮任务：完成阶段五 Fisher 信息、空间相关噪声、多场景候选布局和 Q—位置目标剖面软件框架。
- 选择原因：这是直接反演可辨识性、稀疏布局和后续置信区间的共同前置；中心差分、相关协方差、矩阵求解、布局合同和目标剖面均可在没有正式 FDS 时用明确标记的合成闭合验证。
- 修改文件：新增 `阶段五/src/fisher_layout.py`、`tests/test_fisher_layout.py` 和 `02_Fisher信息与布局/` 下的软件验证报告、灵敏度/布局/Fisher/目标面/剖面 CSV 与 SVG；更新阶段五 README 与本清单。
- 实际命令：恢复时重读固定规则、进度、阶段五 README/代码/测试及阶段一测点 CSV，并检查 `git status --short` 和 `阶段一/outputs/runs`；`uv run --python 3.12 python -m unittest discover -s 阶段五/tests -v`；`uv run --python 3.12 python 阶段五/src/fisher_layout.py`；检查生成指标中负行列式、退化场景和目标面最小值；最终并行运行阶段一至五全部测试、五阶段 `compileall` 和 `git diff --check`。完整日志：`阶段一/outputs/logs/round11_stage1_unittest.log` 至 `round11_stage5_unittest.log`、`round11_compileall.log`、`round11_diff_check.log`。
- 测试或检查结果：阶段一 44/44、阶段二 21/21、阶段三 10/10、阶段四 14/14、阶段五 13/13，共 102/102 测试通过；`compileall` 与 `git diff --check` 通过。23 个原始测点生成 24 组 4/8/12/16 点代表/候选布局，均无重复且均为原始子集；19 组在 4 个合成场景均非退化，另 5 组显式记录 1—3 个 `SINGULAR` 场景；浮点近零负行列式被按相对容差归零后所有信息行列式非负。1271 点合成目标面最小值准确位于生成真值 `Q=35 MW, x_f=50 m`。全部合成 CSV 带非科研证据标识，布局结果另带“不形成推荐”状态。
- 任务状态：A/B 类 Fisher 与候选布局框架完成；退化布局没有用正则项掩盖，贪心候选没有宣称全局组合最优或真实推荐布局，《实施.md》中的正式科学任务和阶段五进入门未勾选。
- 未解决问题：正式灵敏度和布局排序依赖阶段四真实闭合；空间协方差的标准差、相关长度与 nugget 依赖阶段三稳态波动和重复种子；真实局部最优、推荐布局和独立测试精度仍等待外部 FDS 数据链。
- 下一轮候选：阶段五局部 Hessian、目标剖面阈值、观测扰动和参数 Bootstrap 置信区间框架，并用合成噪声检查传感器减少或噪声增大时区间合理变宽。

### 2026-07-29 · 第 12 轮

- 本轮任务：完成阶段五局部 Hessian、卡方目标剖面、相关观测扰动和参数 Bootstrap 置信区间框架。
- 选择原因：置信区间是最低完整反演闭环及阶段五验收的最后一个软件前置；区间算法、相关噪声采样、参数化 Bootstrap 和宽度方向可在正式闭合前用明确标记的合成数据验证。
- 修改文件：新增 `阶段五/src/confidence_intervals.py`、`tests/test_confidence_intervals.py` 和 `03_置信区间/` 下的软件验证报告、局部区间缩放表、剖面区间 CSV/SVG、Bootstrap 参数与汇总表；为 `fisher_layout.py` 增加公开 Cholesky 因子接口；更新阶段五 README 与本清单。
- 实际命令：核对固定规则文件哈希、进度 5.3、实施 5.5、阶段五文件和正式回传目录；`uv run --python 3.12 python -m unittest discover -s 阶段五/tests -v`；`uv run --python 3.12 python 阶段五/src/confidence_intervals.py`；读取局部区间缩放、剖面边界和 Bootstrap 汇总；最终并行运行阶段一至五全部测试、五阶段 `compileall` 和 `git diff --check`。完整日志：`阶段一/outputs/logs/round12_stage1_unittest.log` 至 `round12_stage5_unittest.log`、`round12_compileall.log`、`round12_diff_check.log`。
- 测试或检查结果：阶段一 44/44、阶段二 21/21、阶段三 10/10、阶段四 14/14、阶段五 20/20，共 109/109 测试通过；`compileall` 与 `git diff --check` 通过。合成嵌套布局中，4 点、2 °C 的 Q/位置局部区间宽度为 19.01 MW/2.72 m，12 点为 6.42 MW/0.48 m；12 点噪声由 2 增至 4 °C 后增至 12.89 MW/0.96 m。861 点剖面最小值位于生成真值且未触及网格边界；40 次固定种子相关噪声 Bootstrap 完整输出，无失败样本。
- 任务状态：A/B 类置信区间软件框架完成；局部正态、剖面和参数化 Bootstrap 均明确为近似方法，合成输出全部带非科研证据及“不形成覆盖率/真实精度结论”标识，《实施.md》的独立测试验收和阶段五进入门未勾选。
- 未解决问题：真实噪声协方差、模型误差和闭合不确定性尚无正式数据；剖面网格、Bootstrap 次数与区间方法必须在开发集上固定后才能启封独立测试；真实覆盖率、局部最优错误解比例和网络潜在价值点均等待外部 FDS 数据链。
- 下一轮候选：阶段六集合编码器与数据接口，包括位置/掩码输入、置换不变聚合、约束输出以及传感器数量变化和随机缺测测试。

### 2026-07-29 · 第 13 轮

- 本轮任务：完成阶段六位置感知集合编码器、掩码聚合、约束输出和最小反向自动微分后端。
- 选择原因：这是合成预训练、物理解码和四模型对照的共同网络前置；项目当前无 Torch/JAX/NumPy 固定依赖，采用仓库内小型反向自动微分可真实验证梯度并避免增加重型环境工作流。
- 修改文件：新增 `阶段六/src/set_encoder.py`、`tests/test_set_encoder.py`、阶段六 README、`01_集合编码器/集合编码器软件验证报告.md`、合成接口检查 CSV 和明确标为未训练的初始化 JSON；更新本清单。
- 实际命令：读取实施阶段六和提前清单、扫描依赖文件、检查 uv Python 的 torch/numpy/jax 可用性及正式回传；`uv run --python 3.12 python -m unittest discover -s 阶段六/tests -v`；`uv run --python 3.12 python 阶段六/src/set_encoder.py`；读取接口检查和初始化状态；最终并行运行阶段一至六全部测试、六阶段 `compileall` 和 `git diff --check`。完整日志：`阶段一/outputs/logs/round13_stage1_unittest.log` 至 `round13_stage6_unittest.log`、`round13_compileall.log`、`round13_diff_check.log`。
- 测试或检查结果：阶段一 44/44、阶段二 21/21、阶段三 10/10、阶段四 14/14、阶段五 20/20、阶段六 8/8，共 117/117 测试通过；`compileall` 与 `git diff --check` 通过。标量自动微分与解析梯度一致；338 参数共享 MLP 的前向/反向梯度有限且多数非零；传感器重排输出差异低于 `1e-12`；被掩码值完全隔离；1/4/8/16/23 点输入、约束输出、保存加载和非有限不可信权重拒绝均通过；导出 4/4 合成接口检查通过。
- 任务状态：A/B 类网络结构合同完成；`untrained_initialization.json` 明确标记 `UNTRAINED_INITIALIZATION`，没有当作预训练权重，没有报告网络精度或相对优势，《实施.md》的网络训练和阶段六决策门未勾选。
- 未解决问题：合成扰动、物理解码、联合损失和训练尚未接入；正式解码器等待阶段四闭合固定，FDS 微调与独立测试等待外部结果及数据集解封。
- 下一轮候选：阶段六合成参数/噪声/位置/缺测/模型残差生成器、可替换的可微 `F_physics` 解码器和参数监督—温度重建—删失联合损失。

### 2026-07-29 · 第 14 轮

- 本轮任务：完成阶段六合成扰动生成器、可替换可微物理解码器和参数监督—温度重建—删失联合损失。
- 选择原因：这是网络预训练和物理约束对照的直接前置；合成扰动、解析解码一致性、Q/位置梯度及删失损失纪律可在真实闭合与 FDS 数据缺失时严格验证。
- 修改文件：扩展 `阶段六/src/set_encoder.py` 的 `erf` 自动微分；新增 `src/physics_training.py`、`tests/test_physics_training.py`、`02_合成数据与物理解码/` 下 30 个合成样本 JSONL、联合损失检查 CSV 和软件验证报告；更新阶段六 README 与本清单。
- 实际命令：`uv run --python 3.12 python -m unittest discover -s 阶段六/tests -v`；`uv run --python 3.12 python 阶段六/src/physics_training.py`；读取合成样本及联合损失/梯度摘要；最终并行运行阶段一至六全部测试、六阶段 `compileall` 和 `git diff --check`。完整日志：`阶段一/outputs/logs/round14_stage1_unittest.log` 至 `round14_stage6_unittest.log`、`round14_compileall.log`、`round14_diff_check.log`。
- 测试或检查结果：阶段一 44/44、阶段二 21/21、阶段三 10/10、阶段四 14/14、阶段五 20/20、阶段六 15/15，共 124/124 测试通过；`compileall` 与 `git diff --check` 通过。可微解码器逐点匹配阶段五同一合成前向模型，Q/位置梯度匹配有限差分；30 个样本覆盖 4/8/12/16/23 点，含 57 个缺测、169 个删失和 378 个位置扰动；首样本三个损失分量均有限，338/338 参数获得有限非零梯度；删失损失不使用检测阈值以下的具体观测值。
- 任务状态：A/B 类合成数据与物理解码联合损失框架完成；所有样本均带合成非证据标签，解码器状态明确为 `SYNTHETIC_CONSTANT_LATENT_CLOSURE_NOT_FORMAL`，没有形成真实噪声、预训练范围或网络性能结论。
- 未解决问题：正式物理解码仍等待阶段四闭合；当前扰动幅值只用于软件测试；尚未实现训练日志、开发集微调/交叉验证、独立测试封存和四模型统一评价。
- 下一轮候选：阶段六预训练—开发集微调—分组交叉验证—独立测试流程合同，以及直接优化/全连接/纯集合/物理解码四模型统一评价接口。

### 2026-07-29 · 第 15 轮

- 本轮任务：完成阶段六训练协议、完整工况分组交叉验证、独立测试封存、全连接基线和四模型统一评价框架。
- 选择原因：这是正式网络对照的最后一个可提前软件前置，也是防止独立测试泄漏的关键合同；优化器、日志、分组和评价接口可用合成样本实际贯通而不形成模型优势结论。
- 修改文件：为 `physics_training.py` 增加极端左删失稳定对数 CDF；新增 `阶段六/src/training_evaluation.py`、`tests/test_training_evaluation.py`、`03_训练与四模型对照/` 下合成训练日志、三套合成软件权重、四模型指标、协议快照和验证报告；更新阶段六 README 与本清单。
- 实际命令：`uv run --python 3.12 python -m unittest discover -s 阶段六/tests -v`；`uv run --python 3.12 python 阶段六/src/training_evaluation.py`；读取各模型末 epoch、统一指标和协议 JSON；最终并行运行阶段一至六全部测试、六阶段 `compileall` 和 `git diff --check`。完整日志：`阶段一/outputs/logs/round15_stage1_unittest.log` 至 `round15_stage6_unittest.log`、`round15_compileall.log`、`round15_diff_check.log`。
- 测试或检查结果：阶段一 44/44、阶段二 21/21、阶段三 10/10、阶段四 14/14、阶段五 20/20、阶段六 23/23，共 132/132 测试通过；`compileall` 与 `git diff --check` 通过。Adam 在解析二次目标上收敛；训练只接受合成预训练/开发子集；同一 case_id 不跨折；独立测试未显式最终释放时被拒绝；24 个合成训练工况完成三模型各 4 epoch 实际更新，4 个合成软件工况完成四模型同口径评价；协议记录 `independent_test_accessed=false`。
- 任务状态：A/B 类训练与四模型评价框架完成；三套权重均标记 `SYNTHETIC_PRETRAINED_SOFTWARE_ONLY`，指标标记 `SYNTHETIC_PIPELINE_ONLY_NO_MODEL_ADVANTAGE_CLAIM`，没有按合成软件数值宣布任何网络排序或优势，《实施.md》的阶段六决策门未勾选。
- 未解决问题：正式预训练公式、扰动分布和开发集微调等待阶段三/四；超参数冻结后才可一次性释放 12 组独立测试；外部试验端到端评价和网络价值判定仍等待真实结果。
- 下一轮候选：阶段七统一鲁棒性扰动与留区间分组工具，或阶段四正向五模型统一评价与全场积分工具；优先阶段七鲁棒性工具，因为可直接复用已完成四模型评价接口。

### 2026-07-29 · 第 16 轮

- 本轮任务：完成阶段七五类观测扰动、四类留区间分组和直接反演—物理解码统一鲁棒性矩阵。
- 选择原因：阶段六统一评价接口已经完成，鲁棒性工具可立即复用；扰动确定性、完整工况隔离和矩阵合同不依赖真实 FDS 数值，但正式幅值与结论仍可保持阻塞。
- 修改文件：新增 `阶段七/src/robustness.py`、`tests/test_robustness.py`、阶段七 README、`01_鲁棒性工具/` 下合成矩阵、留区间分组和软件验证报告；更新本清单。
- 实际命令：首次 `uv run --python 3.12 python -m unittest discover -s 阶段七/tests -v` 定位导出决策状态错误；修正后重跑同命令并执行 `uv run --python 3.12 python 阶段七/src/robustness.py`；最终并行运行阶段一至七全部测试、七阶段 `compileall` 和 `git diff --check`。完整日志：`阶段一/outputs/logs/round16_stage1_unittest.log` 至 `round16_stage7_unittest.log`、`round16_compileall.log`、`round16_diff_check.log`。
- 测试或检查结果：首次 6/7，通过数值与分组测试但矩阵错误继承阶段六通用“无模型优势”状态；改为专用“无真实鲁棒性结论”状态后 7/7 通过。最终阶段一 44/44、阶段二 21/21、阶段三 10/10、阶段四 14/14、阶段五 20/20、阶段六 23/23、阶段七 7/7，共 139/139 测试通过；`compileall` 与 `git diff --check` 通过。生成 2 模型×6 扰动的 12 行合成矩阵及 Q/U/Df/x_f 四套完整工况留区间分组。
- 任务状态：A/B 类鲁棒性工具完成；不修改源样本、固定种子复现、最低有效数和 case_id 不跨侧均有测试；所有输出明确为合成工具证据，没有形成真实模型鲁棒性或排名。
- 未解决问题：真实扰动幅值和留区间边界依赖阶段三开发数据；正式网络/直接反演模型未固定；独立测试、外部试验和区间泛化结论仍等待真实数据链。
- 下一轮候选：阶段七外部试验观测转换与端到端评价表，复用阶段一已固定的 3 组热电偶观测和测点映射，但不得把热电偶温度冒充主体气体温度。

### 2026-07-29 · 第 17 轮

- 本轮任务：完成三组公开试验热电偶观测到模型输入的可追溯转换和端到端评价表。
- 选择原因：阶段一已具备真实观测、测点映射与已知工况参数；转换和误差表是阶段七外部验证前置，且可以在正式反演模型缺失时保持预测列空白，不越过科学决策门。
- 修改文件：新增 `阶段七/src/external_observations.py`、`tests/test_external_observations.py`、`02_外部端到端反演/` 下三工况输入 JSONL、等待评价 CSV 和转换说明；更新阶段七 README 与本清单。
- 实际命令：完整读取阶段一外部工况表、测点映射和三组观测文件表头/样例；`uv run --python 3.12 python -m unittest discover -s 阶段七/tests -v`；`uv run --python 3.12 python 阶段七/src/external_observations.py`；读取端到端评价 CSV；最终并行运行阶段一至七全部测试、七阶段 `compileall` 和 `git diff --check`。完整日志：`阶段一/outputs/logs/round17_stage1_unittest.log` 至 `round17_stage7_unittest.log`、`round17_compileall.log`、`round17_diff_check.log`。
- 测试或检查结果：阶段一 44/44、阶段二 21/21、阶段三 10/10、阶段四 14/14、阶段五 20/20、阶段六 23/23、阶段七 12/12，共 144/144 测试通过；`compileall` 与 `git diff --check` 通过。转换 Arup 9 点、CSTB 6 点、IFAB-07 11 点；每点标准差、不确定性口径、原始文件和映射均可追溯；Q、x_f、U、Df、H、W 和基线温度齐全；评价预测/误差列全部为空。
- 任务状态：A/B 类外部端到端准备完成；观测类型始终为 `thermocouple`，模型目标另列 `ceiling_gas_temperature`，状态明确要求响应模型或显式差异处理；没有执行外部反演或填写精度。
- 未解决问题：正式闭合、反演/网络设置、FDS 外部验证和热电偶响应差异处理未固定；三工况评价状态均为 `WAITING_FORMAL_MODEL_FDS_VALIDATION_AND_THERMOCOUPLE_TREATMENT`。
- 下一轮候选：阶段七正向公式—直接反演—网络三层不确定性汇总、温度重建残差空间图和基于开发分布的异常阈值接口。

### 2026-07-29 · 第 18 轮

- 本轮任务：完成三层不确定性来源汇总、温度空间残差和开发分布异常阈值接口。
- 选择原因：这是最终可信度评价和异常复核的工程前置；来源纪律、残差计算、开发集阈值和非诊断性提示可在正式数据前用合成检查验证。
- 修改文件：新增 `阶段七/src/uncertainty_anomaly.py`、`tests/test_uncertainty_anomaly.py`、`03_不确定性与异常提示/` 下合成汇总、异常检查、空间 SVG 和说明；更新阶段七 README 与本清单。
- 实际命令：`uv run --python 3.12 python -m unittest discover -s 阶段七/tests -v`；`uv run --python 3.12 python 阶段七/src/uncertainty_anomaly.py`；最终并行运行阶段一至七全部测试、七阶段 `compileall` 和 `git diff --check`。完整日志：`阶段一/outputs/logs/round18_stage1_unittest.log` 至 `round18_stage7_unittest.log`、`round18_compileall.log`、`round18_diff_check.log`。
- 测试或检查结果：阶段一 44/44、阶段二 21/21、阶段三 10/10、阶段四 14/14、阶段五 20/20、阶段六 23/23、阶段七 17/17，共 149/149 测试通过；`compileall` 与 `git diff --check` 通过。三层来源逐项保留，RSS 只标为独立假设下条件参考；残差按坐标排序并输出 RMSE/最大值/偏差；阈值只接受至少 10 个唯一开发 case_id；合成高残差样本被标为模型失配复核。
- 任务状态：A/B 类不确定性与异常提示框架完成；没有无条件合并相关不确定性，没有用独立测试定阈值，异常字段固定为“不作具体异常诊断”。
- 未解决问题：三层正式不确定性、相关性、阈值分位数和空间残差分布等待开发 FDS/外部结果；没有专项异常标签，不能诊断具体异常类型。
- 下一轮候选：阶段七 t² 瞬态候选工况、验收复核表、创新点—证据表和论文方法/数据流/结果模板；或阶段四正向统一评价与全场积分工具，后者是当前更高的最低闭环缺口。

### 2026-07-30 · 第 19 轮

- 本轮任务：完成阶段四五类正向模型统一评价、固定指标、严格 SI 截面读取和 `C_T/J_T/U_e^field` 积分工具。
- 选择原因：这是最低完整闭环中阶段四剩余的最高优先级软件缺口；评价口径、数据封存、积分维度和单位可在正式模型/切片缺失时用合成数据严格验证。
- 修改文件：新增 `阶段四/src/forward_validation.py`、`src/field_integrals.py`、`tests/test_forward_validation.py`、`03_正向验证与全场解释/` 下合成逐工况/汇总/积分 CSV 与软件验证报告；更新阶段四 README 与本清单。
- 实际命令：重新完整读取固定规则和进度，检查工作树与回传目录，读取阶段四 README/现有测试及阶段一 FDS I/O；多次 `uv run --python 3.12 python -m unittest discover -s 阶段四/tests -v`；`uv run --python 3.12 python 阶段四/src/forward_validation.py`；`uv run --python 3.12 python 阶段四/src/field_integrals.py`；最终并行运行阶段一至七全部测试、七阶段 `compileall` 和 `git diff --check`。完整日志：`阶段一/outputs/logs/round19_stage1_unittest.log` 至 `round19_stage7_unittest.log`、`round19_compileall.log`、`round19_diff_check.log`。
- 测试或检查结果：阶段一 44/44、阶段二 21/21、阶段三 10/10、阶段四 23/23、阶段五 20/20、阶段六 23/23、阶段七 17/17，共 158/158 测试通过；`compileall` 与 `git diff --check` 通过。固定五类模型注册、独立测试封存、全曲线/峰值/分支/删失/无风/远场/六类子集指标均有合同测试；严格 SI 单元表解析和常值 10 m² 截面精确恢复 `C_T=120/240 kJ/m`、`J_T=240/720 kW`、`U_e^field=2/3 m/s`。
- 任务状态：A/B 类正向评价与全场解释工具完成；传统公式系数和五类真实预测保持待文献/开发数据，不用合成排序选择模型；全场合成结果标记 `NO_FIELD_PHYSICS_CONCLUSION`，独立测试未解封。
- 未解决问题：传统经验式原始公式尚需文献核对；真实切片单元表、壁面/辐射损失、闭合参数趋势和模型排序等待正式 FDS；阶段四科学决策门未通过。
- 下一轮候选：阶段一 1.1 原始公式与 2018—2025 文献核对，形成可编程正向基准表；或阶段七论文/验收材料。优先文献核对，因为它是五类正向评价中唯一仍未填的 A 类输入。

### 2026-07-30 · 第 20 轮

- 本轮任务：完成阶段一 §1.1 文献核对及阶段四传统经验峰值基线。
- 选择原因：这是阶段四五类正向评价中最后一个未填的 A 类输入；公式、单位、温度类型和证据等级可在不依赖正式 FDS 回传时核验并测试。
- 修改文件：新增 `阶段一/01_文献调研/empirical_formula_catalog.csv`、`阶段四/src/empirical_baselines.py`；更新文献笔记、正向公式清单、外部试验候选清单、阶段一/二/四 README、阶段二临界风速说明与生成器、阶段四峰值评价、阶段一 `tunnel_config.py`、阶段二/四测试、《实施.md》和本清单。
- 实际命令：Crossref `https://api.crossref.org/works/<doi>` 核对 DOI 元数据；`uv run --python 3.12 --with pypdf python -X utf8 -` 在内存中读取开放 PDF DOI `10.3390/fire8010009` 与 `10.36884/jafm.6.01.19481` 的公式转录；分别运行阶段二和阶段四单元测试；最终运行阶段一至七全部单元测试、七阶段 `compileall`、`git diff --check`，并用 `rg`/PowerShell 复核旧粗估引用、机器表、回传目录和测试计数。完整日志：`阶段一/outputs/logs/round20_stage1_final.log` 至 `round20_stage7_final.log`、`round20_compileall.log`、`round20_diff_check.log`。
- 测试或检查结果：Kurioka 2003 与 Li 2011 最大顶棚气体温升公式实现了原式单位转换、分支和适用域；峰值评价拒绝热电偶响应温度且不伪造完整剖面；Wu–Bakar 2000 水力高度公式作为不改变主分层的备选；删除未使用且无可靠来源的 `K=0.044` 粗估。阶段一 44/44、阶段二 24/24、阶段三 10/10、阶段四 28/28、阶段五 20/20、阶段六 23/23、阶段七 17/17，共 166/166 通过；`compileall`、`git diff --check` 通过。
- 任务状态：阶段一 §1.1 的可定位文献产物、机器公式表、可执行传统峰值基线和测试均已完成并勾选。证据口径明确为“原论文 DOI 元数据 + 开放全文公式转录交叉核验”，未声称直接读取付费原文，未形成任何 FDS 科学结论。
- 未解决问题：Li 2010 临界风速论文存在 2024 澄清 DOI `10.1016/j.firesaf.2024.104100`，正文尚未归档，当前 Li 公式只作预分层；`阶段一/outputs/runs` 仍不存在，真实网格、边界、外部验证、闭合和反演结论全部继续等待外部结果。
- 下一轮候选：阶段七 7.4 的 6 个 t² 瞬态候选设计、验收复核表、创新点—证据表和论文方法/数据流/图表模板；只完成候选与模板，不提前提交 FDS 或填入正式结论。

### 2026-07-30 · 第 21 轮

- 本轮任务：完成阶段七 7.4 的可选瞬态候选、验收/创新/成果证据登记和论文方法、数据流、结果表、图表追溯模板。
- 选择原因：这是当前最后一组不依赖正式 FDS 回传的 P2 材料任务；它能预先固定验收口径和证据字段，同时不越过阶段一至六真实决策门。
- 修改文件：新增 `阶段七/04_论文与验收/` 下 6 个材料文件和 `阶段七/tests/test_paper_materials.py`；补充 `阶段七/README.md`；为瞬态候选表和图表清单补齐统一追溯字段；更新本清单。
- 实际命令：重新完整读取 `AGENTS.md`、`CLAUDE.md`、`实施.md`、本清单和各阶段 README；用 `rg --files`、`git status --short` 和正式回传目录扫描复核全项目；两次运行 `uv run --python 3.12 python -m unittest discover -s tests -v`（阶段七目录）；最终并行运行阶段一至七全部单元测试；运行七阶段 `compileall` 和 `git diff --check`。完整日志：`阶段一/outputs/logs/round21_stage1_unittest.log` 至 `round21_stage7_unittest.log`、`round21_compileall.log`、`round21_diff_check.log`。
- 测试或检查结果：首次阶段七测试 22/23，通过后发现 `论文图表清单.csv` 把计划输出路径和证据路径混为一列；新增独立空白 `evidence_path` 后阶段七 23/23 通过。最终阶段一 44/44、阶段二 24/24、阶段三 10/10、阶段四 28/28、阶段五 20/20、阶段六 23/23、阶段七 23/23，共 172/172 通过；`compileall` 与 `git diff --check` 通过。6 个候选严格覆盖 3 种增长率×2 个峰值功率，`t_to_Qmax=sqrt(Qmax[kW]/alpha)`，全部无 FDS 输入路径；11 条验收判据、4 项创新、12 项预期成果和 15 个图表条目均有合同测试。
- 任务状态：阶段七 7.4 的 A/B 类候选与模板完成并勾选。所有验收决定仍为 `NOT_EVALUATED`，证据路径保持空白；阶段七目录不存在 `.fds`，没有生成、提交或运行可选瞬态工况，也没有填入正式科研数值。
- 未解决问题：`阶段一/outputs/runs` 仍不存在；阶段一网格/边界/外部验证门、阶段二先导判伪、阶段三数据库、阶段四闭合、阶段五正式反演、阶段六网络对照及阶段七科学结论全部等待真实 FDS 结果链。瞬态候选还需在阶段六门后复核增长率文献、资源预算和准稳态适用性。
- 下一轮候选：重新扫描全部未完成项和正式回传目录；若仍无回传，核验是否只剩外部阻塞，并整理精确 CHID、输入位置和最低回传文件清单。

### 2026-07-30 · 第 22 轮

- 本轮任务：全项目未完成项与外部计算交接审计，判定是否达到“只剩外部阻塞”停止条件。
- 选择原因：第 21 轮完成最后一组 A/B 类待办后，必须从实际文件、测试日志、候选状态和正式回传目录复核，不能仅据清单文字宣布无可做任务。
- 修改文件：仅更新本清单的本轮审计记录；未修改代码、配置、生成器、`.fds` 或科研数据。
- 实际命令：用 `rg` 扫描《实施.md》和本清单全部未勾选项及源码占位模式；检查 `阶段一/outputs/runs`、`result_check.csv` 和第 21 轮七阶段日志计数；读取短试算、网格、长度、外部验证、先导、数据库和瞬态 CSV；逐个检查当前可提交 `.fds` 是否存在且 `HEAD CHID` 与文件名一致；运行 `git diff --check`。审计未运行 FDS。
- 测试或检查结果：本清单剩余未勾选项全部属于外部批次、真实科学决策门或“禁止提前结论”，不存在未完成的 A/B 类条目；第 21 轮日志确认 172/172 测试通过。`阶段一/outputs/runs` 和正式 `result_check.csv` 均不存在。当前可立即交给外部平台的 13 个 CHID 输入全部存在：`smoke_22mesh`；`gsA_c, gsA_m, gsA_f, gsB_c, gsB_m, gsB_f, gsC_c, gsC_m, gsC_f`；`Arup_Tunnel, CSTB_Tunnel_Test_2, IFAB-07`。
- 任务状态：达到停止条件 B“只剩外部阻塞”，但全项目未完成。短试算输入位于 `阶段一/outputs/inputs/smoke_22mesh.fds`；9 个网格输入位于 `阶段一/fds_cases/<chid>.fds`；3 个外部验证提交包位于 `阶段一/outputs/external_inputs/<chid>/`。每个工况至少回传实际 `.fds`、`.out`、`_devc.csv`、`_hrr.csv`，建议回传 `.end`；网格和外部验证还需 `.smv`、切片/三维场和 `.bf`。
- 未解决问题：必须先取得 `smoke_22mesh` 的 FDS 6.10.1 合法性和三种并行配置性能，再完成 9 个网格与 3 个外部验证。生产网格确定后才从 `length_boundary_cases.csv` 生成并运行必做 `lenA_150, lenC_150`；`lenA_200, lenC_200` 仅在 150 m 不能判定时运行。阶段一通过后，阶段二 12 个 CHID 仍保持 `WAITING_STAGE1_GATE`；阶段三 68 行保持 `WAITING_STAGE2_GATE`；阶段七 6 个瞬态候选保持 `CANDIDATE_ONLY_WAITING_STAGE6_GATE`，均不得提前生成或提交。
- 下一轮候选：发现回传目录后先运行 `阶段一/src/check_fds_results.py`；通过轻量检查后依次进行并行基准/网格/外部验证，冻结生产网格并运行长度对照，再推进 12 组先导判伪、68 组数据库、闭合、直接反演、网络对照和阶段七正式评价。

### 2026-07-30 · 第 23 轮

- 本轮任务：重新开始后的正式回传与未阻塞任务复核。
- 选择原因：项目规则要求新执行上下文不得继承未经磁盘验证的结论；需确认第 22 轮之后是否出现新结果或新的可执行任务。
- 修改文件：仅追加本轮进度记录；未修改代码、配置、生成器、输入文件或数据。
- 实际命令：重新完整读取 `AGENTS.md`、`CLAUDE.md`、`实施.md`、本清单和全部阶段 README；检查 `阶段一/outputs/runs`、`outputs/analysis/quality/result_check.csv`、13 个当前输入及其修改时间、全部未勾选提前任务、第 21 轮七阶段测试日志和工作树；检查生成器默认输出、`run_batch.sh`、`run_case.sh` 与 README 路径示例；运行 `git diff --check`。未运行 FDS。
- 测试或检查结果：截至 `2026-07-30 01:30 +08:00`，正式 `outputs/runs` 与 `result_check.csv` 仍不存在；13 个已准备输入均存在且没有新修改。剩余提前清单仍只有外部批次、真实决策门和禁止提前结论。README 的 `outputs/inputs` 示例由生成器默认目录及 `run_batch.sh` 先生成逻辑支撑，不是失效路径。磁盘日志仍证明第 21 轮 172/172 测试通过；本轮无代码变更，无需用重复测试替代缺失的 FDS 证据。
- 任务状态：第二次连续复核得到相同外部阻塞；项目仍未完成，当前没有安全且未完成的 A/B 类任务可转向。
- 未解决问题：首要缺口仍是 `smoke_22mesh` 外部 FDS 6.10.1 短试算与并行基准回传；其后网格、外部验证、长度边界和所有阶段二至七正式研究结论均由同一真实结果链阻塞。
- 下一轮候选：仅在发现 `阶段一/outputs/runs/<chid>/` 新目录后运行轻量检查并继续分析；若外部状态仍未变化，则再次记录同一阻塞证据，不生成受门控的先导、数据库或瞬态输入。

### 2026-07-30 · 第 24 轮

- 本轮任务：第三次连续外部阻塞审计与目标状态收束。
- 选择原因：持续目标规则要求同一阻塞至少连续出现三轮并经磁盘复核后，才可正式标记阻塞；不得因一次缺失或历史总结直接停止。
- 修改文件：仅追加本轮审计记录；未修改代码、配置、生成器、FDS 输入或科研数据。
- 实际命令：重新完整读取 `AGENTS.md`、`CLAUDE.md`；完整加载 `实施.md`、本清单和各阶段 README 并核对长度、哈希与修改时间；再次检查 `阶段一/outputs/runs`、正式质量表、13 个输入、先导/数据库/瞬态状态、第 21 轮七阶段测试日志和 `git diff --check`。未运行 FDS。
- 测试或检查结果：截至 `2026-07-30 01:32 +08:00`，`outputs/runs` 与 `outputs/analysis/quality/result_check.csv` 仍不存在；13/13 当前外部输入存在；12 个先导均为 `WAITING_STAGE1_GATE`，68 个数据库候选均为 `WAITING_STAGE2_GATE`，6 个瞬态候选均为 `CANDIDATE_ONLY_WAITING_STAGE6_GATE`。现有日志仍为 172/172 测试通过，`git diff --check` 通过。
- 任务状态：与第 22、23 轮相同的外部阻塞已连续三轮得到证实；当前所有未完成任务均需要外部 FDS 结果或其后按真实结果通过的决策门，项目未完成且无法在本机继续产生有效科学证据。
- 未解决问题：首个必要外部状态变化是回传 `smoke_22mesh` 的实际 `.fds`、`.out`、`_devc.csv`、`_hrr.csv`（建议含 `.end`、`.smv`、切片、`.bf`、`_cpu.csv`）并提供三种并行配置性能记录；之后才能合法启动正式网格和后续研究链。
- 下一轮候选：用户或外部平台回传结果后恢复目标；首先执行 `阶段一/src/check_fds_results.py --chids smoke_22mesh`，检查第一条 ERROR、全部 WARNING/rejected、非零 HRR、入口方向、关键列、单位和时间范围，再决定是否提交正式批次。

### 2026-07-31 · 第 25 轮

- 本轮任务：按用户确认的日志版本重新分析 9 组网格数据，放宽过严的准稳态判据，并完成真实场积分参与的网格敏感性复核。
- 选择原因：`阶段一/03_网格敏感性/runs` 已出现 9 组 300 s DEVC/HRR CSV，`derived/cross_section_integrals.csv` 已含 9 工况×11 截面×61 时刻的真实切片派生量，原先“无正式结果”的状态已变化；必须以磁盘数据重跑，而不能继承旧阻塞结论。
- 修改文件：更新 `quasi_steady_detect.py`、`time_average_bootstrap.py`、`analyze_grid_convergence.py`、`fds_io.py` 与阶段一分析测试；更新准稳态规范和网格敏感性报告；新增 `03_网格敏感性/analysis/` 下准稳态明细、Bootstrap 时间平均、场积分平均剖面和网格汇总。
- 实际命令：运行 `check_fds_results.py --rundir 03_网格敏感性/runs --expected-version any`；按 15 s 平滑、90 s 趋势窗和 30 s 平均窗重跑 9 工况准稳态；运行分块 Bootstrap；运行带 `cross_section_integrals.csv` 的网格收敛分析；运行阶段一 `py_compile`、全部单元测试和 `git diff --check`。本机未运行正式 FDS。
- 测试或检查结果：准稳态由原严格口径的 6/9 提高为 9/9 PASS；放宽仅涉及 LES 瞬时 HRR 波动（15%）和两类平滑温度长期趋势（0.15 °C/s），回流长度、5% 焓流漂移、真实缺测和时间空档判据未取消。网格中→细结果为：A 的 ΔT_p/κ_u/κ_d/C_T/J_T 差异 4.10/9.88/3.46/7.34/8.99%；B 为 6.37/21.24/43.41/11.15/14.97%；C 的 ΔT_p/x_p/H/κ_u/C_T/J_T 为 36.95/100/11.00/8.10/9.84%，且细网格 κ_d 因峰值位于 75 m、下游仅 2 点而不可拟合。阶段一最终 47/47 测试通过，`py_compile` 与 `git diff --check` 通过。
- 任务状态：9 个网格工况的准稳态窗口和时间平均已真实产出；但网格决策门未通过，0.25 m 不能确定为生产网格。场积分清单记录版本为 FDS 6.9.1 和输入/SMV/切片哈希；当前回传目录仍缺 `.out`、`.end`，轻量质量检查为 0 PASS/9 FAIL，不能在仓库内复核 ERROR、WARNING/rejected。
- 未解决问题：补齐 `gsC_f` 下游中心线温度（优先从原始温度切片提取 x=90/95 m，无法提取时补跑）；回传 9 组 `.out`/`.end`；核查相近统计窗口后设计 0.125 m 全局或局部嵌套加密对照。已有多项中→细差异显著超过 5%，补齐单个 κ_d 不会自动使当前网格收敛。
- 下一轮候选：先恢复日志和原始温度切片并补齐 `gsC_f` κ_d；随后根据资源预算生成新的加密对照，外部运行并回传，再重跑同一分析链。阶段一网格门通过前不得冻结生产网格或启动后续正式 FDS 批次。

### 2026-07-31 · 第 26 轮

- 本轮任务：落实用户最终确认的资源范围——核心模型固定 L=100 m、dx=0.25 m，不再加密网格，也不运行 150/200 m 长度对照。
- 选择原因：用户明确接受计算资源与科学精度之间的取舍；项目必须把“固定研究范围”和“决策门已通过”分开记录，避免后续把未收敛或未验证误写成完成。
- 修改文件：更新《实施.md》、阶段一 README、网格报告、长度报告、资源预算、阶段一完成报告和 `tunnel_config.py` 注释；保留已生成的 `lenA_150/lenC_150.fds` 作追溯，不删除用户文件，但从当前运行队列移除。
- 实际命令：核对 9 组网格汇总、长度输入和 3 个外部验证提交目录；运行 `uv run --python 3.12 python -m unittest discover -s tests -v`、`uv run --python 3.12 python -m compileall -q src tests` 与 `git diff --check`。本机未运行正式 FDS。
- 测试或检查结果：阶段一 48/48 测试通过，源码和测试编译通过，`git diff --check` 通过。资源约束后核心预算由 81–84 调整为 79–82；0.25 m 和 100 m 均记录为条件性范围，不标记网格收敛或洞口独立。
- 任务状态：长度批次状态为 `WAIVED_BY_USER_FIXED_100M_NOT_VALIDATED`。下一项真实科学门是 3 个公开实体试验的外部 FDS 复现；提交包 `Arup_Tunnel`、`CSTB_Tunnel_Test_2`、`IFAB-07` 及真实热电偶观测均已存在。
- 未解决问题：网格回传仍缺 `.out/.end`；外部验证尚无 FDS 输出；因此阶段一平台精度、阶段二先导和阶段三数据库的正式科学结论仍未完成。外部验证使用各试验官方几何与网格，不应强行改成核心模型的 100 m/0.25 m。
- 下一轮候选：外部平台运行 3 个验证提交包并回传实际 `.fds/.out/_devc.csv/_hrr.csv/.end`，同时回传 `.smv`、相关切片和 `.bf`；回传后先运行 `check_fds_results.py`，再计算外部温度剖面指标。验证通过后按 100 m、0.25 m 条件性范围启动 12 组先导判伪。

### 2026-07-31 · 第 27 轮

- 本轮任务：按用户要求把后续 FDS 与外部实体试验复现并行，生成完整的 12 组先导输入，同时保留 68 组数据库的结构判伪门。
- 选择原因：外部验证与先导判伪使用不同证据链，可以并行节省等待时间；68 组数据库依赖先导结构是否成立，提前生成/运行会产生整批设计失效风险，因此没有越过该门。
- 修改文件：阶段二先导生成器改为引用阶段一 `WORKING_GRID_DX/L/W/H` 单一事实源，状态更新为 `CONDITIONAL_PARALLEL_PENDING_EXTERNAL_VALIDATION`；更新阶段二测试、README、先导设计报告、《实施.md》、阶段一外部回传说明和本清单；重新生成 `pilot_cases_candidate.csv`。
- 实际命令：运行阶段二设计生成器；用阶段一 FDS 生成器将 12 个输入写入 `阶段一/outputs/pilot_inputs/`；静态核对 12 个先导和 3 个外部官方输入；运行阶段一、阶段二完整测试、两阶段 `compileall` 和 `git diff --check`。本机未运行正式 FDS。
- 测试或检查结果：12/12 先导均为 L=100 m、dx=0.25 m、22 MESH、T_END=300 s，CHID、DUMP、REAC 和 TAIL 检查通过；3 个外部输入及 IFAB 伴随文件检查通过。阶段一 48/48、阶段二 25/25 测试通过，编译与差异检查通过。
- 检查修正记录：自定义 PowerShell 静态检查最初三次分别因 `$chid:` 插值语法、把 IFAB 的 `CATF+MULT` 和 Arup/CSTB 的 `MULT` 逻辑网格误当成直接 `&MESH` 数量而失败；输入本身未改。最终按官方快照的基础 MESH×MULT 展开规则核对，Arup/CSTB/IFAB 逻辑 MESH 为 5/12/12 并通过。
- 任务状态：3 个外部验证与 12 个先导可同时提交。`gsA_m/gsB_m/gsC_m` 是可复用项，但旧回传记录为 FDS 6.9.1 且缺 `.out/.end`；应优先恢复日志，无法恢复时在本批重跑。68 组数据库继续保持 `WAITING_STAGE2_GATE`。
- 未解决问题：15 个工况尚无本轮外部 FDS 结果；先导 U_c 仍为 Li 关联的预分层，不能当作真实临界风速；外部验证和先导判伪均未形成科学结论。
- 下一轮候选：外部平台并行运行 3 个官方验证与 12 个先导，逐 CHID 回传实际 `.fds/.out/_devc.csv/_hrr.csv/.end`，建议含 `.smv`、切片和 `.bf`。回传后先统一运行 `check_fds_results.py`，再分别执行外部温度验证和阶段二结构判伪；两者通过后才生成 68 组数据库。

### 2026-08-01 · 第 28 轮

- 本轮任务：识别未登记的 3 个外部验证与 12 个先导回传，修复结果质量检查器，并完成严格区分正式/预分析的外部温度验证和阶段二结构判伪集成。
- 选择原因：磁盘中已出现新的真实 FDS 输出，原“等待回传”状态失效；按项目规则必须先质量审计再分析，并禁止把旧版本结果升级为正式结论。
- 修改文件：修复 `阶段一/src/check_fds_results.py` 并扩展回归测试；新增 `analyze_external_validation.py` 及测试；新增 `阶段二/src/analyze_pilot_falsification.py` 及测试；扩展 FDS 自适应输出间隔和拟合区间阈值接口；更新阶段一/二/四 README、两份阶段二方法报告、外部验证报告、《实施.md》和本清单。
- 实际命令：对外部目录和 `outputs/pilot_input` 分别运行质量检查；对 3 个外部工况运行 `analyze_external_validation.py --allow-review`；对 12 个先导运行准稳态识别、500 次时间平均 Bootstrap 和 `analyze_pilot_falsification.py --allow-review --sensor-threshold-C 5 --n-boot 500`；运行新增阶段一/二定向测试。正式 FDS 未在本机运行。
- 测试或检查结果：外部 0 FAIL/3 REVIEW、先导 0 FAIL/12 REVIEW；所有 CSV 覆盖 `T_END`，HRR 非零，`.out` 无真正 ERROR/WARNING/rejected 且写有正常结束，但均为 FDS 6.9.1 且缺 `.end`。外部预分析 NRMSE 为 Arup 0.2403、CSTB 0.2659、IFAB 0.1928。先导 12/12 找到准稳态窗，12×23 测点完成 500 次 Bootstrap；结构预分析选择 0.3H。补足有限源受限参数优化后，其留一工况中位 NRMSE 从点源 0.0988 降至约 0.0513，故预分析选择有限源类，但宽度系数跨折不稳定；4/12 通过全部候选门，11/12 峰值区间宽于 0.5H，`pilot_12_q100_r160_d80` 下游点不足。
- 任务状态：外部验证和阶段二正式决策门均未通过；所有新增数值输出标为 `PRELIMINARY_REVIEW_NOT_FORMAL`，68 组数据库继续保持 `WAITING_STAGE2_GATE`。
- 未解决问题：需要三组外部验证的 FDS 6.10.1 正式回传；先导至少需要目标版本代表工况作版本对照，且当前 6.9.1 预分析已经显示结构性失败与强风下游测量域不足，不能直接用同一设计扩大数据库。正式删失阈值还缺随机种子重复和工程传感器阈值证据。
- 下一轮候选：运行完整七阶段回归、编译和差异检查，形成精确外部 CHID 与回传清单。
- 后续实现：保留原 12 组设计不变，另新增并静态验证 `pilot_diag_q100_r160_d70_x41`、`pilot_diag_q100_r160_d80_x41` 两个 FDS 6.10.1 测量域诊断输入。两者同 Q、同 U、仅 Df 不同，火源 41 m 已通过多 MESH 燃烧器不被内部边界切穿的生成合同；它们不计入原 12 组，也不改变数据库门。有限源拟合新增源中心/双侧衰减率/源强受限坐标优化和合成恢复测试，避免不公平地固定点源参数。
- 最终验证：阶段一至七分别 54/32/10/32/20/23/23，共 194/194 测试通过；七阶段 `compileall` 与 `git diff --check` 通过。现有回传 README 已列出 6 个立即外部 CHID、完整输入/工作/回传路径和结果返回后的继续顺序。

### 2026-08-01 · 第 29 轮

- 本轮任务：落实用户“外部库和已回传结果统一采用现有版本”的决定，把 FDS 6.9.1 设为正式证据基线，并重新执行 15 组质量检查和两条真实分析链。
- 规则调整：不再仅因结果不是 6.10.1 降级；缺 `.end` 时，只有 `.out` 明确 `STOP: FDS completed successfully` 且 DEVC/HRR 均达到 `T_END` 才接受替代完成证据。6.10.1 本机实跑结论保留为该版本兼容性记录，不伪装成 6.9.1 结论。
- 修改与产出：更新 `CLAUDE.md`、质量检查器、版本/完成证据测试、阶段一/二当前状态文档和外部回传说明；外部验证重新输出 `FORMAL_QUALITY_PASS`；先导分析新增 `FORMAL_QUALITY_PASS_THRESHOLD_CANDIDATE`，使运行质量和阈值证据两个门不再混淆。
- 实际检查结果：3 个外部验证和 12 个先导均为 `PASS`，0 ERROR、0 WARNING、0 rejected，火源非零且计算完整。外部指标仍为 Arup/CSTB/IFAB NRMSE 0.2403/0.2659/0.1928，偏差未因版本放行改变。先导候选阈值分析仍为 4/12 通过、11/12 峰值区间宽于 0.5H、`pilot_12` 下游不可辨识，正式门为 `NOT_EVALUATED_THRESHOLD_EVIDENCE`。
- 自动化验证：阶段一至七分别 55/33/10/32/20/23/23，共 196/196 测试通过；七阶段 `compileall` 与 `git diff --check` 通过。首次全量回归发现阶段三夹具仍写 FDS 6.10.1，按新基线被正确标为 REVIEW；将测试夹具同步为 6.9.1 后全量通过。
- 当前真实状态：版本与缺 `.end` 已不再阻塞，也不需要原 15 组版本复算。尚未通过的是外部试验吻合、阶段二结构比例及删失阈值证据；68 组数据库继续保持门控。
- 下一轮候选：优先用现有时序和全场文件定位三组外部偏差，并比较有限源/过渡或双尺度衰减结构；同时形成逐工况噪声阈值和工程传感器阈值证据。只有离线结构达到约 9/12 后，才考虑两个 x=41 m 强风诊断工况。

### 2026-08-01 · 第 30 轮

- 本轮任务：用现有真实试验/FDS 时序诊断三组外部验证偏差，排查回传、DEVC 映射和比较时间窗。
- 选择原因：原 15 组已经按 6.9.1 基线通过运行质量检查，无需重跑；当前最高优先级是确定外部吻合较弱是否来自可修正的数据处理错误，而不是盲目修改官方输入。
- 修改与产出：新增 `阶段一/src/diagnose_external_validation.py` 及 2 个回归测试；输出逐点残差、五窗口敏感性和工况汇总到 `阶段一/outputs/analysis/validation/diagnostics/`；更新外部验证报告、阶段一 README 和《实施.md》。
- 实际命令：运行新增诊断测试；运行真实诊断器读取 3 组官方执行输入、真实原始试验时序、归档观测和 6.9.1 DEVC 时序。本机未运行 FDS，也未修改官方输入。
- 测试或检查结果：新增 2/2 测试通过。Arup/CSTB/IFAB 的 9/6/11 个测点坐标及 `THERMOCOUPLE` 量名全部匹配；窗口平移后的最佳 NRMSE 为 0.2296/0.2531/0.1853，相对当前仅改善 4.45%/4.82%/3.86%，且 Bias 分别始终为负/正/负。最突出残差为 Arup `Tl05` +105.02 °C、CSTB `Tm0313` +180.96 °C（`Tm0915` 实测温升仅 0.58 °C 而 FDS 为 142.51 °C）、IFAB `T060` −270.23 °C。
- 任务状态：确认原 15 组没有回传或运行质量问题；外部偏差不是坐标、量名或当前时间窗造成，而是官方 FDS 响应与实验之间的系统差异。外部精度门仍未通过，不能通过调参官方输入或挑选时间窗制造通过。
- 下一轮候选：复跑现有有限源留一工况和五项门后，若失败仍集中在拟合区间敏感及下游截断，则不再增加无可辨识的双尺度自由度，直接运行两个 x=41 m 强风测量域诊断工况；原 15 组不重跑，68 组继续门控。

### 2026-08-01 · 第 31 轮

- 本轮任务：复核阶段二结构失败是否来自实现问题，并重新扫描阶段三至七还有无可在本机提前完成的任务。
- 选择原因：外部偏差已排除映射和时间窗后，最高优先级是决定能否仅靠离线结构改造跨过阶段二门；若下游观测本身不足，则继续增加模型自由度会制造不可辨识结果。
- 实际命令：用现有 12 组真实 6.9.1 温度剖面重跑 500 次 Bootstrap、有限源留一工况选择和五项结构门；运行阶段二 33 项测试；随后运行阶段一至七全部单元测试和 `compileall`，扫描 TODO/FIXME/NotImplemented/占位实现、阶段三至七 README、两个诊断回传目录和工作树差异。本机未运行 FDS。
- 测试或检查结果：阶段二仍为 4/12；单项通过数为远场对数线性 8/12、区间稳定 6/12、近场残差 10/12、临界结构 9/12、删失稳定 11/12、源模型精度 11/12。阶段一至七分别 57/33/10/32/20/23/23，共 198/198 测试通过；全部源码/测试编译通过，`git diff --check` 通过。扫描未发现空实现，唯一“占位”文本是论文模板要求等待字段保持空白的规则。
- 任务状态：不再为原 15 组安排复算。`pilot_12_q100_r160_d80` 峰值下游近场外只有 1 点，且多个强风剖面到 85 m 仍为高温平台；在此基础上添加双尺度项不可辨识。两个 x=41 m 诊断 `.fds` 已生成并通过生成合同，但对应 `outputs/runs/<chid>/` 均不存在。
- 外部阻塞：运行 `pilot_diag_q100_r160_d70_x41` 与 `pilot_diag_q100_r160_d80_x41`；另需至少两个独立重复种子结果和可追溯的目标工程传感器精度，才能正式确定 `DeltaT_noise/DeltaT_sensor`。外部试验精度门、网格收敛和洞口独立性也仍未通过，不能宣称阶段一或全项目完成。
- 结果回传后的顺序：先用 `check_fds_results.py` 检查两个诊断工况，再执行准稳态/Bootstrap/结构判伪；若移位后显著改善，修订强风工况的可辨识测量域；若仍失败，才调整解析结构。阶段二门通过前不生成 68 组 FDS，阶段四至七正式结论继续等待真实数据库。

### 2026-08-01 · 第 32 轮

- 本轮任务：按现有 FDS 6.9.1 规则重新审计全部回传、重放真实分析链，并确认新增诊断/随机种子工况的外部阻塞状态。
- 选择原因：用户确认规则和外部库均沿用现有版本；需要把“原 15 组是否有问题”与“后续科学门为何仍未通过”彻底分开，并以当前磁盘结果而非上一轮摘要判断下一步。
- 实际命令：完整读取 `AGENTS.md`、`CLAUDE.md`、`实施.md`、本清单和全部阶段 README；扫描 292 个项目文件、工作树、正式/历史回传目录和空实现模式；重新运行外部 3 组与先导 12 组 `check_fds_results.py`；重跑外部正式指标、时间窗/映射诊断和先导 500 次 Bootstrap 结构判伪；运行阶段一至七全部单元测试、七阶段 `compileall` 和 `git diff --check`。本机未运行正式 FDS。
- 测试或检查结果：原 15 组仍为 15/15 `PASS`，均为 FDS 6.9.1、0 ERROR、0 WARNING、0 rejected、火源非零且正常达到 `T_END`。外部指标仍为 Arup/CSTB/IFAB NRMSE 0.2403/0.2659/0.1928，时间窗最佳改善仅 4.45%/4.82%/3.86%；先导仍为 4/12 通过，正式门仍是 `NOT_EVALUATED_THRESHOLD_EVIDENCE`。阶段一至七分别 58/35/10/32/20/23/23，共 201/201 测试通过；全部源码/测试编译和 `git diff --check` 通过。
- 任务状态：原 15 组不需要重跑，其运行质量没有问题；未通过的是外部试验数值吻合和阶段二结构/阈值科学门。两个 x=41 m 诊断与两个非零 `RND_SEED` 重复输入均存在并通过生成合同，但四个对应 `outputs/runs/<chid>/` 目录仍不存在。阶段三至七没有新的未完成 A/B 类任务可在本机安全推进。
- 外部阻塞：运行 `pilot_diag_q100_r160_d70_x41`、`pilot_diag_q100_r160_d80_x41`、`pilot_06_q40_r100_d45_seed104729`、`pilot_11_q100_r160_d70_seed204729`；另需填写并批准 `阶段二/03_删失与峰值/sensor_threshold_evidence_template.csv` 中可追溯的工程传感器阈值依据。
- 下一轮候选：四组结果回传后先运行质量检查；随后对诊断工况执行准稳态、时间平均、Bootstrap 和结构判伪，对种子重复计算逐工况噪声分量；与已批准的传感器证据合并确定 `DeltaT_th`。只有阶段二门真实通过后，才冻结并生成 68 组数据库 FDS。

### 2026-08-01 · 第 33 轮

- 本轮任务：明确四个新增输入的 Git 传输方式，并落实用户对 5 °C 工程传感器阈值的有条件批准。
- Git 检查：四个 `.fds` 均位于 `阶段一/outputs/pilot_diagnostic_inputs/` 或 `阶段一/outputs/pilot_seed_inputs/`，每个约 8.3 kB；原先被 `阶段一/outputs/**` 忽略，故不会随 Git 上传。本轮仅为这两个小型可复现输入目录增加白名单，`git add -n` 已确认四个文件均可纳入版本控制；尚未替用户执行 commit 或 push。
- 阈值依据：采用 IEC 60584-1:2013 K 型热电偶 2 级允差。删失关注的弱温升区低于 333 °C，单次读数允差为 ±2.5 °C；基线与观测温度作差按保守最坏情况取 `2×2.5=5 °C`。用户于本轮批准该工程要求，正式证据表已写为 `APPROVED`；实际温度测量链须达到 K 型 2 级或更好，否则必须提高阈值。
- 证据边界：5 °C 只确定 `DeltaT_sensor`，不替代 `DeltaT_noise`。最终检测阈值仍为 `max(5 °C, DeltaT_noise)`，后者继续等待两个非零随机种子重复结果。
- 验证结果：阶段一 58/58、阶段二 35/35 定向回归通过；四个输入生成合同再次通过；`git diff --check` 通过。
