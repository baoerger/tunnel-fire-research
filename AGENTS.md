# 项目级代理指令

## 技术规范路由

- 根目录的 `CLAUDE.md` 是本项目对所有编码代理生效的 FDS 技术规范；文件名不表示它只适用于 Claude。
- 凡任务涉及 FDS、`.fds` 文件、隧道火灾模型、`阶段一` 中的生成/运行/读取/后处理脚本、FDS 输出 CSV、仿真验证或相关技术结论，必须在采取任务操作前完整读取 `CLAUDE.md`，并遵守其中与任务相关的约束。
- 若 `CLAUDE.md` 在当前任务中发生变化，或不能确定其内容仍完整保留在上下文中，应在继续相关工作前重新完整读取。
- `CLAUDE.md` 中记录的目标版本实跑结论优先于仓库内旧文档和历史写法；系统、开发者及用户当前明确指令仍具有更高优先级。
- 对完全不涉及上述 FDS 内容的任务，无需读取 `CLAUDE.md`。

## 维护原则

- 详细 FDS 规则只在 `CLAUDE.md` 中维护，避免在本文件复制后产生双份规范漂移。
- 新的 FDS 兼容性结论只有经过目标版本短试算验证后，才可写入 `CLAUDE.md`；同时更新受影响的配置、生成器、测试和说明文档。
