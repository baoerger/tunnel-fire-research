# 12 组先导候选设计报告

状态：**历史候选设计已完成实跑；协议 V1 正式结论为 4/12、FAIL。用户固定
100 m 后启用的条件域 V2 保留该失败证据，并以参数掩码和封存测试继续。**

设计表由 `pilot_design_targets.csv` 和 `src/design_pilot_cases.py` 生成。固定要求：

- 恰好 12 组，全部属于未来 50 组开发集的 `development_pilot`，不额外计入数据库总数；
- 覆盖低/中/高功率、无风/弱风/临界/强风及至少 5 个 D_f；
- 临界区至少 4 组；
- 至少 6 个候选属于同 Q、同 U、不同 D_f 的配对；
- 复用 `gsA_m/gsB_m/gsC_m` 三个中网格代表工况，避免重复计算；
- 离散燃烧器 HRRPUA 限于 500–3000 kW/m²；每组都有选择理由与替补工况。

当前 U_c 只是 Li–Lei–Ingason 关联在 `chi_r=0.30` 下的初估。可定位数值见 `critical_velocity_estimates.csv`，逐工况 Q、U、D_f、U/U_c、流态和 HRRPUA 见 `pilot_cases_candidate.csv`。

这些候选输入已经完成 FDS 6.9.1 实跑和质量审计。正式阈值分析只有 4/12
通过全部 V1 门，点源/有限源取舍、删失、峰值稳定性及 d80 下游域删失证据
见阶段二 README 和正式输出。该结果禁止沿 V1 扩大数据库；它不等于 V2
独立确认，V2 的科学结论仍须等待完整开发集和模型冻结后的封存测试。
