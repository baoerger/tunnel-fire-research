# 随机种子重复说明

阶段二正式温升删失阈值需要把单次稳态时间波动与独立 LES 重复之间的离散
分开。现有 12 组各只有一次运行，分块 Bootstrap 不能替代独立随机种子重复。

FDS 官方标签 `FDS-6.9.1`（commit `889da6ae08d08dae680f7c0d8de66a3ad1c65375`）
的 `FDS_User_Guide.tex` 明确说明在 `MISC` 行使用 `RND_SEED` 改变预定义随机
序列；同标签源码 `Source/read.f90` 将 `RND_SEED` 读入 MISC namelist，
`Source/init.f90` 将其加到各 MESH 的预定义种子。该依据只确认官方语法，
正式可用性仍须由外部 FDS 6.9.1 实跑检查，不写成已实跑兼容性结论。

`pilot_seed_repeat_cases.csv` 选择两个已有基准：近临界
`pilot_06_q40_r100_d45` 和高功率强风 `pilot_11_q100_r160_d70`。原结果作为
默认 `RND_SEED=0` 基准，新增一次非零种子重复；除 CHID 和 RND_SEED 外，
Q、U、Df、几何、网格、测点、输出和时长保持一致。

生成位置：

```text
C:\Users\xiao.cheng\Desktop\科研\阶段一\outputs\pilot_seed_inputs\
```

生成命令：

```bash
uv run --python 3.12 python 阶段一/src/generate_fds_case.py \
  --csv 阶段二/01_先导工况设计/pilot_seed_repeat_cases.csv \
  --outdir 阶段一/outputs/pilot_seed_inputs
```

## 正式结果与噪声口径

2026-08-02 在 `阶段一/outputs/extrainput/<chid>/` 找到两组完整回传。两组均为
FDS 6.9.1、300 s 正常结束、质量 `PASS`，并确认非零种子语法已在本项目正式
结果中实跑。原始目录不移动；后处理显式使用该历史回传位置。

父工况和重复工况分别使用自己通过判定的 30 s 准稳态窗口：

- `pilot_06_q40_r100_d45`：105--134 s；重复：86--115 s；
- `pilot_11_q100_r160_d70`：144--173 s；重复：155--184 s。

逐测点重复均值标准差取 `|mean_parent-mean_repeat|/sqrt(2)`，并与父/重复各自
时间块 Bootstrap 标准差取较大者。删失只关注弱温升区，因此在两次平均温升
不超过 5 °C 的测点中乘 1.96 并取最大值。两对结果分别为 0.125039927 °C 和
0.077437413 °C；项目采用较大的 0.125039927 °C 作为代表性保守包络，显式
赋给全部 12 个先导和 2 个诊断 CHID，但不声称其余 CHID 各自做过独立重复。

可重复命令和产物：

```bash
uv run --python 3.12 python 阶段二/src/derive_seed_noise_thresholds.py \
  --additional-design 阶段二/01_先导工况设计/pilot_diagnostic_cases.csv
```

逐点、工况汇总和正式赋值表位于 `阶段二/03_删失与峰值/`。最终检测阈值为
`max(5,0.125039927)=5 °C`。
