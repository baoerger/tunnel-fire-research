# 协作教程 — 两台电脑同步本项目(Git + GitHub)

> 本项目已托管在 GitHub 私有仓库 `tunnel-fire-research`。
> 本文件会随仓库一起同步 —— 推送后,另一台电脑 clone 下来就能看到这份教程。

---

## 0. 30 秒理解核心概念

| 概念 | 通俗解释 |
|---|---|
| **仓库 (repo)** | 这个项目文件夹,Git 在里面记录所有改动历史 |
| **commit (提交)** | 给当前改动"拍一张快照"并存到本地历史。每条提交都带一句说明 |
| **push (推送 / 上传)** | 把本地的提交传到 GitHub(云端) |
| **pull (拉取 / 下载)** | 把 GitHub 上别人新提交的改动拉到本地 |
| **远程 origin** | 指向 GitHub 上这个仓库的地址别名 |

一句话:**本地改 → commit → push 上传;别人改了 → pull 下载到本地。**

---

## 1. 本机(已配好)的日常工作流

这台 Windows 机器已经初始化并推送过了。以后每次工作:

```powershell
# ① 开工前先下载最新(防止和对方的改动撞车)
git pull    
        # 连同未跟踪的新文件一起临时保存
          git stash push -u -m "拉取前临时保存"

        # 拉取远端 main
          git pull --ff-only

        # 恢复本地修改
          git stash pop


# ② 编辑你的文件 ……(用 VSCode 正常改)

# ③ 暂存全部改动
git add -A

# ④ 提交(写一句这次改了啥)
git commit -m "说明:"

# ⑤ 上传
git push
```

> **黄金法则:先 `pull` 后改;改完 `commit` + `push`。** 每做一个有意义的步骤就提交一次,这样"每一步都更新"。

---

## 2. 另一台电脑:一次性配置 + 下载克隆

### 2.1 装 Git 和 GitHub CLI

**Windows:**
```powershell
winget install --id Git.Git -e --accept-source-agreements --accept-package-agreements
winget install --id GitHub.cli -e --accept-source-agreements --accept-package-agreements
```
> 装完**重新开一个** PowerShell 窗口(让环境变量生效)。

**macOS(装了 Homebrew):**
```bash
brew install git gh
```
> 没装 Homebrew:先装 Xcode 命令行工具 `xcode-select --install` 会自带 git;gh 去 https://github.com/cli/cli/releases 下 .pkg。

**Linux (Debian/Ubuntu):**
```bash
sudo apt update && sudo apt install -y git
# gh 需用官方源(apt 里的版本可能旧):
(type -p wget >/dev/null || sudo apt install -y wget) \
 && sudo mkdir -p -m 755 /etc/apt/keyrings \
 && wget -qO- https://cli.github.com/packages/githubcli-archive.key | sudo tee /etc/apt/keyrings/githubcli-archive-keyring.gpg > /dev/null \
 && sudo chmod go+r /etc/apt/keyrings/githubcli-archive-keyring.gpg \
 && echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/githubcli-archive-keyring.gpg] https://cli.github.com/packages stable main" | sudo tee /etc/apt/sources.list.d/github-cli.list > /dev/null \
 && sudo apt update && sudo apt install -y gh
```

### 2.2 登录 GitHub(和本机一样的步骤)

在终端运行:
```bash
gh auth login --hostname github.com --git-protocol https --web
```
按提示:复制一次性码 → 回车打开浏览器 → 粘贴码 → Authorize。看到 `✓ Logged in as <用户名>` 即成功。

> 私有仓库需要权限。如果对方是**另一个 GitHub 账号**,先看下面第 3 步邀请;如果是**同一个账号**(你两台机器都用自己账号),直接跳到 2.3。

### 2.3 下载克隆(第一次把项目拉到这台机器)

去 GitHub 仓库页面 → 点绿色 **Code** 按钮 → 选 **HTTPS** → 复制地址(形如 `https://github.com/你的用户名/tunnel-fire-research.git`)。

然后:
```bash
cd 你想存放的父目录
git clone https://github.com/你的用户名/tunnel-fire-research.git
cd tunnel-fire-research
```

克隆完就有一份完整项目 + 全部历史了。以后日常用第 1 节的工作流即可。

> ⚠️ 克隆后**不要**再 `git init`;这个文件夹已经是仓库了。

---

## 3. 邀请协作者(仅当对方是另一个 GitHub 账号)

私有仓库默认只有你能看。要让另一台电脑(另一个账号)能访问/推送:

1. 浏览器打开仓库页 → **Settings** → 左侧 **Collaborators** → **Add people**
2. 输入对方的 GitHub 用户名或注册邮箱 → **Add**
3. 对方邮箱收到邀请 → 点链接 → **Accept invitation**
4. 之后对方就能 `git clone` 并 `git push` 了

> 若两台机器都用**你自己的同一个 GitHub 账号**,跳过这步 —— 直接 clone 即可。

---

## 4. 每一步都更新的黄金法则

让"每个改动都同步"的关键习惯:

1. **开工前** `git pull` —— 永远先拉取最新
2. **小步提交** —— 改完一个小目标就 `commit`(别攒一大堆才提交)
3. **提交就推** —— `commit` 后立刻 `push`,对方才能看到
4. **离开前** `git push` —— 收工前确保推干净

最省事的三连(改完后):
```bash
git add -A && git commit -m "说明" && git push
```
(PowerShell 里把 `&&` 换成换行逐条运行;VSCode 图形界面见第 6 节)

---

## 5. 冲突处理

两台机器改了**同一文件**并同时 push 时,后 push 的一方会提示冲突。处理:

```bash
git pull
# 若输出 "CONFLICT (content): Merge conflict in <文件>"
```

在 **VSCode** 打开该文件,会看到:
```
<<<<<<< HEAD
你本地的内容
=======
对方(GitHub上)的内容
>>>>>>> origin/main
```
用编辑器顶部的 **Accept Current / Accept Incoming / Accept Both** 按钮选择保留哪部分(也可手动删掉这些标记后写最终内容),保存。然后:
```bash
git add -A
git commit -m "解决冲突:<文件>"
git push
```

> 只有同一文件的同一行附近都改了才会冲突;不同文件、同一文件不同区域一般自动合并,不冲突。

---

## 6. 用 VSCode 图形界面(推荐,少敲命令)

VSCode 自带 Git 支持,对研究者最友好:

- **打开源代码管理**:左侧栏源代码管理图标,或 `Ctrl+Shift+G`
- 改动的文件会列出来;点文件可看逐行 diff
- 顶部消息框写提交说明 → 点 **✓ 提交**
- 提交后,点源代码管理面板里 `...` 菜单 → **推送 / 拉取 / 同步**
  - **同步(Sync)** = 先 pull 后 push,日常最省事,一键搞定

推荐装两个扩展(扩展市场搜):
- **Git Graph**:可视化整个提交历史,点提交能看每次改了啥
- **GitHub Pull Requests and Issues**:在 VSCode 里直接管 PR/issue

---

## 7. 注意事项

| 事项 | 说明 |
|---|---|
| **`.docx` 是二进制** | Git 看不了逐字 diff、合并冲突也难处理。建议正文写在 `.md`,docx 只作最终交付稿 |
| **仿真大输出** | `*.s3d *.smv *.prt5` 等已被 `.gitignore` 排除,不会上传(见仓库根 `.gitignore`)。要共享大结果数据请用网盘或 Git LFS |
| **`.sh` 脚本换行** | `.gitattributes` 已强制 LF,Windows 改了到 Linux 集群跑也不会报 `bad interpreter` |
| **国内网络** | `git push/pull/clone` 连不上时开 VPN/代理再试;HTTPS 方式即可,无需配 SSH key |
| **不要直接传大文件** | 单文件超 50MB 别 `git add`,会让仓库变臃肿。用 Git LFS 或网盘 |

---

## 8. 速查表

| 想做的事 | 命令 |
|---|---|
| 看改了啥 | `git status` |
| 看具体改动 | `git diff` |
| 看历史 | `git log --oneline --graph` |
| 下载最新 | `git pull` |
| 暂存全部 | `git add -A` |
| 提交 | `git commit -m "说明"` |
| 上传 | `git push` |
| 一键同步(改完后) | `git add -A` → `git commit -m "..."` → `git push` |

---

## 9. 第一次的"提交-推送"示范(在这台或另一台都一样)

```bash
git pull                              # 先拉最新(第一次克隆后可省)
# …改文件…
git add -A                            # 暂存
git commit -m "测试:第一次协作提交"     # 提交
git push                              # 推送
```

看到类似 `main -> main` 或 `origin/main` 的输出就是成功了。另一台机器 `git pull` 即可看到这次改动。
