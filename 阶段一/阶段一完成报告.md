# 阶段一完成报告（状态更新）

## 1. 已完成的工程准备与本机验证

| 子任务 | 当前状态 |
|---|---|
| FDS 基准输入 | 由 `src/generate_fds_case.py` 重生成；不再维护手写副本 |
| FDS 版本合同 | FDS 6.10.1 已完成输入解析、启动、非零 HRR、DEVC/HRR CSV 最小实跑验证 |
| 网格敏感性 | 9 行工况表、生成器和分析脚本就绪；正式计算未完成 |
| 洞口边界 | 150/200 m 工况表和分析脚本就绪；正式计算未完成 |
| 外部复现 | 模板和指标脚本就绪；`TBD` 参数尚待按文献补齐 |
| 测点与输出 | 已按任意 L/W/H 参数化，基准为温度 0.90H、速度 0.95H |
| HRR/对流比例 | 已改为 `_hrr.csv HRR/Q_RADI` 口径 |
| 准稳态/时间平均 | 脚本就绪，所有设备数据按 CSV 单位归一化 |
| 自动化测试 | 生成语法、离散燃烧面积、几何、CSV 解析和单位转换契约已建立 |
| 运行入口 | 单工况、CSV 批量和 Slurm array 脚本已按路径/CHID 重构 |

本机现有 Python 与 FDS 6.10.1 可用。短试算证明已修正输入能够运行并产生有效火源输出，但不等价于正式 300 s 研究工况完成。

## 2. 尚未完成的研究结果

- 9 个网格工况的正式计算和 ΔT_p、x_p、κ_u、κ_d 收敛性判定。
- 150/200 m 对照及 100 m 洞口无关性结论。
- 外部试验工况选定、参数补齐和复现指标。
- 多工况准稳态 `χ_r=-Q_RADI/HRR` 稳定性及是否需要修正关系。
- 准稳态窗口、时间平均、Bootstrap 噪声尺度及种子重复。

这些项必须使用完整时长结果，不能由 0.5~5 s 启动验证代替。

## 3. 平台结论

- **Windows 本机**：可用于生成、单元测试、短试算、CSV 解析和轻量后处理，目标版本为 FDS 6.10.1。
- **Linux/HPC**：仍推荐用于细网格和 81~84 个正式批量工况，以满足计算时间、内存和存储需求；不是因为本机缺少软件，而是因为正式规模较大。
- 当前生成输入为单 MESH，默认单进程；只有改为多 MESH 后才应分配多个 MPI 进程。Slurm 模板的模块名、MPI 类型和资源参数须在目标集群验证。

## 4. 正式运行前顺序

1. `python -m unittest discover -s tests -v`。
2. 批量生成目标 CSV；外部模板若仍含 `TBD` 必须先补齐。
3. 用 FDS 6.10.1 做代表性短试算，检查第一条 ERROR、全部 WARNING、无 VENT rejected、`HRR_tot>0`、入口 +x。
4. 检查 `_devc.csv` 和 `_hrr.csv HRR/Q_RADI` 可由 `fds_io` 解析。
5. 再提交正式时长计算，检测准稳态后取 10~30 s 平均窗口。

## 5. 进入下一阶段前提

- [ ] 网格核心目标量收敛
- [ ] 洞口边界影响可接受
- [ ] 外部试验复现达到预设指标
- [ ] 火源辐射/对流比例口径确定
- [ ] 正式准稳态与时间平均结果完成

结论：阶段一的代码、输入生成和短验证链路已按 FDS 6.10.1 重构；阶段一的正式数值研究结论尚未完成。
