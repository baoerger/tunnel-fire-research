# §1.5 外部试验验证报告

状态：**等待外部 FDS 6.10.1 结果，不得形成复现精度结论。**

## 固定输入与真实观测

| CHID | 提交输入 | 真实观测剖面 | FDS 结果质量检查 | 指标输出 |
|---|---|---|---|---|
| `Arup_Tunnel` | `outputs/external_inputs/Arup_Tunnel/Arup_Tunnel.fds` | `observations/Arup_Tunnel_temperature_profile.csv` | 待回传 | 待计算 |
| `CSTB_Tunnel_Test_2` | `outputs/external_inputs/CSTB_Tunnel_Test_2/CSTB_Tunnel_Test_2.fds` | `observations/CSTB_Tunnel_Test_2_temperature_profile.csv` | 待回传 | 待计算 |
| `IFAB-07` | `outputs/external_inputs/IFAB-07/IFAB-07.fds` + `mesh.txt/output.txt` | `observations/IFAB-07_temperature_profile.csv` | 待回传 | 待计算 |

来源版本、测点坐标、温度类型、基线和比较窗口见 `外部试验候选清单.md`、`external_cases_template.csv` 与 `measurement_mapping.csv`。

## 回传后的固定顺序

1. 将完整工况目录放入 `outputs/runs/<chid>/`。
2. 运行 `python src/check_fds_results.py --chids <chid>`；`FAIL` 不进入分析，`REVIEW` 先逐条处理。
3. 从官方输入的同名 `THERMOCOUPLE` 设备提取与实测完全相同的时间窗和坐标，导出两列 `x_rel_fire_m,dT_mean_C`。
4. 运行 `validation_metrics.py`，保存到 `outputs/analysis/validation/`。
5. 在下表登记原始路径、脚本、指标和图，而不是只写结论。

## 结果登记（当前均未计算）

| CHID | Mean Bias (°C) | NRMSE | 相对离散度 | R² | 峰值位置误差 (m) | 温度剖面图 | 判定 |
|---|---:|---:|---:|---:|---:|---|---|
| `Arup_Tunnel` | — | — | — | — | — | — | 等待外部结果 |
| `CSTB_Tunnel_Test_2` | — | — | — | — | — | — | 等待外部结果 |
| `IFAB-07` | — | — | — | — | — | — | 等待外部结果 |

上游回流长度、高温区位置和温度非对称性只有在相应输出可用时分别评价；不能用单条顶棚温度剖面冒充这些结论。阶段一外部验证是否达标须基于三组真实回传结果综合判定。
