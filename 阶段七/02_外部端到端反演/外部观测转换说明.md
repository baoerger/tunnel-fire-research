# 外部观测到反演输入转换说明

状态：**三组真实观测已可追溯转换；正式反演和误差评价保持等待。**

`阶段七/src/external_observations.py` 联合读取：

- `阶段一/05_外部试验复现/external_cases_template.csv`；
- `阶段一/05_外部试验复现/measurement_mapping.csv`；
- `阶段一/05_外部试验复现/observations/external_temperature_profiles.csv`。

转换结果 `external_inversion_inputs.jsonl` 包含 Arup 9 点、CSTB 6 点、IFAB-07 11 点，记录已知 Q、x_f、U、Df、H、W、基线温度、测点坐标、标准差、不确定性口径、原始文件和参考文献。环境温度由每个测点 `T_mean-dT_mean` 的中位数恢复。

所有观测的 `observation_temperature_type` 保持为 `thermocouple`，模型目标另列为 `ceiling_gas_temperature`，兼容状态为 `REQUIRES_RESPONSE_MODEL_OR_EXPLICIT_DISCREPANCY`。不得把这些热电偶数据直接改名为主体气体温度。

`external_end_to_end_evaluation.csv` 预先记录真值、几何和误差列，但 Q/x 预测和误差全部为空，状态为 `WAITING_FORMAL_MODEL_FDS_VALIDATION_AND_THERMOCOUPLE_TREATMENT`。只有阶段四正式模型、阶段六设置和热电偶差异处理固定后才能填表；当前没有外部反演精度结论。
