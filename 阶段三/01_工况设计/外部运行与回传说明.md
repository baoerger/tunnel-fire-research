# 阶段三 100 m 条件域 V2 外部运行与回传

本批登记 68 个物理工况，均可由阶段一生成器从
`database_cases_candidate.csv` 生成到
`C:\Users\xiao.cheng\Desktop\科研\阶段一\outputs\stage3_100m_v2_inputs\`。
逐 CHID 的完整路径和文件清单位于 `external_run_manifest_100m_v2.csv`。其中
12 个 `role=pilot` 已有质量通过的正式 FDS 6.9.1 结果，直接复用
`阶段一\outputs\pilot_input\<chid>\`，`external_run_required=no`；不得重复计算。
其余 56 个为本轮新增外部计算。

每个 `external_run_required=yes` 的 CHID 在服务器使用独立工作目录：

```bash
mkdir -p /project/fds_tunnel/stage3_100m_v2/<chid>
cd /project/fds_tunnel/stage3_100m_v2/<chid>
mpiexec -n 22 fds <chid>.fds
```

输入均有 22 个 MESH，MPI 进程数不得超过 22；正式采用的 MPI/OpenMP 组合仍
应以目标平台短试算性能为准。本机不运行本批正式 FDS。

至少上传对应 `<chid>.fds`。计算完成后至少回传实际执行的 `<chid>.fds`、
`<chid>.out`、`<chid>_devc.csv`、`<chid>_hrr.csv` 到；建议同时保留
`<chid>.end`：

```text
C:\Users\xiao.cheng\Desktop\科研\阶段一\outputs\runs\<chid>\
```

若缺 `.end`，仍须满足项目规定的 `.out` 正常结束及两类 CSV 达到 `T_END`
替代证据。需要 Smokeview、全场积分或边界分析的代表工况再回传 `<chid>.smv`、
`<chid>_*.sf*` 和 `<chid>_*.bf`。

12 个 `independent_test` 可以同时计算，但结果用途必须保持
`SEALED_UNTIL_FINAL_EVALUATION`，在闭合公式、反演设置和网络超参数冻结前不得
读取其科学指标。回传后先运行：

```bash
uv run --python 3.12 python 阶段一/src/check_fds_results.py
uv run --python 3.12 python 阶段三/src/batch_postprocess.py --n-boot 2000
```

质量或准稳态未通过的 CHID 不进入时间平均数据库；不得用合成数据替代。
