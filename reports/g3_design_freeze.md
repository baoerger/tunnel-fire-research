# G3 设计、隔离、哈希与批算冻结

## 结论

设计、数值、资源和隔离四项审计全部通过。开发集 19 个物理工况、27 次运行已经生成和登记，可进入 G4 外部计算；扩展池 4 个工况按 G2 决定保持禁用。锁箱 10 个物理工况、14 次运行只完成输入与哈希封存，当前禁止运行和读取。

## 固定口径

- 全部正式输入统一采用 `dx=0.25 m`、21 个镜像 MESH、`T_END=300 s`。
- 测温只保留冻结的 `T90` 层，速度测点保留 `U95`；不再输出 G2 的 T85/T95。
- 五个 D1 主种子代表工况使用 heavy 输出，其余开发工况使用 light 输出。
- 峰值结论继续携带 13% 网格不确定性，不声称严格网格无关。

## 开发批次

| 批次 | 次数 | 科学接收顺序 |
|---|---:|---|
| D1 | 5 | 三个中心代表条件和一对中点偏移，优先接收 |
| D2 | 6 | 其余中心 3×3 工况 |
| D3 | 16 | 其余偏移与四个条件的种子重复 |

完整文件、SHA-256、回传位置和返回文件要求见 `reports/g3_development_run_handoff.csv`。服务器可以合批计算 D1–D3，但本地科学分析仍按 D1→D2→D3 解封。

D1 CHID：

- `d_q018_s35_x50_g25_r104729`
- `d_q027_s45_x40_g25_r104729`
- `d_q027_s45_x50_g25_r104729`
- `d_q027_s45_x60_g25_r104729`
- `d_q036_s50_x50_g25_r104729`

## 资源冻结

按已完成的 0.25 m、300 s 先导运行换算 60 s 基准并加入 25% 余量，开发集预计 1167.7 核时，尚未运行的开发加锁箱预计 1778.2 核时、36.4 GiB。完整 57 条科学轨迹的 G3 冻结预算为 2270.2 核时，绝对上限为 3064.8 核时。

## 锁箱隔离

锁箱输入、CSV 与哈希已经封存；协议中的 generation/preparation/run/read 四个权限将在本次冻结后全部关闭。G8 显式解锁前，不得上传计算、读取输出或用于训练和调参。
