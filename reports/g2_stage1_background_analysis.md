# G2 阶段 1：三个背景长算分析

> 分析日期：2026-08-19  
> 阶段结论：`G2_STAGE1_BACKGROUND_PASS`  
> 当时下一状态：`G2_PILOT_STAGE2_EXTERNAL_FDS_RUN_REQUIRED`
> 2026-08-19 调度补充后当前状态：`G2_PILOT_REMAINING_BATCH_EXTERNAL_FDS_RUN_REQUIRED`

## 结论

三个 `dx=0.25 m`、`T_END=300 s` 背景工况均由 FDS 6.10.1（Revision `FDS-6.10.1-0-g12efa16-release`）以 21 MPI × 1 OpenMP 完成。输入 SHA-256 与冻结清单一致，三个工况均正常到达 300 s，未检出 `ERROR`、`WARNING`、`rejected`、异常终止或伪 HRR。

自动结果检查为 `PASS=3, REVIEW=0, FAIL=0`。每个工况的 `_devc.csv`、`_hrr.csv` 和 `.smv` 均存在且可解析；每个 `.smv` 引用 105 个场文件，缺失数为 0。本机 FDS 未生成 `.end`，以 `.out` 成功语句和 DEVC/HRR 到达 `T_END` 作为协议允许的替代完成证据。

因此，G2 阶段 1 背景门通过。原记录先放行阶段 2 的 18 MW 与 36 MW 中网格 heavy 长算，阶段 2 随后已独立完成。2026-08-19 经用户明确批准，当前外部调度改为阶段 3–4 剩余 11 条一次合批，但科学审计仍按 3→4 串行，后置结果不得越过前置门。此结论只建立背景偏流和数值温度地板，不形成火灾稳态、网格或公式结论。

## 背景地板与偏流

按三个种子最后 60 s（240–300 s）汇总全部 147 个温度设备和 49 个纵向速度设备：

| 指标 | 三种子合并结果 |
|---|---:|
| 平均温度偏差 | -0.00102 ℃ |
| 温度偏差标准差 | 0.000774 ℃ |
| `abs(ΔT)` 99% 分位数 | 0.002425 ℃ |
| 最大 `abs(ΔT)` | 0.00283 ℃ |
| 平均纵向速度 | -0.000251 m/s |
| `abs(U)` 99% 分位数 | 0.005313 m/s |
| 最大 `abs(U)` | 0.005814 m/s |

依据协议：

```text
DeltaT_det = max(0.5 K, background_abs_deltaT_p99, 3*background_deltaT_sd)
           = max(0.5, 0.002425, 0.002322)
           = 0.5 K
```

三个种子的背景温度偏差和纵向速度都远低于后续火灾判据量级，未发现需要阻断阶段 2 的背景偏流。

## 运行与 Git 边界

- 三项 wall-clock 分别为 198.668 s、195.006 s 和 194.905 s。
- 每项输出约 12.33 MiB，三项合计约 37 MiB。
- 原始 `runs/` 继续由 `.gitignore` 排除；Git 只保存本报告、质量表、背景指标及关键文件 SHA-256。
- 阶段 2 只放行 `p_q018_s40_x50_g25_r104729` 与 `p_q036_s40_x50_g25_r104729`，其他阶段仍不得运行。
