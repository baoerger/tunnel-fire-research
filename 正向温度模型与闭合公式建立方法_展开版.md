# 正向温度模型与闭合公式建立方法（展开版）

## 一、这部分研究到底要解决什么问题

本研究要建立的不是一个只能拟合单个 FDS 工况的温度曲线，而是一个可以跨工况使用的显式正向模型：

\[
\Delta T(x)=F_{\mathrm{physics}}(Q,x_f,U,D_f,H,x)
\]

其中，`Q` 是火源热释放率，`x_f` 是真实火源位置，`U` 是纵向风速，`D_f` 是等效火源直径，`H` 是隧道高度，`x` 是顶棚温度测点位置。模型输出顶棚附近中心线气体温升 `ΔT(x)`，绝对温度为：

\[
T(x)=T_0+\Delta T(x)
\]

通俗地说，这一部分要回答三个问题：

1. 给定火源功率、位置和风速，隧道顶棚各位置会有多热？
2. 温度峰值为什么会偏离真实火源位置，上下游为什么以不同速度衰减？
3. 能否把这个温度公式反过来，用少量温度测点估计火源功率和位置？

本研究采用“物理解析骨架＋数据驱动闭合”的方式。温度随位置变化的基本形式由输运方程推导，不交给机器学习任意猜测；FDS 数据只负责确定解析模型中尚不知道的四个闭合关系。

## 二、必须首先说明的当前研究状态

以下内容是本项目已经落实到代码中的建模与拟合方法，但目前还不能把其中的闭合系数写成最终科研结果。

- 正式证据基线为 FDS 6.9.1。
- 当前研究范围限定为 `100 m × 10 m × 5 m` 矩形隧道、`dx=0.25 m`、顶棚气体温度测点高度 `z=0.90H=4.5 m`、纵向观测区 `x=15–85 m`。
- 原统一分段指数协议 V1 在12个先导工况中只有 `4/12` 通过，正式结论是 `FAIL`，不能改写为已经验证成功。
- 现采用允许单侧删失的100 m条件域协议 V2。现有12个工况参与过 V2 的形成，不能再视为独立验证。
- 50个开发工况目前只有12个真实结果，另38个尚待回传。因此，最终闭合公式的结构、系数和独立测试精度均未定稿。
- 当前已经完成的是解析核、参数提取、四类结构化回归、符号表达式安全检查、正向计算和反演的软件框架。
- 当前还没有由 PySR 或遗传编程产生并通过真实数据验证的最终符号公式；阶段六目前使用的也是合成常数闭合，不能称为真实闭合模型。

因此，论文中应写“拟建立”“将比较”“待50个开发工况完成后确定”，不能写成“已经获得最终公式及系数”。

## 三、实验数据如何转化为温度模型输入

### 3.1 FDS工况提供什么数据

每个工况至少包含：

- 目标火源热释放率 `Q`；
- 真实火源位置 `x_f`；
- 纵向风速 `U`；
- 等效火源直径 `D_f`；
- 顶棚中心线多个位置的气体温度时序；
- `_hrr.csv` 中的实际 `HRR` 和 `Q_RADI`；
- 工况几何、网格、FDS版本和删失类别等元数据。

准稳态窗口确定后，对每个测点的温度时序进行时间平均，并计算：

\[
\Delta T_i=\overline{T_i}-T_0
\]

温差用摄氏度或开尔文表示时数值相同，但无量纲化中的环境绝对温度 `T_0` 必须使用开尔文。

### 3.2 对流火源功率不能只取名义值

闭合公式使用的是对流火源无量纲功率。若辐射份额采用 `\chi_r`，则：

\[
Q_c=(1-\chi_r)Q
\]

更严格的正式处理应从 FDS 准稳态 `_hrr.csv` 计算：

\[
\chi_r=-\frac{Q_{RADI}}{HRR},
\qquad
Q_c=HRR+Q_{RADI}
\]

这里 FDS 的 `Q_RADI` 通常为负值。不能把 `_hrr.csv` 中的计算域 `Q_CONV` 直接当成经验公式所需的火源对流热释放率。

只有在确认 `\chi_r` 在研究域内足够稳定时，才可统一采用预设值 `\chi_r=0.30`。

## 四、温度模型基于什么方程建立

### 4.1 一维有效输运方程

研究从顶棚高温烟气层的稳态能量平衡出发，将复杂三维流动、湍流混合、卷吸和壁面散热压缩为一维有效模型：

\[
D_e\frac{d^2\theta}{dx^2}
-U_e\frac{d\theta}{dx}
-K_e\theta
=-S_f(x)
\]

其中：

- `θ(x)=T(x)-T_0` 为顶棚附近气体温升；
- `U_e` 为高温烟气沿隧道纵向输运的有效参数；
- `D_e` 为湍流混合、烟气层扩散等作用形成的有效扩散参数；
- `K_e` 为壁面换热、卷吸稀释等形成的综合热损失参数；
- `S_f(x)` 为火羽流到达顶棚后形成的等效热源。

这里的 `U_e、D_e、K_e` 是用于描述顶棚温度剖面的有效参数，不应直接解释成某一个局部测点的真实速度、分子扩散系数或单独的换热系数。

### 4.2 点源假设与分段指数解析解

远离火源近场时，先把顶棚热源近似为位于连续温度峰值 `x_p` 的点源：

\[
S_f(x)=S_0\delta(x-x_p)
\]

解析解为：

\[
\theta(x)=\theta_p e^{k_u(x-x_p)},\qquad x<x_p
\]

\[
\theta(x)=\theta_p e^{-k_d(x-x_p)},\qquad x\ge x_p
\]

其中：

\[
k_u=\frac{\sqrt{U_e^2+4D_eK_e}+U_e}{2D_e}
\]

\[
k_d=\frac{\sqrt{U_e^2+4D_eK_e}-U_e}{2D_e}
\]

\[
\theta_p=\frac{S_0}{\sqrt{U_e^2+4D_eK_e}}
\]

`k_u` 和 `k_d` 分别控制上游和下游的衰减。风沿 `+x` 方向时，通常有 `k_u>k_d`：上游温度下降较快，下游烟气被风输送得更远。

这不是凭经验随意选两条指数曲线。两个指数衰减率是同一个对流—扩散—热损失方程的两个特征根，并满足：

\[
k_u-k_d=\frac{U_e}{D_e},
\qquad
k_uk_d=\frac{K_e}{D_e}
\]

### 4.3 为什么要使用无量纲形式

定义：

\[
\xi=\frac{x-x_p}{H},
\qquad
\phi=\frac{\theta}{T_0}
\]

\[
Pe_e=\frac{U_eH}{D_e},
\qquad
Da_e=\frac{K_eH^2}{D_e},
\qquad
\Pi_S=\frac{S_0H}{D_eT_0}
\]

得到无量纲衰减率：

\[
\kappa_u=Hk_u
=\frac{\sqrt{Pe_e^2+4Da_e}+Pe_e}{2}
\]

\[
\kappa_d=Hk_d
=\frac{\sqrt{Pe_e^2+4Da_e}-Pe_e}{2}
\]

最终的无量纲温度核为：

\[
\phi(\xi)=
\frac{\Pi_S}{\sqrt{Pe_e^2+4Da_e}}
\begin{cases}
e^{\kappa_u\xi},&\xi<0\\
e^{-\kappa_d\xi},&\xi\ge0
\end{cases}
\]

这个形式自动包含三个重要物理性质：

1. `Fr→0` 时若 `Pe_e→0`，则 `κ_u=κ_d`，无风时温度剖面趋于左右对称；
2. `Da_e>0` 时温度随远离峰值而衰减；
3. `|x-x_p|→∞` 时 `ΔT→0`，温度回到环境温度。

### 4.4 点源近场不够好时怎么办

点源解在 `x_p` 两侧的导数不连续。若50个开发工况表明峰值附近存在稳定的宽平台或系统残差，则使用高斯有限源作为备选：

\[
S_f(x)=\frac{S_0}{\sqrt{2\pi}\sigma_s}
\exp\left[-\frac{(x-x_p)^2}{2\sigma_s^2}\right],
\qquad \sigma_s=c_sD_f
\]

温度用格林函数卷积计算：

\[
\theta(x)=\int_{-\infty}^{\infty}G(x-s)S_f(s)\,ds
\]

`c_s` 必须是由开发集共同确定的全局参数，不能为每个工况单独调节。只有当有限源在分组交叉验证和峰值附近残差上显著优于点源时才采用，否则保留更简单的点源模型。

## 五、每个FDS工况的闭合参数如何计算

闭合公式不是直接拿全部温度点做一次黑箱拟合，而是先从每个合格工况提取具有物理意义的中间参数。

### 5.1 第一步：估计连续峰值，而不是直接取最高温测点

在离散最高温测点附近选择5个点，进行局部二次拟合：

\[
\Delta T(x)=a(x-x_c)^2+b(x-x_c)+c
\]

当 `a<0` 且顶点位于局部拟合窗口内时：

\[
x_p=x_c-\frac{b}{2a}
\]

\[
\Delta T_p=a(x_p-x_c)^2+b(x_p-x_c)+c
\]

这样得到连续的峰值位置和峰值温升，避免测点间距使峰值位置只能取离散坐标。核平滑可作为敏感性分析，但不与二次拟合结果混用。

真实火源位置与温度峰值位置分开定义：

\[
x_p=x_f+\delta_p,
\qquad
\frac{\delta_p}{H}=\frac{x_p-x_f}{H}
\]

`δ_p` 表示风和羽流弯曲造成的峰值偏移，它也是需要建立闭合公式的量。

### 5.2 第二步：排除火源近场并处理检测阈值

点源指数解主要描述远场，因此先排除：

\[
|x-x_p|<0.3H\sim0.5H
\]

现有程序默认使用 `0.4H`，正式取值必须由开发集交叉验证冻结。还要排除低于检测阈值的温升。目前工程传感器阈值候选为 `5 °C`，最终阈值应由实际测量链和FDS噪声共同确定：

\[
\Delta T_{th}=\max(\Delta T_{noise},\Delta T_{sensor})
\]

低于阈值的上游温度不等于真实温升为零，不能把这些点强行拟合成陡峭指数，应按左删失数据处理。

### 5.3 第三步：分别拟合上下游对数斜率

定义距峰值的正距离：

\[
r_i=
\begin{cases}
x_p-x_i,&x_i<x_p\\
x_i-x_p,&x_i>x_p
\end{cases}
\]

对每一侧分别拟合：

\[
y_i=\ln \Delta T_i=\alpha-kr_i
\]

普通最小二乘斜率为：

\[
\widehat{s}=
\frac{\sum_i(r_i-\bar r)(y_i-\bar y)}
{\sum_i(r_i-\bar r)^2}
\]

\[
\widehat{k}=-\widehat{s}
\]

上游得到 `k_u`，下游得到 `k_d`，并换算为：

\[
\kappa_u=Hk_u,
\qquad
\kappa_d=Hk_d
\]

每侧至少需要3个近场外且高于阈值的测点。拟合还要记录对数空间RMSE，不能只保存斜率。

### 5.4 第四步：由衰减率反算四个逐工况闭合目标

当上下游衰减率都可辨识且稳定时：

\[
Pe_e=\kappa_u-\kappa_d
\]

\[
Da_e=\kappa_u\kappa_d
\]

因为：

\[
\sqrt{Pe_e^2+4Da_e}=\kappa_u+\kappa_d
\]

所以有效源强为：

\[
\Pi_S=rac{\Delta T_p}{T_0}
\sqrt{Pe_e^2+4Da_e}
=\frac{\Delta T_p}{T_0}(\kappa_u+\kappa_d)
\]

峰值偏移为：

\[
\delta_p/H=(x_p-x_f)/H
\]

因此，每个完整可辨识工况最终提供四个闭合目标：

\[
\{Pe_e,Da_e,\Pi_S,\delta_p/H\}
\]

### 5.5 第五步：检查拟合窗口稳定性

不能只在一个人为选择的区间上拟合。程序允许预先固定多组窗口，例如：

\[
(0.3H,5.5H),\quad(0.4H,5.0H),\quad(0.5H,4.5H)
\]

分别重新估计 `κ_u` 和 `κ_d`。若相对变化超过20%，参数仍保留用于诊断，但其正式闭合训练掩码关闭：

\[
\max_m\left|
\frac{\kappa^{(m)}-\kappa^{(base)}}{\kappa^{(base)}}
\right|>0.20
\quad\Longrightarrow\quad
\text{不进入正式参数闭合拟合}
\]

20%是预设门，必须在正式分析前冻结，不能根据结果事后修改。

### 5.6 单侧删失如何处理

V2协议把工况分为：

- `bilateral_identifiable`：上下游都可稳定估计；
- `upstream_censored`：上游低于检测阈值，只能可靠使用下游信息；
- `downstream_domain_censored`：100 m计算域没有提供可辨识的下游远场，只能可靠使用上游信息；
- `no_obvious_backflow`：上游无明显回流，不能从数值小波动中制造一个衰减率。

缺失侧必须保留为空值并携带掩码，不能用零填充。只有两侧都可用时才能计算 `Pe_e、Da_e、Π_S`。单侧工况可保留其可靠的 `κ_u` 或 `κ_d` 用于结构诊断，完整温度剖面仍可进入第二阶段全曲线拟合。

### 5.7 参数不确定性如何给出

对时间窗口、温度噪声、测点缺失、测点位置误差和拟合窗口进行工况级Bootstrap。每次扰动后重新执行“峰值—斜率—闭合参数”整条链，最后报告参数中位数和2.5%–97.5%分位区间。若成功拟合比例低于80%，该工况不能给出稳定正式参数。

## 六、闭合公式到底是什么

解析核告诉了我们温度曲线的形状，但还不知道不同 `Q、U、D_f` 下 `Pe_e、Da_e、Π_S、δ_p/H` 分别是多少。把这四个量写成输入工况的函数，就是“闭合”。

### 6.1 闭合公式的三个输入

令：

\[
q=Q_c^*=\frac{Q_c}
{\rho_0c_pT_0\sqrt gH^{5/2}}
\]

\[
Fr=\frac{U}{\sqrt{gH}},
\qquad
d=\frac{D_f}{H}
\]

需要寻找：

\[
Pe_e=F_{Pe}(q,Fr,d)
\]

\[
Da_e=F_{Da}(q,Fr,d)
\]

\[
\Pi_S=F_S(q,Fr,d)
\]

\[
\delta_p/H=F_\delta(q,Fr,d)
\]

选择无量纲输入的原因是：减少量纲和尺度对回归的干扰，使公式能够表达相似准则，并避免产生单位不自洽的经验组合。

### 6.2 物理约束怎样硬编码进公式

定义：

\[
\operatorname{softplus}(z)=\ln(1+e^z)>0
\]

不直接回归四个物理量，而是回归四个无约束潜变量 `G_y`：

\[
Pe_e=Fr\,\operatorname{softplus}(G_{Pe})
\]

\[
Da_e=\operatorname{softplus}(G_{Da})+\varepsilon
\]

\[
\Pi_S=q\,\operatorname{softplus}(G_S)
\]

\[
\delta_p/H=Fr\,\operatorname{softplus}(G_\delta)
\]

这样无需在损失中反复处罚，就能保证：

- 无风时 `Pe_e=0`，上下游恢复对称；
- `Da_e>0`，热损失不会变成非物理负值；
- `Π_S>0`，且 `q→0` 时有效源强趋于零；
- 无风时峰值偏移为零。

训练时先把逐工况目标变换到潜变量空间：

\[
z_{Pe}=\operatorname{softplus}^{-1}(Pe_e/Fr)
\]

\[
z_{Da}=\operatorname{softplus}^{-1}(Da_e-\varepsilon)
\]

\[
z_{\Pi}=\operatorname{softplus}^{-1}(\Pi_S/q)
\]

\[
z_\delta=\operatorname{softplus}^{-1}[(\delta_p/H)/Fr]
\]

其中：

\[
\operatorname{softplus}^{-1}(v)=\ln(e^v-1),\qquad v>0
\]

`Fr=0` 时 `Pe_e` 和 `δ_p/H` 是结构性零值，不计算相应潜变量，也不把 `0/0` 填成数字参与拟合。

## 七、具体采用什么回归方式寻找闭合公式

### 7.1 第一层：四类结构化低复杂度回归

当前代码已经实现四类候选基函数。对任一闭合量 `y`，先拟合：

\[
G_y=\boldsymbol\beta_y^T\mathbf b(q,Fr,d)
\]

候选一，幂律/对数线性基：

\[
\mathbf b_1=[1,\ln q,\ln(1+Fr),\ln d]
\]

若这一族胜出，最终公式具有清晰的幂律解释。例如：

\[
Pe_e=Fr\,\operatorname{softplus}
[\beta_{Pe,0}+\beta_{Pe,1}\ln q
+\beta_{Pe,2}\ln(1+Fr)+\beta_{Pe,3}\ln d]
\]

候选二，受限非线性基：

\[
\mathbf b_2=[1,\ln q,\ln(1+Fr),\ln d,
\sqrt q,Fr/(1+Fr),d]
\]

候选三，二阶响应面：

\[
\mathbf b_3=[1,q,Fr,d,q^2,Fr^2,d^2,qFr,qd,Frd]
\]

候选四，加性GAM型基：

\[
\mathbf b_4=[1,\ln q,\sqrt q,q,Fr,Fr^2,
\ln d,d,d^2]
\]

### 7.2 系数如何实际求解

对经过参数掩码筛选后的潜变量目标 `z`，使用带轻微岭正则的最小二乘：

\[
\widehat{\boldsymbol\beta}
=(\mathbf B^T\mathbf B+\lambda\mathbf I)^{-1}
\mathbf B^T\mathbf z
\]

当前程序默认 `λ=10^{-8}`。四个输出分别拟合，各自只使用该目标可用的工况；缺失参数不会被当作零。当前实现对所有系数使用同一岭项。

模型选择采用按完整物理工况分组的五折交叉验证，同一FDS工况派生出的时间窗、Bootstrap样本或缺测版本不得跨入训练折和验证折。每折先计算各闭合参数的中位相对误差，再汇总为该折误差，最终比较四类候选的交叉验证中位误差。

### 7.3 是否采用符号回归

答案是：本研究采用“物理约束公式发现”的总体框架，符号回归是其中的第二层候选，但目前尚未由符号回归得到最终科研公式。

符号回归不搜索整个温度函数：

\[
T=f(Q,U,D_f,x_f,x)
\]

而只搜索四个低维潜在闭合：

\[
G_{Pe}(q,Fr,d),\quad
G_{Da}(q,Fr,d),\quad
G_S(q,Fr,d),\quad
G_\delta(q,Fr,d)
\]

这种做法的核心是“方程结构由物理给定，未知闭合由数据发现”。它属于物理约束的符号公式发现，不是从零发现完整控制方程，也不是纯黑箱温度回归。

受限符号回归的搜索合同为：

- 变量：`Q_star、Fr、Df_over_H`；
- 运算：`+、−、×、÷、常数幂、sqrt、log1p、softplus`；
- 常数幂指数绝对值不超过3；
- 最大复杂度30个抽象语法树节点；
- 禁止三角振荡项、深层嵌套和无物理意义变量；
- 候选必须在整个研究域上通过有限值、奇点、正性和极限检查。

评分不只看训练误差，而是：

\[
J_{SR}=E_{CV}
+\lambda_CC_{expr}
+\lambda_PP_{physics}
+\lambda_SP_{instability}
\]

其中 `E_CV` 为分组交叉验证误差，`C_expr` 为公式复杂度，`P_physics` 为物理违约惩罚，`P_instability` 为不同随机种子和Bootstrap下的结构不稳定惩罚。

只有当四类简单回归都留下稳定系统残差时，才有必要运行 PySR、遗传编程或等价搜索器。若符号回归只带来很小的精度提升但公式明显更长，则仍选择简单结构化公式。

### 7.4 当前代码做到了哪一步

当前 `closure_models.py` 已实现：

- 四类结构化回归；
- `softplus`物理约束；
- 针对不同参数掩码的目标筛选；
- 五折完整工况分组交叉验证；
- 全温度曲线二阶段微调；
- 工况级Bootstrap；
- 符号表达式的变量、运算、幂指数、复杂度和研究域奇点检查。

当前代码尚未实现或尚未完成的是：

- 没有正式运行 PySR/遗传编程并产出最终表达式；
- 没有用齐全的50个真实开发工况确定公式和数值系数；
- 当前全曲线微调代码使用按单工况峰值归一化的均方误差和两轮坐标扰动搜索，尚未把正式的删失似然完全并入优化；
- 当前二阶段坐标微调从每个系数 `±0.05` 开始尝试，每轮将步长减半。这是软件通路验证，不应直接视为最终科研优化器。

正式计算时应在冻结的开发集上加入权重和删失似然，并使用稳定的有界优化器复核结果。

## 八、为什么要进行两阶段拟合

### 8.1 第一阶段：拟合逐工况闭合参数

先用第七节的方法，根据每个工况提取出的 `Pe_e、Da_e、Π_S、δ_p/H` 拟合闭合函数。这一步的优点是每个公式项都能和物理参数直接对应，也便于发现是源强、输运、损失还是峰值偏移没有拟合好。

### 8.2 第二阶段：把公式放回完整温度曲线

仅把闭合参数拟合得好，不保证最终温度曲线一定好。因此把四个闭合公式代回解析核，直接最小化全部开发工况的温度误差：

\[
\min_{\boldsymbol\beta}
\sum_{j,i}w_{ji}
\left[
\frac{T_{FDS,j}(x_i)-
F_{physics}(Q_j,x_{f,j},U_j,D_{f,j},x_i;\boldsymbol\beta)}
{s_j}
\right]^2
+\lambda_c\mathcal L_{cens}
\]

其中 `j` 表示工况，`i` 表示测点，`s_j` 用于平衡不同功率工况的温升尺度，`L_cens` 处理低于检测阈值的上游数据：

\[
\mathcal L_{cens,i}
=-\ln\Phi\left(
\frac{\Delta T_{th}-\widehat{\Delta T}_i}{\sigma_i}
\right)
\]

这样，单侧参数不可辨识的工况虽然不能提供完整闭合目标，但其实际观测温度仍能约束最终正向公式。

### 8.3 最终公式的选择原则

最终选择不以单次训练误差最小为唯一标准，而综合考虑：

1. 五折分组交叉验证误差；
2. 完整温度曲线NRMSE和分区残差；
3. 工况Bootstrap下公式结构和系数是否稳定；
4. 无风、零功率、远场和研究域边界是否满足物理极限；
5. 公式长度及可解释性；
6. 留功率、留风速、留火源尺寸和留位置区间的外推表现。

当两个模型精度相近时，选择更短、更稳定、物理含义更清晰的模型。

## 九、最终正向温度模型如何得到

闭合公式及系数冻结后，正向模型按以下固定顺序计算。

### 9.1 输入无量纲化

\[
q=\frac{Q_c}{\rho_0c_pT_0\sqrt gH^{5/2}},
\qquad
Fr=\frac{U}{\sqrt{gH}},
\qquad
d=\frac{D_f}{H}
\]

### 9.2 计算四个闭合量

\[
(q,Fr,d)
\longrightarrow
(Pe_e,Da_e,\Pi_S,\delta_p/H)
\]

具体计算为：先用最终选中的显式结构计算 `G_Pe、G_Da、G_S、G_δ`，再通过 `softplus` 物理变换得到四个闭合量。

### 9.3 计算温度峰值位置和衰减率

\[
x_p=x_f+H(\delta_p/H)
\]

\[
R=\sqrt{Pe_e^2+4Da_e}
\]

\[
\kappa_u=\frac{R+Pe_e}{2},
\qquad
\kappa_d=\frac{R-Pe_e}{2}
\]

### 9.4 生成任意位置的温度

\[
\Delta T(x)=T_0\frac{\Pi_S}{R}
\begin{cases}
\exp\left[\kappa_u\dfrac{x-x_p}{H}\right],&x<x_p\\
\exp\left[-\kappa_d\dfrac{x-x_p}{H}\right],&x\ge x_p
\end{cases}
\]

\[
T(x)=T_0+\Delta T(x)
\]

整个正向计算链可概括为：

```text
Q、U、Df
  ↓ 无量纲化
q、Fr、d
  ↓ 闭合公式
Pe_e、Da_e、Pi_S、delta_p/H
  ↓ 解析关系
xp、kappa_u、kappa_d、峰值温升
  ↓ 分段指数温度核
各测点温升 DeltaT(x)
```

这就是最终用于温度预测、火源反演、Fisher信息分析和神经网络物理解码的统一正向模型。

## 十、这个模型要与哪些公式和方法比较

本研究应固定比较五类模型，不能只与较弱的黑箱模型比较。

### 10.1 Kurioka等（2003）最大温升公式

定义：

\[
Q_K^*=\frac{Q}{\rho_0c_pT_0\sqrt gH_{eff}^{5/2}},
\qquad
Fr_K=\frac{U^2}{gH_{eff}}
\]

\[
P=\frac{(Q_K^*)^{2/3}}{Fr_K^{1/3}}
\]

则：

\[
\frac{\Delta T_{max}}{T_0}=
\begin{cases}
1.77P^{6/5},&P<1.35\\
2.54,&P\ge1.35
\end{cases}
\]

该公式只预测最大顶棚气体温升。它在 `U=0` 时存在奇点，不能用于无风分支，也不能人为扩展成纵向温度剖面。

### 10.2 Li、Lei和Ingason（2011）最大温升公式

定义：

\[
w^*=\left(\frac{Q_cg}{r\rho_0c_pT_0}\right)^{1/3},
\qquad
u'=\frac{U}{w^*}
\]

则：

\[
\Delta T_{max}=
\begin{cases}
17.5Q^{2/3}/H_{eff}^{5/3},&u'\le0.19\\
Q/(Ur^{1/3}H_{eff}^{5/3}),&u'>0.19
\end{cases}
\]

原公式的 `Q` 使用 kW，`c_p` 使用 kJ/(kg·K)，`r=D_f/2` 是火源半径，不是直径。程序接口必须显式换算单位。该式也只预测最大温升，不预测峰值位置和纵向剖面。

### 10.3 其余三类对照

除两种传统峰值公式外，还应比较：

1. **单工况独立上下游指数拟合**：用于判断分段指数能否描述单个剖面，但它没有跨工况预测能力；
2. **普通非线性温度回归**：直接用 `Q、U、D_f、x-x_f` 回归温度，作为无显式输运骨架的统计基线；
3. **直接完整温度符号回归**：让符号回归直接搜索 `T(Q,U,D_f,x_f,x)`，用于验证“先固定物理核、再发现闭合”是否更稳定。

本研究的第五类模型是“有效输运解析核＋物理约束闭合公式”。Kurioka和Li公式只能比较最大温升；全曲线NRMSE、峰值位置和上下游分区误差只能在能输出纵向剖面的模型之间比较。

### 10.4 正向评价指标

应报告：

- 全温度曲线NRMSE；
- 最大温升相对误差；
- 峰值位置误差；
- 上游和下游分区误差；
- 临界风速附近误差；
- 强风删失似然；
- 留功率、留风速、留尺寸、留位置区间误差；
- 无风对称极限、零功率极限和远场环境温度极限。

正式方案预设独立测试目标为：全曲线NRMSE中位数不超过15%，最大温升相对误差中位数不超过15%，峰值位置误差中位数不超过 `0.5H`。这些阈值必须在独立测试解封前冻结。

## 十一、温度正向模型如何用于反演火源功率和位置

当闭合公式冻结后，已知 `U、D_f、H` 和一组顶棚温度观测，未知量只有 `Q` 与 `x_f`。采用：

\[
\eta_1=\ln Q,
\qquad
\eta_2=x_f/H
\]

以保证功率为正并统一参数尺度。独立高斯噪声下的目标函数为：

\[
\mathcal L_{inv}=
\frac{1}{N}\sum_iw_i
\left[
\frac{\Delta T_i^{obs}-
F_{physics}(Q,x_f,U,D_f,H,x_i)}{\sigma_i}
\right]^2
+\lambda_c\mathcal L_{cens}
\]

若温度测点之间存在空间相关性，则使用：

\[
\mathcal L_{inv}=
(\mathbf y-\mathbf F)^T\Sigma^{-1}(\mathbf y-\mathbf F)
+\ln|\Sigma|
+\lambda_c\mathcal L_{cens}
\]

当前直接反演程序采用“二维粗搜索＋多初值局部细化”：

1. 在 `Q=5–100 MW`、`x_f=15–85 m` 内建立粗网格；
2. 计算每个候选点对应的完整温度曲线和损失；
3. 选取若干损失最低且不同的初值；
4. 在 `(ln Q,x_f/H)` 空间进行有界局部细化；
5. 比较多个局部解，输出 `Q_hat、x_f_hat`、重建曲线、残差和适用域标志。

功率主要控制温升幅值和有效源强，位置主要控制整个温度峰形在空间上的平移；但二者仍可能耦合。因此必须检查二维目标面、Fisher信息矩阵和置信区间，不能只报告一个优化器给出的点估计。

## 十二、与调研文献的具体关系

本研究对调研文献的使用不是简单照搬某篇文章的公式，而是吸收其公式发现策略，并将其改造成适合隧道火灾温度反演的流程。

| 调研工作 | 主要内容 | 对本研究的直接启示 |
|---|---|---|
| Pacheco-Vega等（2003） | 用符号回归从换热数据中建立显式换热关联式 | 说明符号回归适合寻找低维、可解释的工程关联式；本研究据此搜索四个无量纲闭合，而不是整条温度方程 |
| Weatheritt与Sandberg（2016） | 用进化算法发现RANS应力—应变代数修正 | 启示本研究把“已知控制结构”和“未知闭合”分开，只发现闭合部分 |
| El Hasadi与Padding（2019） | 面向稀疏流动数据的半监督符号回归 | 启示在FDS工况有限、部分参数删失时加入物理知识和可用性掩码 |
| Schmelzer、Dwight与Cinnella | 用稀疏符号回归发现代数雷诺应力模型 | 启示同时控制公式稀疏性、复杂度和可解释性，并通过完整流动预测验证闭合 |
| Chen、Liu与Sun（2021） | 在稀缺数据条件下利用物理信息学习控制方程 | 启示把物理残差、边界和极限作为学习约束，而不是只拟合数据点 |
| Xie等（2022） | 从稀疏测量中发现无量纲数和控制规律 | 直接支持本研究先构造 `q、Fr、D_f/H`，再寻找无量纲闭合关系 |
| Tang等（2023） | 深度学习辅助的显式湍流闭合符号回归 | 启示候选公式必须代回完整正向模型，用最终预测误差而不是单独闭合误差筛选 |
| Gurevich等（2024，SPIDER） | 从强湍流数据中发现稀疏、物理可解释的经验关系 | 启示使用局部平衡、稀疏结构、Bootstrap稳定性和物理筛选来抵抗湍流噪声 |
| Kurioka等（2003） | 纵向通风隧道火灾近场及最大顶棚温升经验关系 | 作为本研究最大温升传统基线之一 |
| Li、Lei与Ingason（2011） | 浮力烟气流最大顶棚温升分支公式 | 作为含低风与有风分支的最大温升传统基线 |

其中，Weatheritt、Schmelzer和Tang等工作的共同思想最接近本研究：不让算法任意生成完整物理问题，而是在已知方程骨架中发现未知闭合，并把闭合重新放回完整预测链验证。Xie等关于无量纲规律发现的工作，则直接支撑本研究使用无量纲输入和输出。

## 十三、可以在论文中使用的研究方法表述

可将本部分概括为：

> 本研究以顶棚热烟气纵向对流、有效扩散和综合热损失的稳态平衡为基础，建立一维有效输运方程，并由点源格林函数获得上下游非对称分段指数解析核。针对每个质量审查和准稳态审查通过的FDS工况，首先采用局部二次拟合估计连续温度峰值及其位置，在排除火源近场并处理检测阈值后，分别对上下游温升的对数衰减进行线性回归，提取无量纲衰减率 `κ_u` 和 `κ_d`，进而计算 `Pe_e、Da_e、Π_S` 及峰值偏移 `δ_p/H`。随后以对流火源无量纲功率 `Q_c*`、Froude数 `Fr` 和相对火源尺寸 `D_f/H` 为输入，通过softplus变换硬编码正性、无风对称和零功率极限，依次比较幂律、受限非线性、二阶响应面、GAM型回归和受限符号回归。模型先在逐工况闭合参数上拟合，再代回完整温度解析核进行全曲线二阶段校正，并通过完整工况分组交叉验证和工况Bootstrap筛选稳定、简洁的最终显式公式。冻结的正向模型随后用于顶棚温度预测以及火源功率和位置的直接反演。

## 十四、最终应形成的成果及当前空缺

正式完成后，本部分应形成：

1. 逐工况 `ΔT_p、x_p、κ_u、κ_d、Pe_e、Da_e、Π_S、δ_p/H` 参数表及Bootstrap区间；
2. 四类结构化回归和受限符号回归的五折分组交叉验证结果；
3. 四个最终闭合公式的明确结构、数值系数、适用域和不确定性；
4. 完整正向温度公式及可复现计算程序；
5. 与Kurioka、Li、独立指数、普通回归和直接温度符号回归的对照结果；
6. 在12个封存独立测试工况上的一次性正式评价；
7. 基于冻结正向公式的火源功率—位置反演与置信区间。

目前第1项只有部分真实工况可计算，第2至第7项的软件框架已经具备，但正式闭合公式、最终数值系数和真实独立精度必须等待剩余38个开发工况回传后才能确定。

## 参考文献

1. Pacheco-Vega, A., Cai, W., Sen, M., & Yang, K. T. *Heat Transfer Correlations in an Air-Water Fin-Tube Compact Heat Exchanger by Symbolic Regression*. Proceedings of the ASME Heat Transfer Division, 2003, 23–28.
2. Weatheritt, J., & Sandberg, R. D. A novel evolutionary algorithm applied to algebraic modifications of the RANS stress–strain relationship. *Journal of Computational Physics*, 2016. https://doi.org/10.1016/j.jcp.2016.08.015
3. El Hasadi, Y. M. F., & Padding, J. T. Solving fluid flow problems using semi-supervised symbolic regression on sparse data. *AIP Advances*, 2019. https://doi.org/10.1063/1.5116183
4. Schmelzer, M., Dwight, R. P., & Cinnella, P. Discovery of Algebraic Reynolds-Stress Models Using Sparse Symbolic Regression. *Flow, Turbulence and Combustion*. https://doi.org/10.1007/s10494-019-00089-x
5. Chen, Z., Liu, Y., & Sun, H. Physics-informed learning of governing equations from scarce data. *Nature Communications*, 2021. https://doi.org/10.1038/s41467-021-26434-1
6. Xie, X., Samaei, A., Guo, J., Liu, W. K., & Gan, Z. Data-driven discovery of dimensionless numbers and governing laws from scarce measurements. *Nature Communications*, 2022. https://doi.org/10.1038/s41467-022-35084-w
7. Tang, H., Wang, Y., Wang, T., & Tian, L. Discovering explicit Reynolds-averaged turbulence closures for turbulent separated flows through deep learning-based symbolic regression with non-linear corrections. *Physics of Fluids*, 2023. https://doi.org/10.1063/5.0135638
8. Gurevich, D. R., Golden, M. R., Reinbold, P. A. K., & Grigoriev, R. O. Learning fluid physics from highly turbulent data using sparse physics-informed discovery of empirical relations (SPIDER). *Journal of Fluid Mechanics*, 2024. https://doi.org/10.1017/jfm.2024.813
9. Kurioka, H. et al. Fire properties in near field of square fire source with longitudinal ventilation in tunnels. *Fire Safety Journal*, 2003. https://doi.org/10.1016/S0379-7112(02)00089-9
10. Li, Y. Z., Lei, B., & Ingason, H. The maximum temperature of buoyancy-driven smoke flow beneath the ceiling in tunnel fires. *Fire Safety Journal*, 2011. https://doi.org/10.1016/j.firesaf.2011.02.002
