# 直接反演软件验证报告

状态：**求解器完成；真实反演误差和置信区间等待正式闭合与独立测试。**

实现流程为 Q—位置粗网格搜索、保留若干低损失初值、在 `log Q` 与 `x_f/H` 中作有界局部细化、比较多初值结果并输出温度重建残差。强风检测限以下观测可通过左删失高斯损失处理。

单元测试使用常数潜变量的合成物理闭合生成观测，能恢复已知 `Q=42 MW`、`x_f=51.3 m`；这只证明软件对自身生成模型的参数恢复能力，不得作为 §30.2 的正式精度证据，也不能证明不存在真实多局部最优。

