# 当前实施进度

> 最后更新：2026-08-21
> 当前协议状态：科学协议 v1.1，**G5 正向公式已冻结 A，进入 G6 直接反演**
> 快速反演状态：`G7_SKIPPED_NOT_REQUESTED`

## 1. 当前结论

四个 G1 工况、G2 的 16 个先导工况及 G4 的 27 条开发运行均已完成。严格网格门失败证据继续保留，后续统一采用 `0.25 m` 工作网格并保留 13% 峰值网格不确定性。G4 按 D1→D2→D3 完成接收，27/27 质量与 60 s 稳态窗通过。G5 在 9 个 Q-D 父组的留一折外验证中选择 `M1_S1`：峰值保留功率与火源尺寸项，曲线使用平滑单区幂律；拉伸指数 S2 和两区 S3 均按预注册规则拒绝。折外峰值误差中位/90% 分位为 4.34%/11.21%，曲线 NRMSE 为 3.27%/4.48%，正向门通过并冻结 A。当前状态为 `G5_PASS_FREEZE_A_G6_DIRECT_INVERSION_REQUIRED`；锁箱仍未运行、未读取。

## 2. 已完成

- [x] 冻结两份当前主稿：科学主次定稿版和实施方案。
- [x] 将两份理论 Word 文档集中到 `docs/`。
- [x] 取消“阶段一至阶段七”旧目录结构，以项目根目录作为唯一研究根。
- [x] 将 FDS 配置、生成、读取和质量检查集中到 `src/fds/`。
- [x] 将网格、准稳态、Bootstrap 和曲线分析集中到 `src/analysis/`。
- [x] 将旧正向/反演方法降级为 `extras/legacy_modeling/` 脚手架。
- [x] 将快速反演代码隔离到 `extras/fast_inversion/`，当前不进入主体执行链。
- [x] 更新 `CLAUDE.md`、`.gitignore`、README 和两份主方案中的路径合同。
- [x] 删除旧阶段报告、旧有风设计、重复生成输入和约 6.95 GB 可再生仿真输出。
- [x] 删除后统一回归测试通过：`139/139`。
- [x] 建立精简版项目自动执行 Loop 提示词，冻结外部 FDS 交接、回传校验和同线程恢复规则；详细约束直接引用项目事实源，避免重复漂移。
- [x] 实现 `background|fire`、固定全局三层测点、按网格尺寸选择的镜像 MESH 拓扑和 light/heavy 输出合同。
- [x] 扩展背景/火灾结果质量门；无风准稳态改用左右传播长度，不使用旧有风回流长度。
- [x] 建立 `case_registry.csv`、`job_attempts.csv`、输入双哈希、RESTART 基础验证和锁箱拒绝守卫。
- [x] 生成并静态审计 4 个 G1 短试算；全部复制到独立 attempt 回传目录。
- [x] 本轮完整测试 `153 passed`，另有 26 个 subtests；唯一警告是 5 s 工况按设计仅用于启动检查。
- [x] 使用 FDS 6.10.1、21 MPI × 1 OpenMP 完成四个 G1 实跑；输入 SHA-256 全部匹配，四项均正常到达 `T_END`。
- [x] G1 自动质量检查为 `PASS=2, REVIEW=2, FAIL=0`；两个 `REVIEW` 仅因 5 s 工况短于 `3×TAU_Q`，按设计接受。
- [x] 27 MW/40 s 末段平均 HRR 为 26.847 MW，目标闭合误差 0.566%；357 个切片和 21 个边界文件完整。
- [x] 轻量结论、质量表、运行指标及关键输出 SHA-256 已保存到 `reports/g1_short_*`；约 480 MiB 原始结果继续留在被 Git 忽略的 `runs/`。
- [x] 按实施方案建立恰好 16 条 G2 先导矩阵：3 背景、6 网格、3 洞口/平移、4 种子重复。
- [x] 生成 16 个 `T_END=300 s` 输入，其中 5 个预注册代表工况使用 heavy 输出；静态审计和输入/attempt 哈希全部通过。
- [x] 扩展注册表以保存请求、离散和实际燃烧器字段、测点/MESH/输出档位及目标版本；G1 记录完整保留。
- [x] G2 本地准备后完整测试 `157 passed`，另有 42 个 subtests。
- [x] 完成 G2 阶段 1 三个 300 s 背景长算；质量检查 `PASS=3, REVIEW=0, FAIL=0`，输入哈希、版本、时间轴和场文件均通过。
- [x] 使用三个种子末 60 s 背景数据确定 `DeltaT_det=0.5 K`；平均纵向速度为 -0.000251 m/s，未发现阻断性背景偏流。
- [x] 完成 G2 阶段 2 的 18/36 MW 中网格 heavy 长算；联合质量检查 `PASS=2, REVIEW=0, FAIL=0`，两项 HRR、准稳态和完整场输出均通过。
- [x] 按用户明确要求同步修改科学主稿和实施方案，将 G2 剩余 11 条改为一次性外算；科学审计顺序、阈值、停止规则、身份链和锁箱隔离保持不变。
- [x] 整理并验收阶段 3 的 10 个网格/种子工况；全部到达 300 s，无 ERROR/WARNING/rejected，输入与关键输出完整。
- [x] 修正正式温度曲线读取规则，优先使用 `T90_*`，并把准稳态同步平均窗由旧默认 30 s 改为协议规定的 60 s。
- [x] 完成阶段 3 定量分析：18/36 MW 种子门通过，网格门因峰值温升中-细差超限而失败。
- [x] 按用户决定冻结 `0.25 m` 工作网格，保留 13% 峰值网格不确定性，并放行阶段 4。
- [x] 整理并验收阶段 4 三个洞口/平移工况；输入哈希匹配，均正常到达 300 s，结果质量检查 `PASS=3, REVIEW=0, FAIL=0`。
- [x] 完成 2000 次同步时间块 Bootstrap 和平移分析；`xi_h=5/4/3` 全部通过，按规则选择最大有效域 `xi_h=5`，保留 `b_open=3H` 和 `x_f=40–60 m`。
- [x] 汇总 16 个先导的运行身份、输入哈希、实际版本、脚本哈希和物理证据；冻结 `z/H=0.90`、60 s 平均窗、300 s 最小充分时长、0.5 °C 检测限及单一撞顶/近撞顶层级。
- [x] 由 G2 冻结自动生成 G3 开发、禁用扩展池和锁箱清单；41 份正式输入全部通过 MESH、燃烧器、CHID、路径、设备和输出档位静态审计。
- [x] 开发设计矩阵达到 `rank=4/4`，标准化条件数 1.008；九个 Q-D 父组满秩，五个位置父组均有完整 40/50/60 m 配对。57 条接受科学轨迹占 61 条上限；计入一次粗网格失败启动后，预计累计进程启动为 58/79。
- [x] 冻结 27 个开发工况的 D1/D2/D3 顺序、双哈希和唯一回传目录；14 个锁箱输入只生成封存，生成、准备、运行和读取权限全部关闭。
- [x] 整理第三方服务器回传的 27 个开发工况；输入哈希全部匹配，全部正常到达 300 s，无 FDS ERROR/WARNING/rejected。
- [x] 按 D1→D2→D3 完成 G4 科学接收和 2000 次同步时间块 Bootstrap；冻结 19 个物理工况、27 条运行的不可变派生数据库。
- [x] 完成 M0/M1 峰值与 S1/S2/S3 形状候选的 9 折父组外层验证；选择 `M1_S1`并冻结公式、误差模型、适用域和 forward callable。

## 3. 当前闸门状态

| 闸门 | 状态 | 说明 |
|---|---|---|
| 仓库重构 | `PASS` | 新目录、导入和文档路径已统一 |
| G0 环境与协议冻结 | `PASS` | 协议升为 v1.1，正式版本冻结为 FDS 6.10.1 精确 Revision |
| G1 软件合同与短试算 | `PASS` | 四项实跑完成；无 ERROR/WARNING/rejected，27 MW HRR 闭合通过 |
| G2 先导与网格冻结 | `COMPLETE_WITH_USER_GRID_OVERRIDE` | 唯一冻结文件已生成；严格网格门失败证据保留，0.25 m 峰值保留 13% 不确定性 |
| G3 设计、隔离与资源冻结 | `PASS` | 27 个开发输入可外算；14 个锁箱输入保持封存 |
| G4–G6 主体正向与直接反演 | `G5_PASS_FREEZE_A_G6_REQUIRED` | `M1_S1` 正向公式已通过折外门；当前进入 G6 直接反演 |
| G7 快速反演 | `SKIPPED_NOT_REQUESTED` | 仅导师在锁箱打开前明确要求时激活 |
| G8 独立锁箱 | `SEALED_G3` | 输入与哈希已封存，尚未运行或打开 |

## 4. 当前阻断项

当前没有待回传的开发 FDS 工况。严格网格门失败和 FDS 6.9.1/6.10.1 版本偏差记录继续保留，但按用户决定不再阻断执行；峰值公式和误差带中仍保留 13% 网格不确定性。当前待办是 G6 复用冻结 A 的同一 forward callable、协方差和删失合同，完成 Q/x_f 直接反演折外验证与冻结 B。锁箱不得上传、运行或读取。

## 5. 下一步执行顺序

- [x] G0：冻结本地协议、环境和科学资源上限；服务器运维由用户自行管理。
- [x] G1：实现背景模式、全局测点、镜像 MESH 和运行身份链。
- [x] 补齐对应合同测试并保存 G1 输入/哈希清单。
- [x] 完成四个代表工况的 FDS 6.10.1 短试算和质量审计。
- [x] G2：冻结 16 条先导设计、输入、哈希、阶段顺序和回传目录。
- [x] G2 阶段 1：完成三个背景工况并审计背景偏流与检测限原始证据。
- [x] G2 阶段 2：完成 18/36 MW 中网格 heavy 工况并审计长算、HRR 与场输出。
- [x] G2 阶段 3 外算与本地计算：阶段 3 所需 8 个新增工况全部回传或完成，连同两个阶段 2 中网格参考共 10 项进入分析。
- [x] G2 阶段 3 审计：种子门通过，网格门失败，已保存轻量结果表和分析报告。
- [x] 工作网格决定：用户选择统一采用 `0.25 m`，并接受峰值最大约 13% 的网格不确定性。
- [x] G2 阶段 4 外算与审计：3 个 27 MW 洞口/平移工况全部通过，冻结有效域 `xi_h=5` 和洞口余量 `b_open=3H`。
- [x] 汇总全部先导并决定测高、有效域、误差地板和火焰状态层级。
- [x] 使用先导冻结生成并审计开发集、预留扩展池和隔离锁箱清单。
- [x] G4：回传 27 条开发运行，按 D1→D2→D3 完成技术/科学接收并冻结不可变数据库。
- [x] G5：完成峰值和形状模型的分组折外比较，选择 `M1_S1`并冻结 A。
- [ ] G6：完成开发集 Q/x_f 直接反演折外验证，通过后冻结 B。

## 6. 进度更新规则

每次完成一个闸门或出现协议变更时更新本文，并至少记录：日期、变更内容、测试/实跑证据、失败原因、受影响协议版本和下一项任务。没有 FDS 实跑证据时，不得把软件测试 `PASS` 写成物理闸门 `PASS`。

本文件只记录执行状态；科学定义以《无风隧道顶棚纵向温度_正向主导双向闭环实施方案_主次定稿版》为准，具体操作以《实施方案》为准。

## 7. 最近记录

- 2026-08-21：完成 G5 正向候选比较与冻结 A。峰值 M1 的功率/尺寸指数为 0.813/-0.596，尺寸指数 95% 区间整体位于无实质效应区间之外；形状选择 S1 平滑单区幂律。S2 因折外误差较大且触界被拒绝；S3 的 NLL/NRMSE 改善未达预注册 10%/2 个百分点门限且参数触界，拒绝两区主张。`M1_S1` 折外峰值误差中位/90% 分位 4.34%/11.21%，曲线 NRMSE 3.27%/4.48%，正向门通过；锁箱保持封存，进入 G6。
- 2026-08-21：完成 G4 开发集接收。第三方结果全部按唯一 attempt 目录归位，27/27 输入哈希匹配、正常到达 300 s，忽略版本差异后技术质量 `PASS=27`。27/27 找到合格 60 s 稳态窗，D1/D2/D3、4 组种子、5 组平移和 5 个 D1 重场火焰状态门均通过。冻结 19 个物理工况/27 条运行的派生数据库，锁箱仍未运行、未读取；状态转为 `G4_PASS_G5_FORWARD_MODEL_SELECTION_REQUIRED`。
- 2026-08-21：完成 G3 设计、隔离、哈希与资源冻结。自动生成 19 个物理开发工况/27 次运行、4 个禁用扩展池工况和 10 个物理锁箱工况/14 次运行；41 份输入统一使用 `dx=0.25 m`、21 MESH、`T90/U95` 和 `T_END=300 s`。设计矩阵、静态输入、资源和隔离四门全部通过；开发集按 D1/D2/D3=5/6/16 登记，锁箱权限全部关闭。状态转为 `G3_PASS_G4_D1_EXTERNAL_FDS_RUN_REQUIRED`。
- 2026-08-21：完成 G2 最终先导冻结。机器文件保存 16 个 CHID 的输入/运行核心哈希、实际版本及分析脚本哈希；冻结 `dx=0.25 m`、`z/H=0.90`、60 s 平均窗、`T_END=300 s`、检测限 0.5 °C、`xi_h=5`、`b_open=3H`、`x_f=40–60 m` 和单一撞顶/近撞顶火焰层级。S3 四工况扩展池不启用；进入 G3 设计生成与审计，尚未放行正式主体 FDS。
- 2026-08-21：整理第三方回传的三个阶段 4 工况到唯一 attempt 目录。输入哈希全部匹配；实际 FDS 6.9.1 结果均正常完成到 300 s，质量检查 `PASS=3, REVIEW=0, FAIL=0`。三个工况均找到合格 60 s 平均窗。与阶段 3 六个 0.25 m 代表工况合并后，`xi_h=5/4/3` 全部通过；最不利 `A_global` 点估计/95% 上限为 7.80%/11.70%，最差左右 NRMSE 为 4.41%，40/60 m 相对中心曲线 NRMSE 为 2.89%/2.67%。按规则选择 `xi_h=5`、`b_open=3H` 和 `x_f=40–60 m`，状态进入最终先导冻结。
- 2026-08-20：用户决定后续统一采用 `0.25 m` 工作网格，不追加 `0.20/0.125 m` 主体网格链。严格网格门失败证据原样保留，整体曲线和种子稳定性作为继续依据，峰值统一保留 13% 网格不确定性，不允许声称严格网格无关。状态进入 G2 阶段 4，等待三个 27 MW 洞口/平移工况。
- 2026-08-20：完成 G2 阶段 3 的 10 个网格/种子工况验收与分析。按用户要求忽略版本差异后质量检查 `PASS=10, REVIEW=0, FAIL=0`；10 项均具有合格 60 s 同步平均窗。18/36 MW 种子门全部通过，但中-细峰值温升差为 12.79%/8.34%，超过 7.5%，阶段结论为 `G2_STAGE3_FAIL_GRID_GATE`。同时修复温度读取器对 `T90_*` 的支持和准稳态分析器旧 30 s 默认值。
- 2026-08-20：补做版本敏感性分析。36 MW 的 6.9.1 中网格不同种子比两个 6.10.1 中网格均值高约 3.75%；用它与 6.9.1 细网格探索性比较时峰值差为 4.26%并通过各项限值，说明版本可能改变高功率判定。18 MW 的同版本粗/细峰值仍相差约 50.55%，网格敏感性迹象更强。建议只补算两个 6.9.1 中网格同种子对照。
- 2026-08-19：按用户明确要求把“服务器提交批次”与“科学分析闸门”分离，并同步修改科学主次定稿版和实施方案。阶段 1–2 已通过后，阶段 3–4 剩余 11 条冻结输入获一次性外算授权；回传后的科学分析仍严格按 3→4，阶段 3 失败时阶段 4 结果隔离。输入、哈希、阈值、停止规则、61/79 预算和锁箱规则均未改变。
- 2026-08-19：发现并修复 G2 阶段 3 两个 `dx=0.5 m` 粗网格的 Poisson 初始化错误。原 21 MESH CHID 保留为作废证据，新输入改为 18 MESH 且保持 40,000 个物理单元不变；FDS 6.10.1、18 MPI 启动试算正常完成，完整回归测试 `158 passed`。
- 2026-08-19：按用户明确要求把“服务器提交批次”与“科学分析闸门”分离，并同步修改科学主次定稿版和实施方案。阶段 1–2 已通过后，阶段 3–4 剩余 11 条冻结输入获一次性外算授权；回传后的科学分析仍严格按 3→4，阶段 3 失败时阶段 4 结果隔离。输入、阈值、停止规则、61/79 预算和锁箱规则均未改变；其中两个粗网格输入及哈希已由经验证的 18 MESH 修复版替换。
- 2026-08-19：完成 G2 阶段 2 的 18/36 MW 中网格 heavy 长算，均使用 FDS 6.10.1、21 MPI × 1 OpenMP 正常到达 300 s；联合质量检查 `PASS=2, REVIEW=0, FAIL=0`。HRR 闭合误差分别为 0.018% 和 0.105%；2026-08-20 按协议复核后的 60 s 准稳态时均窗分别为 137–197 s 和 103–163 s。每项 357 个切片与 21 个边界文件均无缺失。轻量证据进入 Git，约 1.60 GiB 原始结果继续留在 `runs/`。状态转为阶段 3，放行 8 个网格/种子工况。
- 2026-08-19：完成 G2 阶段 1 三个背景长算，均使用 FDS 6.10.1、21 MPI × 1 OpenMP 并正常到达 300 s；自动检查 `PASS=3, REVIEW=0, FAIL=0`。三个种子末 60 s 的 `abs(DeltaT)` 99% 分位数为 0.002425 ℃，温度标准差为 0.000774 ℃，协议检测限取 0.5 K；`abs(U)` 99% 分位数为 0.005313 m/s，无阻断性背景偏流。轻量证据进入 Git，约 37 MiB 原始结果仍留在 `runs/`。阶段 2 的 18/36 MW 中网格 heavy 长算现已放行。
- 2026-08-18：完成四个 G1 实跑和本机分析。背景及 27 MW/40 s 自动检查通过，18/36 MW 的 5 s 工况仅因设计时长被标为 `REVIEW` 并按短试算接受；无错误、警告或 rejected。27 MW 末段 HRR 闭合误差 0.566%，heavy 场文件引用无缺失。按用户版本决定发布协议 v1.1，正式证据版本改为 FDS 6.10.1；轻量分析进入 Git，原始 `runs/` 不回传。状态更新为 `G1_PASS_G2_READY`。
- 2026-08-18：新增并精简 `docs/项目自动执行Loop提示词.md`，接入 README。该文档只提供自动推进与外部 FDS 交接协议，详细规则引用现有事实源；不改变科学协议版本或任何物理闸门结论。
- 2026-08-18：完成 G0 本地部分：建立 `config/protocol_v1.json` 非授权草案、G0 环境/资源表和测试日志；修正变量合同中的 `xi`、运行身份和实际量口径；增加旧设计草案批量生成拒绝门。core 34/34、analysis 11/11 通过。尚未获得外部 FDS/集群信息或任何 FDS 实跑证据，G0 保持 `IN_PROGRESS`。
- 2026-08-18：统一保留回归测试 140/140 通过。
- 2026-08-18：按用户明确流程取消服务器信息请求和运行命令模板。后续由代理直接提供生成好的 `.fds`、CHID、哈希和本机回传目录，用户自行外算并放回结果。G0 记为 `PASS_LOCAL`，当前进入 G1；尚无任何 FDS 实跑或物理门通过。
- 2026-08-18：完成 G1 本地实现：新增背景/火灾质量门、无风左右传播准稳态诊断、身份/attempt/RESTART 基础合同和锁箱守卫；完整测试 153/153 通过。生成背景 5 s、18 MW 5 s、36 MW 5 s、27 MW 40 s（heavy）四个输入，静态审计均通过，哈希及回传目录见 `reports/g1_short_run_handoff.csv`。状态转为 `EXTERNAL_FDS_RUN_REQUIRED`；未获得任何 FDS 实跑结论。
- 2026-08-18：完成 G2 本地准备。`config/pilot_cases.csv` 恰含 16 条预注册长算，全部 `T_END=300 s`；生成输入、20 条累计 run/attempt 注册记录及 `reports/g2_pilot_run_handoff.csv`，复核哈希和目录无误。完整测试 157/157 通过。按阶段门只放行三个背景工况，状态转为 `G2_PILOT_STAGE1_EXTERNAL_FDS_RUN_REQUIRED`。
