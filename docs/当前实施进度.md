# 当前实施进度

> 最后更新：2026-08-18  
> 当前协议状态：科学协议 v1.1，**G1 已通过，进入 G2 先导准备**
> 快速反演状态：`G7_SKIPPED_NOT_REQUESTED`

## 1. 当前结论

四个 G1 工况已由 FDS 6.10.1（Revision `FDS-6.10.1-0-g12efa16-release`）完成实跑和质量审计。当前状态为 `G1_PASS_G2_READY`：背景、低/高功率启动、27 MW HRR 闭合及 heavy 输出合同通过，可以继续 G2 先导设计与输入准备。G1 仍只属于运行合同门，**不能解释为新公式、稳态温度结论或直接反演已经完成**。

## 2. 已完成

- [x] 冻结两份当前主稿：科学主次定稿版和实施方案。
- [x] 将两份理论 Word 文档集中到 `docs/`。
- [x] 取消“阶段一至阶段七”旧目录结构，以项目根目录作为唯一研究根。
- [x] 将 FDS 配置、生成、读取和质量检查集中到 `src/fds/`。
- [x] 将网格、准稳态、Bootstrap 和曲线分析集中到 `src/analysis/`。
- [x] 将旧正向/反演方法降级为 `extras/legacy_modeling/` 脚手架。
- [x] 将快速反演代码隔离到 `extras/fast_inversion/`，当前不进入主体执行链。
- [x] 更新 `CLAUDE.md`、`.gitignore`、README 和两份主方案中的路径合同。
- [x] 删除旧阶段报告、旧有风设计、重复生成输入和约 6.95 GB 可再生仿真输出。
- [x] 删除后统一回归测试通过：`139/139`。
- [x] 建立精简版项目自动执行 Loop 提示词，冻结外部 FDS 交接、回传校验和同线程恢复规则；详细约束直接引用项目事实源，避免重复漂移。
- [x] 实现 `background|fire`、固定全局三层测点、21 MESH 镜像拓扑和 light/heavy 输出合同。
- [x] 扩展背景/火灾结果质量门；无风准稳态改用左右传播长度，不使用旧有风回流长度。
- [x] 建立 `case_registry.csv`、`job_attempts.csv`、输入双哈希、RESTART 基础验证和锁箱拒绝守卫。
- [x] 生成并静态审计 4 个 G1 短试算；全部复制到独立 attempt 回传目录。
- [x] 本轮完整测试 `153 passed`，另有 26 个 subtests；唯一警告是 5 s 工况按设计仅用于启动检查。
- [x] 使用 FDS 6.10.1、21 MPI × 1 OpenMP 完成四个 G1 实跑；输入 SHA-256 全部匹配，四项均正常到达 `T_END`。
- [x] G1 自动质量检查为 `PASS=2, REVIEW=2, FAIL=0`；两个 `REVIEW` 仅因 5 s 工况短于 `3×TAU_Q`，按设计接受。
- [x] 27 MW/40 s 末段平均 HRR 为 26.847 MW，目标闭合误差 0.566%；357 个切片和 21 个边界文件完整。
- [x] 轻量结论、质量表、运行指标及关键输出 SHA-256 已保存到 `reports/g1_short_*`；约 480 MiB 原始结果继续留在被 Git 忽略的 `runs/`。

## 3. 当前闸门状态

| 闸门 | 状态 | 说明 |
|---|---|---|
| 仓库重构 | `PASS` | 新目录、导入和文档路径已统一 |
| G0 环境与协议冻结 | `PASS` | 协议升为 v1.1，正式版本冻结为 FDS 6.10.1 精确 Revision |
| G1 软件合同与短试算 | `PASS` | 四项实跑完成；无 ERROR/WARNING/rejected，27 MW HRR 闭合通过 |
| G2 先导与网格冻结 | `READY_TO_START` | 允许设计、生成和审计先导输入；正式长算仍需完成 G2 输入冻结与交接 |
| G3–G6 主体正向与直接反演 | `NOT_STARTED` | 等待 G1/G2 放行 |
| G7 快速反演 | `SKIPPED_NOT_REQUESTED` | 仅导师在锁箱打开前明确要求时激活 |
| G8 独立锁箱 | `NOT_STARTED` | 锁箱尚未生成或打开 |

## 4. 当前阻断项

当前没有阻止 G2 本地准备的外部阻断项。下一项工作是依据实施方案完成 16 次先导设计、生成与冻结；在输入清单、哈希和唯一 attempt 目录准备好以前，不得启动 G2 长算。原始 G1 结果未上传 Git，远端以 `reports/g1_short_analysis.md`、两份 CSV 和协议状态作为交接证据。

## 5. 下一步执行顺序

- [x] G0：冻结本地协议、环境和科学资源上限；服务器运维由用户自行管理。
- [x] G1：实现背景模式、全局测点、镜像 MESH 和运行身份链。
- [x] 补齐对应合同测试并保存 G1 输入/哈希清单。
- [x] 完成四个代表工况的 FDS 6.10.1 短试算和质量审计。
- [ ] G2：运行先导，决定网格、测高、有效域、误差地板和火焰状态子域。
- [ ] 只有 G2 通过后，才生成开发集和锁箱清单。

## 6. 进度更新规则

每次完成一个闸门或出现协议变更时更新本文，并至少记录：日期、变更内容、测试/实跑证据、失败原因、受影响协议版本和下一项任务。没有 FDS 实跑证据时，不得把软件测试 `PASS` 写成物理闸门 `PASS`。

本文件只记录执行状态；科学定义以《无风隧道顶棚纵向温度_正向主导双向闭环实施方案_主次定稿版》为准，具体操作以《实施方案》为准。

## 7. 最近记录

- 2026-08-18：完成四个 G1 实跑和本机分析。背景及 27 MW/40 s 自动检查通过，18/36 MW 的 5 s 工况仅因设计时长被标为 `REVIEW` 并按短试算接受；无错误、警告或 rejected。27 MW 末段 HRR 闭合误差 0.566%，heavy 场文件引用无缺失。按用户版本决定发布协议 v1.1，正式证据版本改为 FDS 6.10.1；轻量分析进入 Git，原始 `runs/` 不回传。状态更新为 `G1_PASS_G2_READY`。
- 2026-08-18：新增并精简 `docs/项目自动执行Loop提示词.md`，接入 README。该文档只提供自动推进与外部 FDS 交接协议，详细规则引用现有事实源；不改变科学协议版本或任何物理闸门结论。
- 2026-08-18：完成 G0 本地部分：建立 `config/protocol_v1.json` 非授权草案、G0 环境/资源表和测试日志；修正变量合同中的 `xi`、运行身份和实际量口径；增加旧设计草案批量生成拒绝门。core 34/34、analysis 11/11 通过。尚未获得外部 FDS/集群信息或任何 FDS 实跑证据，G0 保持 `IN_PROGRESS`。
- 2026-08-18：统一保留回归测试 140/140 通过。
- 2026-08-18：按用户明确流程取消服务器信息请求和运行命令模板。后续由代理直接提供生成好的 `.fds`、CHID、哈希和本机回传目录，用户自行外算并放回结果。G0 记为 `PASS_LOCAL`，当前进入 G1；尚无任何 FDS 实跑或物理门通过。
- 2026-08-18：完成 G1 本地实现：新增背景/火灾质量门、无风左右传播准稳态诊断、身份/attempt/RESTART 基础合同和锁箱守卫；完整测试 153/153 通过。生成背景 5 s、18 MW 5 s、36 MW 5 s、27 MW 40 s（heavy）四个输入，静态审计均通过，哈希及回传目录见 `reports/g1_short_run_handoff.csv`。状态转为 `EXTERNAL_FDS_RUN_REQUIRED`；未获得任何 FDS 实跑结论。
